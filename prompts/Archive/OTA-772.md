---
description: OTA-772 — Resolve STRATEGIES vs STRATEGY_DTE_REQUIREMENTS dual-source
allowed-tools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash
---

# OTA-772 — Resolve STRATEGIES vs STRATEGY_DTE_REQUIREMENTS dual-source

## Terminal context
- This terminal: **Terminal A** (the only terminal for OTA-766; the whole Feature is sequenced here).
- Concurrent terminals: **none**. Do not parallelize any OTA-766 story — they converge on `strategy_scorer.py` and the engine config seed.
- Cross-terminal dependencies: none. This is **prompt 1 of 6** in the OTA-766 chain. It runs first because everything downstream that routes "by strategy id" must read one canonical strategy source.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
cat claude_context/insight_engine-schema-ddl.md
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: insight_engine.md §2, Principle 2 (Strategy independence)**
Strategies are runtime config, not code branches. No strategy's logic may reference, depend on, or be defined in terms of any other strategy. No inheritance, no cross-strategy fallback, no shared mutable state. *Enforced by: engine routes strategies by config lookup only.* The canonical runtime strategy source is the `engine_strategies` table (OTA-680/OTA-695). Any in-code strategy-definition dict is legacy and is being retired.

**Source: insight_engine-schema-ddl.md §"engine_strategies"**
`engine_strategies` natural key is `(owner_app_id, strategy_key)`; OTA strategies are `owner_app_id = 'OTA'`. `strategy_key` is the canonical string handle (e.g. `'steady_paycheck'`, `'trend-rider'`). One row per strategy.

**Source: OTA-766 scope anchor**
Independence only. No new rule content is invented here. This story consolidates the *source of strategy definitions*; it does not change any strategy's behavior.

**Source: OTA-513 (Production Deployed) — "Consolidate STRATEGIES and STRATEGY_DEFINITIONS dicts in strategy_definitions.py"**
OTA-513 already consolidated `STRATEGIES` vs `STRATEGY_DEFINITIONS`. OTA-772 targets a *different* second dict — `STRATEGY_DTE_REQUIREMENTS`. Phase 0 must establish whether `STRATEGY_DTE_REQUIREMENTS` still exists as a separate source after OTA-513, or whether OTA-513 already folded it in (making this story verification-only).

## Phase 0 — Read-only discovery (HARD STOP before any edits)

Do not edit anything in Phase 0. Produce a report and STOP.

1. `cat` the file(s) referenced by OTA-513 — start with `app/analysis/strategy_definitions.py` (or wherever the post-OTA-513 canonical dict now lives). Confirm the actual current location.
2. Grep the repo for every strategy-definition source and every reader:
   ```powershell
   rg -n "STRATEGY_DTE_REQUIREMENTS|STRATEGIES\b|STRATEGY_DEFINITIONS" app
   rg -n "STRATEGY_DTE_REQUIREMENTS" app   # all read sites
   ```
3. Identify, by file:line, (a) the canonical strategy source post-OTA-513, (b) whether `STRATEGY_DTE_REQUIREMENTS` is a distinct dict and where it is defined, (c) every read site of each — especially the **strategy classifier** and the **scorer** (`score_all_strategies` / `strategy_scorer.py`).
4. Determine whether the live evaluation path still reads either in-code dict, or whether the engine now reads `engine_strategies` (OTA-695) and the dicts are vestigial.

**STOP report (paste back to Claude.ai):**
- Canonical source today + file:line.
- Does `STRATEGY_DTE_REQUIREMENTS` still exist as a separate dict? Where, and what fields does it carry that the canonical source does not?
- Every read site of each dict (classifier, scorer, anywhere else).
- Recommended canonical (keep one, delete the other) and the DTE fields that must migrate onto the survivor before deletion.
- Go / no-go: is this a real consolidation, already-satisfied verification-only, or does it need re-scoping?

Wait for Claude.ai's go decision. Do not proceed past Phase 0 without it.

## Scope (after go)
- Pick one source as canonical (default expectation: the dict the scorer/engine already reads). Migrate any unique fields from `STRATEGY_DTE_REQUIREMENTS` (the DTE requirement fields) onto the canonical source so nothing is lost.
- Repoint the strategy classifier and the scorer to read from the single canonical source.
- Delete the duplicate dict.

## Acceptance criteria
- Exactly one canonical strategy-definition source; the duplicate is deleted.
- The strategy classifier and the scorer read from the same source.
- Grep confirms no second strategy-definition dict remains: `rg -n "STRATEGY_DTE_REQUIREMENTS" app` returns nothing (or only a comment noting the consolidation).
- No strategy's DTE behavior changes — values preserved on the survivor.

## Out of scope
- Do not touch `score_all_strategies` routing logic or the per-family scorers — that is OTA-768 + OTA-767 (prompt 2).
- Do not migrate the in-code dict into the `engine_strategies` table here; this story only collapses the *in-code* dual source. (Full table-as-source cutover is the app-wiring feature, OTA-757.)
- No verdict-band, delta, cushion, or normalization changes.

## Verification steps
- `rg -n "STRATEGY_DTE_REQUIREMENTS" app` → empty.
- Run the existing strategy-classifier and scorer tests; confirm DTE-dependent behavior is unchanged (e.g. DTE eligibility per strategy).
- `pytest` the strategy/scoring test modules green.

## Commit instruction
I have been instructed to commit. Per project convention, **Don runs the commit manually** — do not run `git commit`. Stage nothing destructive; present the changed-file list and the exact message below, then ask: **Do you approve? (yes / no)**

## Coordination footer
**OK to continue to OTA-768-OTA-767.md**

## Commit message template
```
OTA-772 feat: collapse STRATEGIES/STRATEGY_DTE_REQUIREMENTS to one canonical strategy source; classifier and scorer read the same dict
```

## Recommended QA level
**Standard** — config consolidation with regression risk on DTE eligibility; rely on existing classifier/scorer tests plus a grep gate.
