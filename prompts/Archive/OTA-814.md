# OTA-814 — Verification sweep: confirm all audit §7.5 cleanups landed

## Terminal context
- This terminal: Terminal A (single stream)
- Concurrent terminals: none.
- Cross-terminal dependencies: **runs last.** Gates on OTA-813 being **deployed** (it resolves §7.5 #4). The other five §7.5 items are already deployed.

## Required reading
Before the sweep:

```
cat claude_context/CLAUDE.md
```
The audit (`rules-engine-audit-2026-05-20.md`) is NOT in `claude_context/` — the §7.5 findings are embedded below in full, so no external read is needed for them.

## Relevant Context — Do Not Deviate Without Escalation

This is a **read-only verification sweep.** Confirm each audit §7.5 code-smell finding is resolved, note the resolving ticket, and surface anything NOT resolved as a **new story** — do not fix anything inline.

The six §7.5 findings and their dispositions (embedded from `rules-engine-audit-2026-05-20.md` §7.5):

- **§7.5 #1 — dead `StrategyDefinition` fields.** Resolved by **OTA-778**. Confirm the unread fields are gone: `name`, `delta_min`, `delta_max`, `iv_rank_min`, `credit_pct_min`, `credit_pct_max`, `exit_profit_pct`, `exit_loss_multiplier`.
- **§7.5 #2 — duplicate EV filter path** (was `vertical_engine.py:265`, `ev_raw >= min_ev_threshold`). Resolved by **OTA-725** (deployed). Confirm one EV path remains — the registered NegativeEVGate formula — and the inline scanning filter is gone.
- **§7.5 #3 — three identical `normalize()` implementations** (`strategy_scorer.py`, `vertical_engine.py`, `long_call_engine.py`). Resolved by **OTA-779**. Confirm a single engine-core canonical `normalize()`; no per-engine copies remain.
- **§7.5 #4 — `COMPONENT_DISPLAY_NAMES` in a scoring file.** Resolved by **OTA-813**. **"Presentation layer" here means server-side export formatting** — the map now lives in `app/api/display_names.py` (consumed by `export_routes.py`'s markdown breakdown). It is NOT a frontend module, and that is correct. Confirm it's out of every scoring/engine/adapter file and in the presentation module.
- **§7.5 #5 — DirectionalEngine disposition.** Resolved by **OTA-752 + OTA-756** (deployed). Confirm `directional_engine.py` is retired with no remaining callers.
- **§7.5 #6 — ATR(14) TODO** (was `strategy_scorer.py:129`). Resolved by the deployed ATR_14 producer. Confirm the TODO is gone and `ATR_14` is a real named value produced by the adapter.

## Scope — verification only (no fixes)

Run each check (PowerShell). For each finding, report PASS (with the evidence) or FAIL (with what's missing):

1. **#1:** `Get-ChildItem app -Recurse -Include *.py | Select-String -Pattern "delta_min|delta_max|iv_rank_min|credit_pct_min|credit_pct_max|exit_profit_pct|exit_loss_multiplier"` — expect no hits on a `StrategyDefinition`/config dataclass. Inspect any hit to confirm it's not a resurrected dead field.
2. **#2:** confirm no inline `ev_raw >= min_ev_threshold` (or equivalent) EV filter in the chain/scanning path; the only EV gate is the registered formula. `Select-String` `vertical_engine` (or its successor) for an EV-threshold filter.
3. **#3:** `Get-ChildItem app -Recurse -Include *.py | Select-String -Pattern "def normalize|def _normalize"` — expect a single canonical engine-core implementation; no duplicates in adapter/scoring files. Confirm `strategy_scorer.py` no longer exists.
4. **#4:** `Get-ChildItem app\insight_engine,app\ota_adapters,app\options_rules,app\analysis -Recurse -Include *.py -ErrorAction SilentlyContinue | Select-String "COMPONENT_DISPLAY_NAMES"` → no hits; confirm it lives at `app/api/display_names.py`.
5. **#5:** confirm `directional_engine.py` is gone (or fully inert) and `Select-String` finds no callers/imports of it.
6. **#6:** confirm the `strategy_scorer.py:129` ATR(14) TODO is gone (file is deleted) and `ATR_14` is a produced named value (search the adapter / input catalog).

Report a consolidated PASS/FAIL table with the resolving ticket per row. For any FAIL, **STOP and recommend a new story** describing the gap — do not remediate here.

## Acceptance criteria
- Each §7.5 finding (#1–#6) is confirmed resolved by grep/inspection, with the resolving ticket noted.
- Any unresolved finding is surfaced as a new story, not silently closed.
- No open §7.5 code-smell remains unaccounted for.

## Out of scope
- No fixes, refactors, or edits of any kind — verification and reporting only.
- The broader audit (§5a/§5b/§5c, etc.) — this sweep is §7.5 only.

## Commit instruction
I have been instructed NOT to commit — this is a read-only verification sweep producing a report, not a code change. If every finding passes, report and stop (Don advances OTA-814). If any finding fails, STOP and report it as a candidate new story.

## Coordination footer
STOP until OTA-813 is deployed. (This is the closing gate for the §7.5 cleanups; #4's resolution depends on OTA-813 landing in the deployed tree.)

## Commit message template
```
(no commit — verification only; any gap is filed as a new story rather than fixed here)
```
