---
allowedTools:
  - Read
  - Edit
  - Bash
---

# OTA-601 — architecture-plan.md § Deployment Architecture: record Option B (ODBC install in main.py is intentional)

## Terminal context

- This terminal: solo (single-terminal work)
- Concurrent terminals: none
- Cross-terminal dependencies: none

## Required reading

Before any edits:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
```

Pay particular attention to two regions of `architecture-plan.md`:

1. The `## Deployment Architecture` section (currently the section ending just before `## Azure Resources Summary`). The new subsection lands at the end of this section.
2. The `## Change Log` table at the bottom of the file. A new top row lands there.

## Relevant Context — Do Not Deviate Without Escalation

Source: `CLAUDE.md` (Document Governance Rules)
Rule: Material changes to SoT docs are filed as Subtasks under the doc's governing Story. This Subtask (OTA-601) sits under OTA-574 (the governing Story for `architecture-plan.md`). Updating the doc's `Last Updated` header and adding a Change Log entry citing OTA-601 are required parts of the material-change discipline — they are not optional.

Source: `CLAUDE.md` (Document Governance Rules)
Rule: Self-initiated edits to SoT docs by Claude Code are forbidden. Edits are permitted only when the prompt explicitly authorizes them. This prompt explicitly authorizes the three edits below and nothing else. If you find yourself wanting to "tidy up" an unrelated section while you're in the file, stop. That's out of scope.

Source: OTA-553 (decision recorded 2026-05-06)
Decision: Option B — keep ODBC install in `app/main.py` lifespan; document as intentional; rely on Always On to mitigate idle-wake penalty. Option A (custom container image, ~$5/mo Azure Container Registry plus a Docker build step) is deferred. The user-supplied `startup.sh` approach was attempted under OTA-545 Phase 2 and reverted 2026-05-02; do not retry that specific approach.

Source: existing `architecture-plan.md` Change Log table format
Rule: Each row is `| YYYY-MM-DD [HH:MM] UTC | <ticket key> | <change description> |`. Most recent entries appear at the top of the data rows.

## Scope

Apply three surgical edits to `claude_context/architecture-plan.md`. No other files are modified. No code is changed. No tests are run.

### Edit 1 — Update `Last Updated` header

Find the literal line near the top of the file:

```
**Last Updated:** 2026-04-30 23:30 UTC
```

Replace with:

```
**Last Updated:** 2026-05-06 UTC
```

### Edit 2 — Add new subsection at end of § Deployment Architecture

Locate the `## Deployment Architecture` section. The final paragraph of that section is the one ending with: *"...because the API contract between the two is versioned and breaking changes go through the same gate."* (the SWA-deploy paragraph).

Immediately after that paragraph, and immediately before the next top-level heading `## Azure Resources Summary`, insert the following content (preceded and followed by one blank line so spacing matches the rest of the document):

```markdown
### Cold-start and runtime OS dependencies

The ODBC Driver 18 install runs inside `app/main.py` lifespan startup. This is the intentional pattern, not a workaround. It costs roughly 90 seconds on cold start. Always On is enabled on the App Service plan, so cold start happens only on app restart — not on idle wake — bounding the user-perceived latency penalty to deploys and rare unplanned restarts.

The alternative (a custom container image with ODBC pre-installed, deployed via Azure Container Registry) is documented as Option A in OTA-553. It is not pursued today because the recurring cost (~$5/mo registry, plus a Docker build step in CI) is not justified by the ~90s cold-start savings at current scale. Revisit when a broader containerization effort is undertaken or when cold-start becomes user-visible (e.g., autoscale-out scenarios where new instances spin up frequently).

A user-supplied `startup.sh` approach was attempted under OTA-545 Phase 2 and reverted on 2026-05-02. The App Service Linux Python (Oryx) runtime makes that pattern brittle. **Do not retry that specific approach.** The general goal of moving ODBC out of `main.py` is still achievable via Option A if/when justified.

References: OTA-553 (decision), OTA-545 (Phase 2 revert), OTA-601 (this update).
```

### Edit 3 — Add Change Log entry

Locate the `## Change Log` table at the bottom of the file. The table header is:

```
| Date | Ticket | Change |
|---|---|---|
```

Insert the following row as the **first data row** of the table — immediately below the `|---|---|---|` separator line, above the existing `2026-04-30 23:30 UTC` entry. Do not modify any existing rows.

```
| 2026-05-06 UTC | OTA-601 | Added § Deployment Architecture subsection "Cold-start and runtime OS dependencies" recording the OTA-553 Option B decision: ODBC Driver 18 install in `app/main.py` lifespan is the intentional pattern. Documents the ~90s cold-start cost mitigated by Always On, the deferred Option A alternative (custom container image), and the explicit prohibition against retrying the user-supplied `startup.sh` approach (OTA-545 Phase 2, reverted 2026-05-02). |
```

## Acceptance criteria

- [ ] Header line `**Last Updated:**` reads `2026-05-06 UTC`.
- [ ] `## Deployment Architecture` contains a new `### Cold-start and runtime OS dependencies` subsection at its end, with the exact text from Edit 2 above.
- [ ] The new subsection sits immediately before `## Azure Resources Summary`.
- [ ] `## Change Log` contains the new row from Edit 3 as its top data row, with the exact text above.
- [ ] All existing Change Log rows are preserved in their original order below the new row.
- [ ] No other content in the file is modified.

## Out of scope

- Any code changes (this is a docs-only change).
- Any other section of `architecture-plan.md` beyond the three edit anchors.
- Any other file in the repo.
- Running tests, linters, or build verification.
- "Tidying up" unrelated sections, fixing unrelated typos, or any opportunistic cleanup. If you notice something else worth changing, mention it in your final report — do not edit it.

## Verification steps

After applying all three edits, run:

```
git diff claude_context/architecture-plan.md
```

Confirm the diff contains exactly:

1. The `Last Updated` header date change.
2. The new `### Cold-start and runtime OS dependencies` subsection inserted before `## Azure Resources Summary`.
3. The new Change Log row inserted below `|---|---|---|`.

And contains nothing else. If the diff shows any unexpected change — whitespace anomalies, accidental edits to adjacent paragraphs, line-ending changes — stop and report rather than committing.

## Commit instruction

I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer

Independent — no downstream dependency.

## Commit message template (if committing)

```
OTA-601: architecture-plan § Deployment Architecture — record Option B (ODBC install in main.py is intentional)
```
