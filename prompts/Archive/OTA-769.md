---
description: OTA-769 — Remove hardcoded TR delta center; TR/LT delta params from junction
allowed-tools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash
---

# OTA-769 — Remove hardcoded TR delta center

## Terminal context
- This terminal: **Terminal A**.
- Concurrent terminals: **none**.
- Cross-terminal dependencies: none. **Prompt 4 of 6.** Depends on prompts 1–3. Has a **config-side STOP** (see Phase 0): the delta values land in `engine_strategy_rule_junction.parameters` JSON, and the seed-application mechanism must be confirmed before values are written.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
cat claude_context/insight_engine-schema-ddl.md
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: insight_engine.md §2, Principle 2 (Strategy independence)**
"No `if strategy_id == X` branches anywhere in engine code." No implicit "everything that isn't TR" default — LT reads its own values from its own row.

**Source: insight_engine.md §3.4 (Junction-only invariant) + §6.3 (`formula:<name>`)**
A rule's per-strategy parameter values live only in the junction row. Registered formulas are pure `(named_values, params) -> float in [0, 100]`; they read thresholds/centers from `params`, never from literals in formula code.

**Source: insight_engine-schema-ddl.md §"Junction parameter storage"**
Variable per-rule parameters are a single `parameters NVARCHAR(MAX)` JSON column on `engine_strategy_rule_junction`, validated at engine load against `engine_rules.parameter_schema`. `delta_center` / `delta_half_range` belong here, per (strategy, Delta-Quality-rule) row.

**Source: OTA-725 (Screening rule library, Code & Test Complete)**
The Delta Quality scoring criterion is already a registered formula. This story makes it read `delta_center` / `delta_half_range` from junction `params`, and removes the strategy-identity branch that currently supplies them.

## Phase 0 — Read-only discovery (HARD STOP before any edits)

Do not edit in Phase 0.

1. Find the delta branch and the Delta Quality formula:
   ```powershell
   rg -n "trend-rider|delta_center|delta_half_range|delta_quality|Delta Quality" app
   ```
2. `cat` the delta scoring path. Confirm the `if strategy_key == "trend-rider"` branch and what `delta_center` / `delta_half_range` it sets for TR vs the implicit non-TR default for LT.
3. Inspect the registered Delta Quality formula (OTA-725): does it already accept `delta_center` / `delta_half_range` via `params`, or is it still reading a literal / branch?
4. Confirm whether `delta_center` / `delta_half_range` are declared in the Delta Quality rule's `parameter_schema` (`engine_rules`). If not, that schema change is part of this story.
5. **Seed mechanism:** determine how junction `parameters` get values into the live `engine_strategy_rule_junction` rows — a code/JSON seed the importer reads, a SQL migration, the strategy-admin UI, or `Scoring Parameters.xlsx`. This decides whether Claude Code can apply the config or whether Don must.

**STOP report (paste back to Claude.ai):**
- The `trend-rider` branch + file:line; the TR values and the LT implicit default.
- Delta Quality formula signature + whether it already reads `params`.
- Whether `delta_center` / `delta_half_range` are in the rule `parameter_schema`.
- The seed-application mechanism, and whether config values are Claude-editable or Don-applied.
- Proposed junction param values for TR and LT (carry the existing values forward — independence only, no recalibration).
- Go / no-go.

Wait for Claude.ai's go decision.

## Scope (after go)
- **Code side:** remove the `if strategy_key == "trend-rider"` branch from the delta scoring path. Ensure the Delta Quality formula reads `delta_center` / `delta_half_range` from `params`. Add them to the rule `parameter_schema` if missing.
- **Config side:** populate TR's and LT's own junction rows with their `delta_center` / `delta_half_range` (existing values, carried forward). If the seed mechanism is Don-applied (xlsx/UI/SQL), STOP and hand Don the exact rows/values; do not fabricate a binary-seed edit.

## Acceptance criteria
- No `strategy_key == "trend-rider"` branch in the delta scoring path (`rg -n "trend-rider" app` → empty in scoring).
- TR and LT each source `delta_center` / `delta_half_range` from their own junction rows — no implicit non-TR default.
- The Delta Quality rule reads these values from config (`params`), not from literals.
- Existing TR/LT delta behavior preserved (values carried forward).

## Out of scope
- No recalibration of delta values — independence only.
- No change to other scoring criteria, normalization, cushion, or verdict bands.

## Verification steps
- `rg -n "trend-rider|delta_center\s*=|delta_half_range\s*=" app` → no literals/branches in the scoring path.
- Engine startup validation passes (junction params validate against `parameter_schema`; no missing-required-field failure for the Delta Quality rule on TR or LT).
- Delta Quality score for representative TR and LT candidates unchanged vs pre-change.
- `pytest` green.

## Commit instruction
I have been instructed to commit. **Don runs the commit manually** — do not run `git commit`. If any config value must be applied by Don (xlsx/UI/SQL), surface that in the approval ask. Present the changed-file list + the exact message below, then ask: **Do you approve? (yes / no)**

## Coordination footer
**OK to continue to OTA-770.md**

## Commit message template
```
OTA-769 feat: remove hardcoded TR delta center; TR and LT read delta_center/delta_half_range from their own junction rows
```

## Recommended QA level
**Standard** — code change plus a config-seed; gate on engine startup validation and per-strategy delta-score parity.
