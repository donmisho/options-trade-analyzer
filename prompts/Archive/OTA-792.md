# OTA-792 — Config change audit trail

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none
- Cross-terminal dependencies: CRUD write path (OTA-782) + Apply / `config_version` bump (OTA-790) committed

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/auth-process.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-schema-ddl.md
```

(Actor identity comes from the BFF session → `auth-process.md` required.)

## Phase 0 — Read-only investigation (hard go/no-go STOP)

Do NOT edit in Phase 0. Investigate and report:

1. The OTA-782 write path — the single choke point every config change passes through (where the audit hook belongs).
2. The BFF session identity accessor (how to get the acting user for the actor field) — per `auth-process.md`.
3. Whether an audit/history table exists or one must be added; if added, follow `engine_*` / schema conventions (`dbo`, JSON as `NVARCHAR(MAX)`, soft denormalized keys consistent with the bronze pattern).
4. How `config_version` (bumped by OTA-790's Apply) is exposed so each audit entry can link to it.
5. How draft writes (preview-only) are distinguishable from Apply (live promotion) at the write path.

Report findings + go/no-go.

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine.md` §6.2 (tables are the source of truth → changes must be attributable)
- Because the tables are the runtime source of truth, every change to them must be attributable — the audit trail is the config-side analogue of the engine's per-rule trace.

Source: ticket AC
- Each committed config change records: actor (from BFF session identity), timestamp, target row(s), and a before/after of the changed fields.
- The audit entry links to the `config_version` produced by the change (OTA-790).
- The trail is queryable per strategy (ideally also per rule / junction row).
- **Preview-only draft writes are distinguishable from Apply (live promotion)** in the trail.

Source: `auth-process.md`
- Actor identity is the BFF session identity — never anonymous, never client-supplied.

Source: `insight_engine-schema-ddl.md` §1 (conventions, if adding a table)
- `dbo` placement; JSON as `NVARCHAR(MAX)` with `ISJSON` checks; soft denormalized string keys (don't hard-FK into the `engine_*` surrogates if following the bronze portability pattern).

## Scope
Record a config-change audit trail at the OTA-782 write choke point: actor, timestamp, target row(s), before/after of changed fields, linked to the `config_version`. Distinguish draft (preview) writes from Apply (live) promotions. Provide a per-strategy (ideally per-row) query path.

## Acceptance criteria
- Each committed config change records actor (BFF session), timestamp, target row(s), and a before/after of the changed fields.
- The audit entry is linked to the `config_version` produced by the change.
- The trail is queryable per strategy (and ideally per rule/junction row).
- Draft (preview-only) writes are distinguishable from Apply (live promotion) in the trail.

## Out of scope
- A full audit UI surface (the AC asks for a queryable trail, not necessarily a dedicated page — confirm scope with Don if a UI is implied).
- Changing CRUD/Apply behavior beyond adding the audit hook.

## Verification steps
- Make a draft edit and an Apply; confirm two distinguishable audit rows with correct actor/timestamp/before-after and `config_version` link.
- Query the trail per strategy.
- Confirm actor is the session identity, never anonymous.
- **QA level: medium-high.** Attribution correctness + the draft-vs-Apply distinction are the load-bearing parts.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-793.md

## Commit message template (if committing)
OTA-792 feat: config-change audit trail (actor, before/after, config_version link)
