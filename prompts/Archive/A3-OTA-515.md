---
allowedTools: Read, Edit, Write, Bash, Grep, Glob
---

# OTA-515 — Add WAIT_FOR_EARNINGS verdict with dte_after_earnings routing

## Terminal context
- This terminal: **Terminal A**
- Concurrent terminals: Terminal B (architecture main.py chain), Terminal C (AI stack), Terminal D (provider/strategy/docs)
- Cross-terminal dependencies: none for this prompt

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
cat claude_context/UI-GUIDANCE.md
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: business-rules.md → Hard Gates Pipeline (canonical)**
The verdict pipeline is the canonical decision surface. Adding a verdict adds a state to the enum AND a routing branch in the gate logic. The NEG EV gate must still fire BEFORE WAIT_FOR_EARNINGS routing — a negative-EV trade does not become viable just because earnings is the binding constraint.

**Source: ticket OTA-515 → Decision tree** (the canonical spec for this Story)

```
if earnings_in_window:
    dte_before_earnings = trading_days(entry, earnings - 1)
    dte_after_earnings  = trading_days(earnings + 1, expiry)

    # Route 1 — no viable window on either side
    if dte_before_earnings <= 7 AND dte_after_earnings < 14:
        verdict = PASS
        reason  = "Insufficient time both before and after catalyst"

    # Route 2 — can't enter pre-earnings, but strong post-earnings window remains
    elif dte_before_earnings <= 7 AND dte_after_earnings >= 14:
        verdict = WAIT_FOR_EARNINGS
        reason  = f"Re-evaluate {earnings + 1}. {dte_after_earnings} DTE remaining post-IV crush."

    # Route 3 — pre-earnings window viable, but post-earnings entry likely better
    elif dte_after_earnings >= 21:
        verdict = WAIT_FOR_EARNINGS  # recommended; annotate reason
        reason  = "Post-earnings entry likely better. Waiting preserves most of window."

    # Route 4 — pre-earnings window is the only viable one
    else:  # dte_before_earnings >= 8, dte_after_earnings < 21
        score normally using effective_DTE = dte_before_earnings
        classify strictly as pre-earnings momentum play
```

**Source: ticket OTA-515 → Debit vs credit asymmetry**
- Debit spreads: WAIT_FOR_EARNINGS narrative reads "Wait is strictly better — entry improves post-crush."
- Credit spreads: WAIT_FOR_EARNINGS narrative reads "Wait trades premium for safety — credit will be smaller but gap risk eliminated."
The two messages are differentiated by `trade_structure` (debit vs credit). The narrative source is the SKILL.md (post-OTA-537 migration) — for this prompt, follow whatever the current source-of-truth is for `TRADE_EVALUATION_SYSTEM_PROMPT` content; if it has not yet migrated, edit `app/ai/prompts.py`. **Coordinate with Terminal C if both prompts touch the prompt source simultaneously.**

**Source: UI-GUIDANCE.md → Trade type display + dark theme tokens (canonical)**
- Verdict badges follow trade-type badge conventions: title case, no underscores. Display name: **Wait For Earnings** (NOT `WAIT_FOR_EARNINGS`).
- Color: amber. Use a dark-theme CSS variable for amber that's distinct from the existing WAIT badge. If no amber token exists, ADD one to `web/src/styles/tokens.js` (following the existing pattern; do NOT inline a hex). Suggest naming `--verdict-wait-earnings` to be explicit.
- Never inline hex colors anywhere.

**Source: CLAUDE.md → House Style Rules**
- Date display: `mm-dd-yyyy` via `formatDate()`. The `reevaluate_on` date in the payload renders through `formatDate()` in the UI.
- Scores on 0–100 scale: `##.00`. The verdict card score format is unchanged by this prompt.

## Phase 1 — Read-only diagnosis (STOP gate before Phase 2)

1. `cat business-rules.md` — read the Hard Gates and Verdict sections in full. Note the existing verdict enum location (likely `app/models/verdict.py` or `app/analysis/verdict.py`; grep to find).
2. `grep -rn "EXECUTE\|PASS\|^WAIT" app/ --include="*.py" | grep -i verdict` — find every consumer of the verdict enum. List them.
3. `grep -rn "earnings_in_window\|dte_after_earnings\|effective_DTE" app/` — confirm OTA-502's `earnings_in_window` and `effective_DTE` fields are present and what they're shaped like.
4. `cat app/api/evaluation_routes.py` — find where verdict assignment happens. Note the gate ordering (NEG EV must come first).
5. `cat web/src/components/<verdict badge component>.jsx` (likely under TradeVerdict / VerdictBadge / EvaluationCard — grep for "EXECUTE" in `web/src/`).
6. `cat web/src/styles/tokens.js` — list all amber/orange-adjacent tokens already defined. Determine if a new token is needed or if an existing one suffices.

**STOP. Report back to Don with:**
- Location of the verdict enum and every call site.
- Current shape of `computed_fields` returned to the frontend (full field list).
- The exact gate-ordering as currently implemented in `evaluation_routes.py`.
- Proposed token name and color value (hex) for the amber variant. Don approves the color before any UI change.
- Whether the prompt source for evaluation narrative is still in `app/ai/prompts.py` or has migrated to `SKILL.md` (Terminal C's C2 work).

Wait for Don's go-ahead before Phase 2.

## Phase 2 — Implement

### Backend

1. Add `WAIT_FOR_EARNINGS` to the verdict enum.
2. Add `dte_after_earnings` (int, nullable) and `reevaluate_on` (date string `mm-dd-yyyy`, nullable) as fields in the verdict response model / `computed_fields` payload.
3. Implement the four-route decision tree in the gate pipeline. Insert AFTER the NEG EV gate. The branching key is `earnings_in_window` (truthy → enter the tree).
4. The `trading_days(start, end)` helper must skip weekends and US market holidays. If a helper already exists, use it; otherwise implement using the existing market-calendar provider (likely under `app/analysis/calendar.py` or similar — `grep -n "trading_days\|market_calendar" app/`). Do not invent a new calendar source.
5. For `WAIT_FOR_EARNINGS` verdicts, populate `reevaluate_on` = the next trading day after earnings.
6. Update the narrative prompt source so the prompt receives `verdict`, `dte_after_earnings`, and `trade_structure` (debit/credit) and renders one of the two differentiated messages from the embedded context above. If the prompt source is still `app/ai/prompts.py` (Terminal C hasn't migrated yet), edit there. If migrated, edit the relevant SKILL.md section. **Either way, leave a comment with `OTA-515` referencing this Story for traceability.**

### Frontend

1. Add the new verdict to the verdict-badge mapping. Display name: **"Wait For Earnings"** (title case, no underscores).
2. Use the new amber CSS token (added to `tokens.js` if absent) for the badge background and border. Match the existing badge sizing and border-radius. Buttons sized to content per CLAUDE.md house style.
3. When verdict is `WAIT_FOR_EARNINGS`, render the `reevaluate_on` date below the verdict reason. Format via `formatDate()` from `web/src/utils/formatDate.js`. Label: `"Re-evaluate on:"`.
4. Update any verdict-filter UI (filter dropdowns, position-page filters, etc.) to include the new verdict. Grep for verdict filter components.

### Re-evaluation hook (deferred to a follow-on Story unless trivial)

The ticket scope lists "Re-evaluation hook — if user opted in, auto-re-run scan on `earnings + 1`." If the existing scheduler / cron infrastructure makes this a one-line addition, do it. Otherwise, file a follow-on Subtask under this Story and explicitly note the deferral in the commit message.

## Test cases (must all pass)

| Entry | Expiry | Earnings | dte_before | dte_after | Expected verdict |
|---|---|---|---|---|---|
| Apr 22 | Apr 25 | Apr 29 | n/a (post-expiry) | n/a | No gate — proceed normally |
| Apr 22 | May 1 | Apr 29 | 4 | 2 | PASS (Route 1) |
| Apr 22 | May 15 | Apr 29 | 4 | 11 | PASS (Route 1) — AMZN case |
| Apr 22 | May 22 | Apr 29 | 4 | 16 | WAIT_FOR_EARNINGS (Route 2) |
| Apr 22 | Jun 20 | Apr 29 | 4 | 36 | WAIT_FOR_EARNINGS (Route 2) |
| Apr 22 | Jul 15 | Apr 29 | 4 | 53 | WAIT_FOR_EARNINGS (Route 2) |
| Apr 22 | Jun 30 | Apr 29 | 4 | 42 | WAIT_FOR_EARNINGS (Route 2) |
| Apr 08 | May 15 | Apr 29 | 14 | 11 | Route 4 — score with effective_DTE=14, classify as pre-earnings momentum |

Add these as a parameterized pytest under `tests/analysis/test_verdict_routing.py` (create file if absent).

## Acceptance criteria

- `WAIT_FOR_EARNINGS` present in verdict enum, all backend call sites, frontend badge mapping, verdict filters.
- All eight test cases pass.
- Debit vs credit narrative differentiation verified by manually evaluating one debit and one credit pair where Route 2 fires; confirm the rendered narrative matches the embedded context messages.
- `reevaluate_on` field present in payload for WAIT_FOR_EARNINGS verdicts and rendered in UI via `formatDate()`.
- New amber CSS token added to `tokens.js` (if needed) with name approved by Don in Phase 1 report.
- AMZN regression suite passes (Route 1 case is in there).
- NEG EV gate still fires before earnings routing — verified by a test case with negative EV + earnings_in_window = True returning PASS (NEG EV) not WAIT_FOR_EARNINGS.

## Out of scope

- Auto re-evaluation cron/scheduler (deferred unless trivial — see body).
- Refactoring the verdict module (out of scope; surgical edit only).
- Adding more verdicts beyond WAIT_FOR_EARNINGS.

## Verification steps

1. `python -c "import app.main"` succeeds.
2. `pytest tests/analysis/test_verdict_routing.py -v` — all 8 cases green.
3. Local backend + frontend per `development-environment.md`. Evaluate AMZN with the May 22 expiry (Route 2). Confirm the badge renders amber, displays "Wait For Earnings", and shows the re-evaluate date below.
4. Evaluate AMZN with the May 15 expiry (Route 1 PASS). Confirm normal PASS rendering, no amber badge.
5. Evaluate one credit-spread WAIT_FOR_EARNINGS case and confirm the narrative reads the credit message, not the debit message.
6. Inspect `tokens.js` diff. Confirm no inline hex anywhere in JSX/CSS touched by this prompt.
7. `git diff` review with Don. Confirm SoT docs untouched.

QA Level: **Level 3** — verdict pipeline change. Run AMZN regression + manual narrative consistency check on three symbols (one Route 1, one Route 2, one Route 4).

## Commit instruction

I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer

OK to continue to **A4-OTA-557.md**

## Commit message template

```
OTA-515 feat: WAIT_FOR_EARNINGS verdict with dte_after_earnings routing; debit/credit narrative differentiation
```
