---
allowedTools: Read, Edit, Write, Bash, Grep, Glob
---

# OTA-584 + OTA-585 + OTA-586 + OTA-587 + OTA-588 + OTA-595 — Apply Governance Doc Updates (Multi-OTA)

## Multi-OTA bundle scope
This prompt applies six related SoT doc updates in a single commit transaction. All six tickets share the same domain (`claude_context/*.md`), share no inter-ticket dependencies, and either all roll forward or all roll back.

| Ticket | Action |
|---|---|
| OTA-584 | Replace `claude_context/CLAUDE.md` with restructured draft (slim, no Feature references, sections extracted to pointer docs) |
| OTA-585 | Refresh `claude_context/bugfix.md` to align with new governance structure |
| OTA-586 | Create `claude_context/product-roadmap.md` (OTAR Categories + linking procedure) |
| OTA-587 | Create `claude_context/development-environment.md` (FastAPI/Vite setup, venv, zombie-process handling) |
| OTA-588 | Create `claude_context/deployment-workflow.md` (slot-swap deploy + Change Log impl scaffolding) |
| OTA-595 | Refresh root `CLAUDE.md` pointer to current 12-doc SoT inventory |

## Terminal context
- This terminal: **Terminal D**
- Concurrent terminals: any of A/B/C may be running
- Cross-terminal dependencies: **None.** This prompt only touches `claude_context/*.md` and root `CLAUDE.md`. No code files. Safe to run at any time, in parallel with everything else.
- Recommendation: **Run this prompt first on Terminal D** so subsequent code-work prompts (on any terminal) are reading the new SoT structure when they `cat claude_context/CLAUDE.md`.

## Pre-flight requirement (Don's setup)

Before running this prompt, Don must have placed the six draft files in a scratch directory at the repo root:

```
./_doc-drafts/
    CLAUDE.md                    (for OTA-584; restructured version)
    bugfix.md                    (for OTA-585; refreshed)
    product-roadmap.md           (for OTA-586; new)
    development-environment.md   (for OTA-587; new)
    deployment-workflow.md       (for OTA-588; new)
    CLAUDE-root.md               (for OTA-595; root pointer; rename on copy)
```

The drafts originate from the Claude Web session that produced this prompt. Don downloads them from project knowledge or this session's outputs and drops them into `./_doc-drafts/`.

If `./_doc-drafts/` does not exist or is missing files when this prompt starts, **STOP and report to Don**. Do not proceed with partial inputs.

`./_doc-drafts/` is added to `.gitignore` as part of this prompt so the scratch directory never gets committed.

## Required reading
Before any file changes:

```
cat CLAUDE.md
cat claude_context/CLAUDE.md
ls claude_context/
```

This shows current root pointer state, current `claude_context/CLAUDE.md` state, and the existing inventory of governance docs.

## Phase 1 — Inventory check (read-only, STOP gate)

1. Confirm `./_doc-drafts/` exists and contains all six files listed above. Report sizes.
2. For each destination file, report whether it currently exists in the repo and its current size:
    - `claude_context/CLAUDE.md` (should exist)
    - `claude_context/bugfix.md` (should exist; may be a stub)
    - `claude_context/product-roadmap.md` (likely does not exist — creating new)
    - `claude_context/development-environment.md` (likely does not exist — creating new)
    - `claude_context/deployment-workflow.md` (likely does not exist — creating new)
    - `CLAUDE.md` at repo root (should exist; this is the pointer file)
3. List the current 12-doc SoT inventory by inspecting `claude_context/`. Don confirms this matches what OTA-595's root pointer should reference.

**STOP. Report all findings to Don. Wait for confirmation before Phase 2.**

## Phase 2 — Apply doc updates

Each step: read the draft, write to destination, confirm with `git diff` after each substep. Do not commit between substeps — the entire bundle commits together.

### 2.1 — OTA-584: Replace claude_context/CLAUDE.md

```
cp ./_doc-drafts/CLAUDE.md claude_context/CLAUDE.md
git diff claude_context/CLAUDE.md
```

Inspect: confirm the new content has no "Feature" references (per ticket requirement). Confirm sections that were extracted to pointer docs (development-environment, deployment-workflow, product-roadmap) are no longer duplicated here.

### 2.2 — OTA-585: Refresh claude_context/bugfix.md

```
cp ./_doc-drafts/bugfix.md claude_context/bugfix.md
git diff claude_context/bugfix.md
```

Inspect: confirm aligned with new governance structure (references the new pointer docs where applicable).

### 2.3 — OTA-586: Create claude_context/product-roadmap.md

```
cp ./_doc-drafts/product-roadmap.md claude_context/product-roadmap.md
git status claude_context/product-roadmap.md
```

Inspect: confirm OTAR Categories are listed and the OTAR ↔ OTA linking procedure (Polaris work item link, type ID 10038/10039, direction inwardIssue/outwardIssue) is documented per the original CLAUDE.md content.

### 2.4 — OTA-587: Create claude_context/development-environment.md

```
cp ./_doc-drafts/development-environment.md claude_context/development-environment.md
git status claude_context/development-environment.md
```

Inspect: confirm FastAPI/Vite setup, venv conventions (`venv` not `.venv`), and zombie-process handling are all covered.

### 2.5 — OTA-588: Create claude_context/deployment-workflow.md

```
cp ./_doc-drafts/deployment-workflow.md claude_context/deployment-workflow.md
git status claude_context/deployment-workflow.md
```

Inspect: confirm slot-swap deploy procedure and Change Log implementation scaffolding are present.

### 2.6 — OTA-595: Refresh root CLAUDE.md pointer

```
cp ./_doc-drafts/CLAUDE-root.md CLAUDE.md
git diff CLAUDE.md
```

Inspect: the root `CLAUDE.md` pointer file should now reference the current 12-doc SoT inventory, including the three new docs from this commit (product-roadmap.md, development-environment.md, deployment-workflow.md).

### 2.7 — .gitignore update

Add `_doc-drafts/` to `.gitignore` if not already present.

```
grep -q "^_doc-drafts/" .gitignore || echo "_doc-drafts/" >> .gitignore
git diff .gitignore
```

### 2.8 — Final status check

```
git status
```

Expected staged/modified set:
- `CLAUDE.md` (modified)
- `claude_context/CLAUDE.md` (modified)
- `claude_context/bugfix.md` (modified)
- `claude_context/product-roadmap.md` (new)
- `claude_context/development-environment.md` (new)
- `claude_context/deployment-workflow.md` (new)
- `.gitignore` (modified, if needed)

The `_doc-drafts/` directory should be untracked (visible in `git status` output but excluded from staging).

## Acceptance criteria

- All six destination files contain the draft content from `./_doc-drafts/`.
- Root `CLAUDE.md` pointer references the current 12-doc SoT inventory including the three new files.
- `.gitignore` excludes `_doc-drafts/`.
- `git status` shows exactly the seven expected changes (six docs + .gitignore) and no others.
- No code files modified.
- Single commit covers all six tickets.

## Out of scope

- Editing the draft contents themselves (drafts are authoritative as delivered by Don; if there's a typo or misalignment, escalate — don't silently fix).
- Updating any other governance docs not in this bundle.
- Profile-level docs (those are Track 1, Don's manual work, separate from this prompt).
- Any code changes.

## Verification steps

1. `cat claude_context/CLAUDE.md | head -50` — confirm restructured shape.
2. `cat claude_context/product-roadmap.md | head -30` — confirm exists and has OTAR content.
3. `cat claude_context/development-environment.md | head -30` — confirm exists.
4. `cat claude_context/deployment-workflow.md | head -30` — confirm exists.
5. `cat CLAUDE.md | grep -i "product-roadmap\|development-environment\|deployment-workflow"` — confirm pointer references the three new files.
6. `git status` matches expected set per Acceptance.
7. `git diff --stat` review with Don.

QA Level: **Level 1** — docs-only change. No runtime impact.

## Commit instruction

I have been instructed to commit. Do you approve? (yes / no)

This commit closes six tickets. The commit message includes all six OTA keys to trigger Jira automation on each.

## Coordination footer

Independent — no downstream dependency

## Commit message template

```
OTA-584 OTA-585 OTA-586 OTA-587 OTA-588 OTA-595 docs: restructure CLAUDE.md; refresh bugfix.md; add product-roadmap.md, development-environment.md, deployment-workflow.md; refresh root CLAUDE.md pointer
```
