# OTA-688 — Backfill missing rules + resolve code-only rules

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none assumed — confirm with Don.
- Cross-terminal dependencies: requires OTA-681 (tables) + OTA-682 (importer); shares the rule set with
  OTA-683/685/686. Produces `engine_rules` + junction rows.

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md                 # § 3.6 (soft gate = stop_if_fail=false + penalty)
cat claude_context/insight_engine-schema-ddl.md
# Capture source (temporary): requirements/Scoring Parameters.xlsx (Sheet1)
# Missing items + code-only rules: requirements/insights-engine-audit-2026-05-20.md  § 2 (MISSING) and § 3 (code-only)
```

## Relevant Context — Do Not Deviate Without Escalation

Source: Don, project direction (operating principle)
The sheet is a temporary capture vehicle; the `engine_*` tables become the engine's source of truth.
Capture every rule that should exist so the engine reads and processes it; defer value-correctness to
tuning. The win condition is structural completeness, not tuned values.

Source: audit § 2 (MISSING / sheet-not-in-code) — seed these sheet rules as `engine_rules` + junction rows:
- Underlying price floor (`>= 20`) · Data-completeness gates (IV-rank / ATR_14 IS NOT NULL — flag the
  no-producer-yet dependency) · Cushion vs ATR (SP `>= 1`, WG `>= 1.5`; depends on ATR_14 producer) ·
  Theta load fraction (LT, `<= 0.5`) · Chart state confirms direction (TR/LT — coordinate with the OTA-683
  decomposition so it isn't double-seeded) · Spread-width tier compliance (references the OTA-690
  `width_configuration` lookup) · Soft gates: Stock Extended Against Entry (LT), Stock Extended in Trade
  Direction (SP/WG) — model as gate-phase rules with `stop_if_fail = false` + `score_penalty = 10`.

Source: audit § 3 — the eight code-only rules. Disposition:
- **Capture as rules** (audit recommends keep): Cushion graduated penalty (-20 < 1% / -10 < 2%, post-scoring
  adjustment) · DTE 8–13 penalty (-20) · Probability-asymmetry penalty (0/8/15/25, OTA-505) · Earnings-gate
  Route-4 penalty (-15). Model each as the appropriate phase rule (adjustment) with junction params.
- **Not a rule:** Narrative-grounding validator — a post-Claude quality gate that stays in the app layer;
  do NOT seed it as an engine rule.
- **Architectural dispositions (record in `intent`/`rationale`; default below; flag as an assumption Don can
  flip):**
  - **WAIT_FOR_EARNINGS** is NOT a fourth score band. It is the earnings-gate halt outcome — a candidate
    stopped by the earnings gate (`stop_if_fail = true`) exits at that `terminal_phase` and the app may label
    it WAIT_FOR_EARNINGS. The sheet's 3 verdict bands (70/50) stand. Record this; do not add a 4th band.
  - **VerticalSpreadEngine internal weights** (EV35/RR25/Prob20/Liq15/Theta5) and **NakedOptionEngine
    internal weights** (Delta30/Theta25/IV20/RR15/Liq10) are a legacy second scoring system **superseded by
    the strategy junction weights** (the engine has one weight set per strategy). Do NOT seed them as rules.
    Record the supersession; actual code removal is tracked in the later screening-rules feature.

Source: `insight_engine.md` § 3.6
There is no "soft gate" category — former soft gates seed as gate-phase rules with `stop_if_fail = false`
plus a `score_penalty`. (Mechanics values are set in OTA-684; this story creates the rules + the penalty
intent.)

## Scope
Seed the confirmed MISSING sheet rules; capture the four keep-recommended code-only rules as engine rules;
record the architectural dispositions (WAIT_FOR_EARNINGS, engine-internal weights) and the narrative-
validator exclusion.

## Acceptance criteria
- All confirmed MISSING sheet rules are seeded as `engine_rules` (+ junction rows), with no-producer-yet
  dependencies (ATR_14, true IV rank) flagged.
- The four keep-recommended code-only rules are captured as engine rules (cushion graduated penalty, DTE
  8–13 penalty, probability-asymmetry penalty, earnings Route-4 penalty).
- The narrative-grounding validator is explicitly NOT seeded (recorded as an app-layer quality gate).
- WAIT_FOR_EARNINGS is recorded as the earnings-gate halt outcome (3 score bands stand) — not a 4th band.
- The two engine-internal weight systems are recorded as superseded by junction weights — not seeded.
- Former soft gates are modeled as `stop_if_fail = false` + `score_penalty` rules (no soft-gate category).
- No item from § 2 MISSING or § 3 is left without either a seeded rule or a recorded disposition.

## Out of scope
- Setting gate mechanics values (`evaluation_order`/`stop_if_fail`/`score_penalty` numbers) — OTA-684.
- Removing the legacy engine-internal weight code — later screening-rules feature.
- Building missing input producers (ATR_14, true IV rank) — adapter, later.
- Value tuning of any seeded threshold.

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
```
- Confirm each § 2 MISSING item is a seeded `engine_rules` row (with dependency flags where noted).
- Confirm the four code-only rules exist as rules; the narrative validator is absent (recorded as app-layer).
- Confirm `intent`/`rationale` records the WAIT_FOR_EARNINGS and engine-internal-weights dispositions.

Post-Build QA Gate: Level 1 (rule seed + dispositions). Note in commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
STOP until OTA-683 completes  *(coordinate chart-state / earnings rules so they aren't double-seeded); then OK to continue to OTA-685.md and OTA-684.md for divergence notes + gate mechanics.*

## Commit message template (if committing)
```
OTA-688 feat: backfill missing rules + capture/dispose code-only rules

Seeds MISSING sheet rules (price floor, data-completeness, theta-load, soft gates
as stop_if_fail=false+penalty). Captures keep-recommended code-only rules (cushion/
DTE/asymmetry/Route-4 penalties). Records dispositions: narrative validator = app
quality gate; WAIT_FOR_EARNINGS = earnings halt (3 bands); engine-internal weights
superseded by junction weights. QA Level 1.
```
