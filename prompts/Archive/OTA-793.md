# OTA-793 — UI tests and UI-GUIDANCE compliance

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none
- Cross-terminal dependencies: the entire F13 admin surface (OTA-782–792) committed

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/UI-GUIDANCE.md
cat claude_context/strategy-rules-prototype-v2.html
```

## Phase 0 — Read-only investigation (hard go/no-go STOP)

Do NOT edit in Phase 0. Investigate and report:

1. The frontend test runner and how existing tests are wired into the run.
2. The full set of F13 surfaces to cover: selector + header (OTA-784), Hard Gates / Scoring / Adjustments tabs (OTA-785/786/787), Verdict Bands (OTA-788), catalog browser (OTA-789), Apply/Reset (OTA-790), draft preview render (OTA-791).
3. Any F13 surface that already shipped its own tests (avoid duplicating; fill gaps).
4. Existing UI-GUIDANCE-compliance test helpers, if any, to reuse.

Report findings + go/no-go.

## Relevant Context — Do Not Deviate Without Escalation

Source: ticket AC (coverage)
- UI tests cover the editor flows: load strategy, edit each tab, edit bands, add/remove from catalog, Apply, Reset, and draft-preview render.

Source: `UI-GUIDANCE.md` Part 3 / Part 3a / Part 4 (compliance to verify)
- Dark-theme CSS variables only — no inline hex; colors from `web/src/styles/tokens.js`.
- Scores formatted `##.00`; no `$` prefix on any monetary value; dates `mm-dd-yyyy` via `formatDate()`.
- `var(--bg2)` used **only** where permitted (QuoteBar, filter bars, inset/callout boxes) — **never** on table rows, table headers, expansion panels, group headers, section backgrounds, or full-width bands.

Source: `CLAUDE.md` (Post-Build QA gate)
- Wire the new tests into the frontend test run so they execute in CI, not just locally.

## Scope
Add UI tests across the F13 admin surface and verify UI-GUIDANCE compliance throughout the feature. Wire the tests into the frontend test run.

## Acceptance criteria
- UI tests cover: load strategy, edit each tab, edit bands, add/remove from catalog, Apply, Reset, and draft-preview render.
- UI-GUIDANCE compliance verified: dark-theme tokens only; `##.00` scores; no `$` prefix; `mm-dd-yyyy` via `formatDate()`.
- `var(--bg2)` is used only where permitted — never on rows/headers/expansion panels.
- Tests are wired into the frontend test run.

## Out of scope
- Changing F13 feature behavior. If a test exposes a defect, STOP and report it for a separate fix prompt rather than fixing inline (keeps the test commit clean).

## Verification steps
- Run the frontend test suite; all F13 tests pass and are part of the standard run.
- Confirm the `--bg2` restriction check fails loudly if a row/header/panel misuses it.
- **QA level: this story IS the QA.** Coverage is the deliverable; do not let it become a thin smoke test.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency

## Commit message template (if committing)
OTA-793 test: F13 strategy-admin UI tests + UI-GUIDANCE compliance checks
