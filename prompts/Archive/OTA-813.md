# OTA-813 — Move COMPONENT_DISPLAY_NAMES to the presentation layer

## Terminal context
- This terminal: Terminal A (single stream)
- Concurrent terminals: none
- Cross-terminal dependencies: none. OTA-779 (which deleted `strategy_scorer.py`) is already **Production Deployed** — Phase 0 must establish where the display-name map lives *now*, post-deletion, before any edit.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/UI-GUIDANCE.md
```

(Presentation/frontend-domain story → CLAUDE.md + UI-GUIDANCE.md per the per-domain table, plus architecture-plan.md for the engine/adapter/rule package boundaries this map must NOT live inside.)

## Relevant Context — Do Not Deviate Without Escalation

**Source: `rules-engine-audit-2026-05-20.md` §7.5 #4** — this audit file is NOT in `claude_context/`, so the finding is embedded here verbatim:
> `COMPONENT_DISPLAY_NAMES` (originally at `strategy_scorer.py:46-63`) folds multiple internal metric keys into the same display name — e.g. both `probability_of_profit` and `expected_value` map to "Expected value (EV)". This is a display/presentation concern that was living inside the scoring engine. It must move to the presentation layer.

**Source: OTA-779 (Decompose `strategy_scorer.py`) — already Production Deployed.**
`strategy_scorer.py` has been deleted. OTA-779 split its contents three ways — orchestration → app-side routes, per-rule math → registered formulas, normalization → engine core — and its scope did **NOT** name `COMPONENT_DISPLAY_NAMES`. The map's current location is therefore **UNKNOWN**: it was either (a) relocated during 779, (b) swept into one of the three split destinations, or (c) orphaned/dropped. Phase 0 must determine which before any change. If it was dropped and something still references it, surface that as a finding — do not silently recreate it.

**Source: `architecture-plan.md` engine-decoupling invariant (audit §5c).**
Engine machinery (`app/insight_engine/`), adapters (`app/ota_adapters/*`), and the rule library (`app/options_rules/*`) reference zero presentation concerns. After this story, no engine / adapter / scoring code may import the display-name map.

**Source: `CLAUDE.md` House Style.**
Display names are title case, no underscores. This story **relocates** the map only — it does not change any display string's value or any metric key.

## Scope

### Phase 0 — Discovery (READ-ONLY, then STOP)
Make no edits. Establish and report:

1. Where `COMPONENT_DISPLAY_NAMES` lives now, post-OTA-779. Search the whole tree:
   ```powershell
   Get-ChildItem -Recurse -Include *.py,*.js,*.jsx | Select-String -Pattern "COMPONENT_DISPLAY_NAMES"
   ```
2. Every consumer/importer of the map, and the layer each sits in (backend route/serializer vs frontend component).
3. Whether any engine / adapter / rule-library code currently imports it.
4. The current map contents — capture every key→label pair verbatim so they can be preserved unchanged.
5. Proposed destination, with reasoning: a frontend module (e.g. under `web/src/`) if consumption is purely UI, or a dedicated backend presentation module (e.g. an `app/api/` serialization/formatting helper) if a route attaches display labels server-side.

**STOP and report findings + proposed destination. Do not proceed to Phase 1 until I approve the destination.**

### Phase 1 — Move (only after approval)
- Relocate the map to the approved presentation-layer home.
- Repoint all consumers to import from the new location.
- Remove it from any non-presentation module it currently occupies.
- Relocation only — no change to any display string value or metric key.

## Acceptance criteria
- `COMPONENT_DISPLAY_NAMES` no longer lives in any scoring / engine / adapter / rule file; it resides in the presentation layer (frontend or a dedicated presentation module).
- No engine, adapter, or scoring code imports the display-name map.
- The key→label pairs are byte-for-byte identical to what Phase 0 captured.
- The app builds/imports cleanly and the affected surface renders identical display names.

## Out of scope
- No change to any display string's value or to any metric key.
- No change to scoring, gates, adapters, or rule-library logic.
- No broader engine-decoupling beyond removing this one presentation concern.
- OTA-814's §7.5 verification sweep is a separate later prompt — do not attempt the broader audit here.

## Verification steps
From repo root (PowerShell):
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
```
- No scoring/engine/adapter/rule import of the map:
  ```powershell
  Get-ChildItem app\insight_engine,app\ota_adapters,app\options_rules,app\analysis -Recurse -Include *.py -ErrorAction SilentlyContinue | Select-String "COMPONENT_DISPLAY_NAMES"
  ```
  → expect no hits.
- Map exists at its new presentation home and all consumers resolve it.
- If backend home: `python -c "import app.main"` → no ImportError.
- If frontend home: `npm run build` under `web/` → succeeds.
- Spot-check a metric that previously collapsed (e.g. EV) renders the same label as before.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency. (OTA-814's §7.5 verification sweep runs later, after this is deployed; it is a separate prompt, not chained here.)

## Commit message template (if committing)
```
OTA-813 refactor: move COMPONENT_DISPLAY_NAMES out of the scoring layer into the presentation layer
```
