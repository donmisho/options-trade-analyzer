# OTA-829 — Stand up frontend test runner (vitest) and seed strategy-editor component tests

## Terminal context
- This terminal: **Terminal B** (disjoint from the Terminal A editor chain).
- Concurrent terminals: Terminal A may be running OTA-827 / OTA-790 / OTA-828 (all touch `StrategyAdminPage.jsx` + `client.js`). Terminal B may also carry OTA-761 (backend).
- Cross-terminal dependencies: **Read-only** dependence on `web/src/components/JunctionRowEditor.jsx` (this story imports its helpers to test them but does NOT edit it). It modifies `web/package.json` — confirm Terminal A is not mid-editing `package.json` (it shouldn't be). **Run this against a committed tree** so tests exercise stable code, not an in-flight working copy.

## Required reading
Before any code changes:

    cat claude_context/CLAUDE.md
    cat claude_context/UI-GUIDANCE.md
    cat claude_context/prompt-style.md
    cat claude_context/build-execution.md

## Relevant Context — Do Not Deviate Without Escalation

Source: CLAUDE.md § Post-Build QA Gate (QA Levels)
Rule: Test-harness/test-file additions that do not change production component behavior are Level 0–1. Verify `npm test` runs green and `npm run build` is unaffected.

Source: CLAUDE.md § Working Patterns (shared-file rules)
Rule: `web/src/api/client.js` is project-critical. This story does not edit it. `package.json` is shared in the sense that two terminals must not edit it at once — sequence accordingly.

Source: OTA-790 (Apply/Reset) — advisory-vs-authoritative validation model
Invariant: the per-strategy invariants (eval_order uniqueness, scoring weights sum to 1.0000, verdict-band monotonicity) are NOT enforced at save against a draft. The client-side indicators in the F13 editors (OTA-785/786/788) are **ADVISORY** — they flag, they do not block. The authoritative catch lives at the preview path and at Apply (`load_config(include_draft_key)`). **Tests must assert these indicators are advisory** (flag fires, save is not blocked). No test may assert "rejected at save."

Source: prompt-style.md § Prompt File Template + § Standing reminder
Reason this story exists: the F13 editor prompts (OTA-785/786/787/788) included a "component test" verification step that could not run — `web/` has no test runner. This story makes that step executable and amends the template so future prompts reference the real runner.

Source: CLAUDE.md § House Style Rules
Rule: scores `##.00`; the monotonicity / weight-sum / collision logic under test must reflect the `##.00` and 1.0000-sum conventions where relevant.

## Phase 0 — Read-only investigation (HARD STOP — no edits)

Do not modify any file in this phase. Re-verify against the live tree.

Investigate and report:
1. **Confirm the gap.** `cat web/package.json`. Confirm there is no `test` script and no vitest/jest/@testing-library in dependencies or devDependencies. Report the current scripts block and the Vite version (vitest must match the Vite major — report it so the right vitest version is chosen).
2. **Locate the logic to test.** In `web/src/components/JunctionRowEditor.jsx` (and `ParameterEditor` / `defaultsFromSchema`, and any helper the editors use), find the three advisory checks:
   - verdict-band monotonicity,
   - scoring-weight sum (≈ 1.0000),
   - `evaluation_order` collision detection.
   Report for EACH: is it an exported pure function, an inline expression inside a component, or computed in `StrategyAdminPage.jsx`? **This determines testability.** If a check is inline and not extractable without refactoring a shipped component, report it — do NOT refactor production code in this story; instead test it via the component's rendered output (Testing Library) or recommend a follow-up extraction.
3. **Existing test conventions.** Confirm there is no `web/vitest.config.*`, no `web/src/**/*.test.*` / `*.spec.*` already present, and no test setup file. Report the cleanest config approach (standalone `vitest.config.js` vs. `test` block in `vite.config.js`).
4. **jsdom need.** Determine whether the three checks can be unit-tested as pure functions (no DOM) or require rendering (jsdom + Testing Library). Report which, per check.
5. **Doc touch points.** Identify the exact lines in `build-execution.md` and `prompt-style.md` (the prompt template's Verification steps area) that should reference `npm test`. Report them — these are SoT docs; per CLAUDE.md § Document Governance, doc edits are deliberate and logged.

**End Phase 0 with an explicit GO / NO-GO recommendation and STOP.** State: the vitest version you'll install, the config approach, whether any check needs a follow-up extraction story (don't refactor here), and the exact doc edits. Wait for Don's approval before any edit.

## Scope
After GO:
1. Add `vitest` (+ `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` as needed) to `web/` devDependencies; add a `test` script (and `test:run` for CI-style single-pass) to `package.json`; add the minimal config + setup file.
2. Seed tests for the three advisory invariants against the helpers identified in Phase 0:
   - verdict-band monotonicity: advisory flag fires on a non-monotonic set; does not throw / does not block.
   - scoring-weight sum: advisory flag fires when weights ≠ 1.0000; passes at exactly 1.0000 (respecting `##.00`/rounding tolerance).
   - `evaluation_order` collision: advisory flag fires on a duplicate order within a phase; clear when unique.
3. Amend `build-execution.md` and the `prompt-style.md` prompt template so the component-test verification step references `npm test` (update each file's `Last Updated` header and add a change-log entry citing OTA-829, per house style).

## Acceptance criteria
- `npm test` runs green from `web/`; a single-pass invocation exists for non-watch runs.
- The three seed tests exist and pass, and each asserts the indicator is **advisory** (flag fires; no save-block, no throw). No test asserts "rejected at save."
- `npm run build` is unaffected (no production bundle change).
- No production component behavior changed — only test files, config, `package.json`, and the two SoT docs are touched. If Phase 0 found a check that needs extraction to be testable, that extraction is deferred to a follow-up story (named in the Phase 0 report), not done here.
- `build-execution.md` and `prompt-style.md` updated with the `npm test` reference, `Last Updated` bumped, change-log entry citing OTA-829.

## Out of scope
- Backend / pytest changes.
- End-to-end or browser tests.
- Refactoring shipped editor components to extract logic (defer to a named follow-up if Phase 0 requires it).
- Broad coverage beyond the three seed invariants.

## Verification steps (PowerShell) — QA Level 1 (test-harness + docs; no production behavior change)
    cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
    .\venv\Scripts\Activate.ps1

    cd web
    npm install
    npm test            # or the single-pass script — expect all green
    npm run build       # expect unchanged success
    cd ..

    # Confirm no production component file changed (only tests/config/package.json/docs)
    git diff --stat
    #   -> expect: web/package.json, web/vitest.config.* (or vite.config.js), web/src/**/*.test.*,
    #      a setup file, claude_context/build-execution.md, claude_context/prompt-style.md
    #   -> NOT expect: web/src/components/JunctionRowEditor.jsx or any shipped editor component

## Commit instruction
"I have been instructed to commit. Do you approve? (yes / no)"

## Coordination footer
Independent — no downstream dependency. Run in Terminal B against a committed tree, parallel-safe with the Terminal A editor chain (read-only import of `JunctionRowEditor.jsx`; the only shared write surface is `package.json`, which Terminal A does not touch).

## Commit message template (if committing)
OTA-829 chore: add vitest harness + seed advisory-invariant component tests; wire npm test into prompt template (QA L1)
