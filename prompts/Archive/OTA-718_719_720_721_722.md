---
tickets: [OTA-718, OTA-719, OTA-720, OTA-721, OTA-722]
feature: OTA-712 — Options-chain (screening) input adapter
epic: OTA-679 — Insight Engine migration
terminal: T1 (Primary)
commit: yes (single commit covers all five producers)
allowedTools:
  - Read
  - Glob
  - Grep
  - Edit
  - Write
  - Bash
---

# OTA-718–722 — Five audit-gap producers (gamma, ATR_14, IV percentile, chart_state, is_etf)

## Terminal context
- **This terminal:** Terminal 1 (Primary) — OTA-712 options-chain screening adapter.
- **Concurrent terminals:** Terminal 2 (Secondary) — OTA-725 screening rule library. Disjoint files.
- **Cross-terminal dependencies:** Requires OTA-713–717 committed. Each producer below is **additive** to the adapter; they share no logic with each other. Implement them as five independent units with a stop-and-report gate after each — if any one needs a non-trivial data-source decision you cannot resolve from the docs, STOP and report rather than guessing.

---

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/SCHWAB-LOGIN-PROCESS.md
```
Plus (resolve in Phase 0): `insight_engine.md` §5.1 (catalog); `rules-engine-audit-2026-05-20.md` §5d and the gap notes; migration plan (S3.6–S3.10). For OTA-722 also read OTA-690's ticket body (the is_etf catalog requirement + Width Config lookup) — surfaced below.

Domain: **Schwab / broker / market data** + **Architecture / patterns / providers**.

---

## Relevant Context — Do Not Deviate Without Escalation

**Source: insight_engine.md §5.1** — every new producer is declared in `input_catalog` (name, tier, type, null semantics, producer reference). The engine validates references against the catalog at startup.

**Source: migration-plan gaps + audit §5d (verify each in Phase 0):**
- **OTA-718 gamma** — gamma exists in the raw Schwab chain but is never propagated to scored candidates. Add it to candidate construction so the WG Theta-Gamma-Ratio scoring criterion can read it. Tier: RAW (already in chain) → just propagate.
- **OTA-719 ATR_14** — neither computed nor fetched anywhere (a TODO since file creation). Implement it. Feeds the Cushion-vs-ATR gate and the Cushion-barely-above-ATR-floor adjustment. Tier: DERIVED from historical bars.
- **OTA-720 true IV percentile** — current input is an `ATM_IV / 0.60` proxy. Replace with a true percentile from historical IV. **Data-source decision required** (Schwab historical IV vs. external source) — document the decision in the producer and the catalog; if the source is not resolvable from SCHWAB-LOGIN-PROCESS.md / architecture-plan.md, STOP and report before implementing.
- **OTA-721 chart_state** — sheet rules use `chart_state ∈ {Bullish, Bearish, Mixed, Neutral}`; code has only `sma_alignment` (from OTA-717) with no mapping. Implement the mapping inside the adapter. Tier: DERIVED enum, derived from `sma_alignment` and related signals.
- **OTA-722 is_etf** — no ETF detection exists. Add a producer (likely from a symbol reference table). Tier: DERIVED/RAW boolean.

**Source: OTA-690 (config side — still Write Prompt under OTA-680) — coupling for OTA-722**
OTA-690 declares `is_etf` as the input-catalog **requirement** and seeds the ETF tier in the Width Configuration `engine_lookups` set; it names the producer as "the options-chain adapter." OTA-722 is that producer. **The `is_etf` named value must match OTA-690's contract exactly: name `is_etf`, boolean type, and identical null semantics.** Note for Don/Claude Web (not for this terminal to resolve): the *consuming* ETF adjustment rule and Width Config tier are not active until OTA-690 ships — OTA-722 publishes the producer regardless, but end-to-end ETF behavior waits on OTA-690.

**Source: CLAUDE.md** — provider routing via `_get_provider()`; async `.aio` credentials in async paths; producers emit raw numerics/enums, not formatted strings.

---

## OTA-718 — gamma propagation
**Scope:** propagate gamma from the raw chain into candidate construction. **AC:** gamma is present on scored candidates and declared in the catalog; the WG Theta-Gamma-Ratio criterion can resolve it. **Out of scope:** the Theta-Gamma-Ratio formula itself (that is the screening rule library, Terminal 2).

## OTA-719 — ATR_14
**Scope:** implement the ATR_14 producer from historical bars. **AC:** ATR_14 is produced and cataloged (DERIVED); the long-standing TODO is removed; Cushion-vs-ATR inputs are satisfied. **Out of scope:** the Cushion-vs-ATR gate/adjustment formulas (Terminal 2).

## OTA-720 — true IV percentile
**Scope:** replace the `ATM_IV / 0.60` proxy with a true historical IV percentile; document the data-source decision. **AC:** the proxy is gone; a true percentile producer is implemented and cataloged; the data-source decision is recorded in the producer docstring and the catalog entry. **Out of scope:** the IV-rank scoring criterion (Terminal 2). **Gate:** if the historical-IV source is ambiguous, STOP and report before implementing.

## OTA-721 — chart_state enum
**Scope:** map `sma_alignment` (+ related signals) to `chart_state ∈ {Bullish, Bearish, Mixed, Neutral}` inside the adapter. **AC:** `chart_state` produced and cataloged (DERIVED enum, value set documented); the chart-state-confirms-direction gate and mixed-chart-signal adjustment have their input. **Out of scope:** those gate/adjustment formulas (Terminal 2).

## OTA-722 — is_etf
**Scope:** implement the `is_etf` producer (likely from a symbol reference table). **AC:** `is_etf` produced and cataloged as boolean with null semantics **matching OTA-690's contract exactly**; producer reference noted. **Out of scope:** seeding the Width Config ETF tier and the ETF adjustment rule (OTA-690, config side); the ETF adjustment formula (Terminal 2).

---

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
& ".\venv\Scripts\Activate.ps1"

python -m pytest tests/ -k "options_chain and (gamma or atr or iv_percentile or chart_state or is_etf)" -q

# All five new producers present in catalog with correct tiers/types
python -c "from app.ota_adapters.options_chain import OptionsChainAdapter; c={e.name:e.tier for e in OptionsChainAdapter().input_catalog}; print({k:c.get(k) for k in ['gamma','ATR_14','iv_percentile','chart_state','is_etf']})"

# Proxy removed
Select-String -Path "app\ota_adapters\options_chain\*.py","app\analysis\*.py" -Pattern "0\.60|ATM_IV\s*/" 
```
Apply **Level 2**: spot-check each new producer against a known symbol; confirm `is_etf` semantics match OTA-690. Record the level.

---

## Commit instruction
**I have been instructed to commit. Do you approve? (yes / no)** — the five producers commit together as the adapter's gap-closure unit.

## Coordination footer
**OK to continue to `OTA-723.md`**

## Commit message template
```
OTA-718 OTA-719 OTA-720 OTA-721 OTA-722 feat: add gamma, ATR_14, true IV percentile, chart_state, and is_etf producers to the options-chain adapter
```

---

## Phase 0 — read-only discovery (no edits)
Report, then wait: (1) where gamma currently lives in the raw chain payload and why it's dropped; (2) the historical-bars source for ATR_14 and whether it's already fetched; (3) the historical-IV source options and which SCHWAB-LOGIN-PROCESS.md / architecture-plan.md supports — flag if ambiguous; (4) the `sma_alignment` enum values (from OTA-717) to map into `chart_state`; (5) the symbol reference table for `is_etf` and OTA-690's exact `is_etf` type/null contract. Surface any data-source ambiguity (esp. OTA-720) before implementing that producer.
