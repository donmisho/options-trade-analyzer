# OTA-808 — Update architecture-plan.md for migrated engine architecture

## Terminal context
- This terminal: Terminal A (single stream)
- Concurrent terminals: OTA-807 / OTA-809 / OTA-810 may run in parallel — disjoint files.
- Cross-terminal dependencies: OTA-812 (cross-doc reference sweep + LLM-orchestration contract) is **already Code & Test Complete** and has edited `architecture-plan.md` (split the Insight Engine section into generic-engine vs Communicator, added the LLM-orchestration contract). This story layers the package/persistence/sink content onto that file and **cross-references** 812's content — it does not duplicate it. Phase 0 confirms 812's edits are present.

## Required reading
Before any edits:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
```
Also locate and read (canonical in repo; confirm path with `Get-ChildItem -Recurse -Filter <name>`):
- `insight_engine.md` — §8 (package boundaries), §4.3 (sink + bronze contract).
- `insight_engine-schema-ddl.md` — the as-built bronze DDL.

## Relevant Context — Do Not Deviate Without Escalation

**Source: OTA-808 scope.** Add to `architecture-plan.md`:
- **Package boundaries** in the directory map: `app/insight_engine/` (generic engine core), `app/ota_adapters/*` (input adapters), `app/options_rules/*` (rule libraries). Replace the prior "domain-specific insight generation" framing with the generic Insight Engine.
- **Persistence read-path** section: the bronze tables (write-path, owned by the engine sink) feed a Databricks bronze/silver/gold expansion of `payload_json` into analytics tables — Phase 2/3 of the hybrid OLTP + schema-on-read pattern; out of scope for the engine but documented here.
- **Engine-sink wiring** section: how OTA injects its Azure SQL sink at startup, and the path to a shared cross-app bronze zone when STK/FFL arrive.

**Source: as-built reconciliation (CRITICAL).** The as-built persistence is a **single `bronze_evaluations` table discriminated by a `record_type` column** — not the two-table model described in `insight_engine.md` §4.3. Document the **single-table** model. If §4.3 still shows two tables, the DDL wins; note the doc reflects as-built and flag the §4.3 discrepancy in your report rather than copying §4.3.

**Source: OTA-812 (done) — do not duplicate.** The LLM-orchestration contract and the generic-engine vs Insight-Communicator split were authored by OTA-812 and already exist in `architecture-plan.md`. Reference them; do not re-author or restate them.

**Source: `CLAUDE.md` House Style (SoT doc edits).** Update the `Last Updated` header (`yyyy-mm-dd hh:mm UTC`) and add a Change Log entry citing **OTA-808**.

## Scope

### Phase 0 — Discovery (READ-ONLY, then STOP)
No edits. Report:
1. Confirm OTA-812's edits are present in `architecture-plan.md` (the generic-engine vs Communicator split and the LLM-orchestration contract section). **If they are NOT present, STOP** — 812 is not yet in the working tree and 808 must not run ahead of it.
2. Where the directory map and persistence sections live now, and what the current "insight generation" framing says.
3. Whether `insight_engine.md` §4.3 currently shows one or two bronze tables, and what the as-built DDL shows — so the doc lands on the single-table model.
4. A proposed placement for the three new sections.

**STOP and report. Proceed only on approval.**

### Phase 1 — Edit (after approval)
Add the three sections, replace the old framing, cross-reference 812's LLM-orchestration contract, document the single-table `bronze_evaluations` model. Update header + Change Log (OTA-808).

## Acceptance criteria
- Directory map shows `app/insight_engine/`, `app/ota_adapters/*`, `app/options_rules/*` with correct boundaries.
- The "domain-specific insight generation" framing is replaced by the generic Insight Engine.
- Persistence read-path and engine-sink wiring sections exist and match the as-built **single-table** `bronze_evaluations` model.
- The LLM-orchestration contract section is present as a cross-reference to 812's content (not duplicated).
- `Last Updated` header + Change Log entry (OTA-808) present.

## Out of scope
- No code changes.
- Do not re-author the LLM-orchestration contract or the engine/Communicator split (OTA-812 owns them).
- Do not document a two-table bronze model.

## Verification steps
- Directory map contains the three packages.
- `Select-String -Path claude_context\architecture-plan.md -Pattern "domain-specific insight generation"` → no hits.
- Persistence sections reference a single `bronze_evaluations` table with `record_type`; no two-table description.
- LLM-orchestration section present and reads as a cross-reference, not a duplicate.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency. (Phase 0 gates on OTA-812's edits already being in the tree.)

## Commit message template (if committing)
```
OTA-808 docs: update architecture-plan.md with engine package boundaries, bronze read-path, and sink wiring
```
