# OTA-784 — Strategy editor shell and selector

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none
- Cross-terminal dependencies: backend CRUD (OTA-782) + validation (OTA-783) committed in Wave 1

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/UI-GUIDANCE.md
cat claude_context/architecture-plan.md
cat claude_context/insight_engine.md
cat claude_context/strategy-rules-prototype-v2.html
```

(Frontend work → `UI-GUIDANCE.md` required; cross-cutting new page → `architecture-plan.md`. The prototype is the layout contract.)

## Phase 0 — Read-only investigation (hard go/no-go STOP)

Do NOT edit in Phase 0. Investigate and report:

1. The frontend routing/page structure and where a new admin page/route is registered.
2. `web/src/api/client.js` (**project-critical shared file**) — this story adds the config read/write client methods other F13 stories will reuse. Plan additive edits only.
3. Shared components to reuse, not reinvent: `QuoteBar`, `ResultsTable` (`web/src/components/`), `formatDate()` (`web/src/utils/formatDate.js`), tokens (`web/src/styles/tokens.js`).
4. The OTA-782 CRUD + OTA-762 read endpoints this UI calls (confirm shape).
5. **Draft-substrate reconcile.** The selector/shell loads a strategy for editing; OTA-791 (next-but-one) makes edits land on a draft row. Build the shell to load the live strategy read-only-until-edit and leave a clean seam for the draft session OTA-791 introduces. If you find yourself needing draft behavior to make the shell work, STOP and flag — sequencing may need revisiting.
6. In the prototype: the strategy nav/selector (`.nav-strategy`), the editable header card, the three-tab structure, and the Verdict Bands section — confirm the structural anchors.

Report findings + go/no-go.

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine-schema-ddl.md` §2 (`engine_strategies` fields)
- Header fields map to: `strategy_key`, `display_name`, `consumer_surface`, `compatible_structures` (JSON, screening only), `dte_min`/`dte_max`, `enabled`, plus status (see below). Per-rule weights/params are NOT here — they live on the junction (the tab stories).

Source: OTA-525 (lifecycle vocabulary) + OTA-781 key design decision
- Status vocabulary is `active` / `inactive` / `deprecated`. `draft` is **reserved** for the OTA-791 preview mechanism — it is not a user-selectable status in this editor.

Source: ticket AC + `insight_engine.md` §6.1
- The selector lists OTA strategies (`owner_app_id='OTA'`) and loads the selected one. SHARED strategies (if any) are visible but read-only.

Source: `UI-GUIDANCE.md` Part 3 / Part 3a / Part 4
- Dark-theme CSS variables only — never inline hex; colors come from `web/src/styles/tokens.js`.
- `var(--bg2)` is allowed only on QuoteBar / filter bars / inset & callout boxes. **Never** on table rows, table headers, expansion panels, group headers, section backgrounds, or any full-width band.
- Dates render `mm-dd-yyyy` via `formatDate()`. No `$` prefix on any value. Scores `##.00` (0–100).

## Scope
Build the editor shell per the prototype: strategy selector; per-strategy header (key, label, status, enabled structures, DTE range); the three-tab scaffold (Hard Gates / Scoring / Adjustments) with switchable tab bodies left as mount points for their own stories; and the Verdict Bands panel mount point. This story is the container only — tab bodies and the bands editor are OTA-785/786/787/788.

## Acceptance criteria
- Selector lists `owner_app_id='OTA'` strategies and loads the selected strategy into the editor; SHARED strategies are visible but read-only.
- Header fields render and edit: key, label, status (`active`/`inactive`/`deprecated`), enabled structures, DTE range.
- The three-tab scaffold and Verdict Bands panel are present and switchable; tab/panel bodies are mount points filled by later stories.
- Layout matches the prototype's structure (selector + header + tabs + bands panel).
- `web/src/api/client.js` config methods are added additively and reused by the shell.

## Out of scope
- Tab body contents (OTA-785/786/787/788), catalog browser (OTA-789), draft/preview (OTA-791), Apply/Reset (OTA-790).
- Writing junction rows (the tabs do that).

## Verification steps
- Manual click-through: select each strategy, edit header fields, switch tabs, confirm mount points render placeholders.
- Render test for the shell.
- UI-GUIDANCE spot check: tokens only, `--bg2` not on panels/rows/headers, `formatDate()` for any dates.
- **QA level: medium.** Single new surface; manual click-through of the shell per the QA gate.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-789.md

## Commit message template (if committing)
OTA-784 feat: strategy editor shell + selector (header, tab scaffold, bands mount)
