# OTA-685 — Reconcile sheet-vs-code divergences (capture authoritative values + rationale)

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none assumed — confirm with Don.
- Cross-terminal dependencies: requires OTA-681 (tables) + OTA-682 (importer); shares the rule set with
  OTA-683/686/688. Produces `engine_rules` values + junction `rationale` entries.

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-schema-ddl.md
# Capture source (temporary): requirements/Scoring Parameters.xlsx (Sheet1)
# Code-side divergences: requirements/insights-engine-audit-2026-05-20.md  § 2 (rule-by-rule comparison)
```

## Relevant Context — Do Not Deviate Without Escalation

Source: Don, project direction (operating principle for ALL seed stories)
The spreadsheet is a **temporary capture vehicle**. Once seeded, the `engine_*` tables are the engine's
source of truth. `business-rules.md` documents the high-level business approach, not specific values. **We
are not litigating value-correctness now** — the goal is that every rule is captured so the engine can read
and process it. Value tuning happens LATER, via the rule-management screen, after engine/app/UI work.
Therefore for every divergence: **seed the sheet value as captured, record the divergence in the junction
`rationale`, and move on. Do NOT make a value-correctness decision.**

Source: audit `insights-engine-audit-2026-05-20.md` § 2 — record these divergences (sheet value seeded;
code value noted in `rationale` for later tuning):
- **Liquidity floors:** OI (sheet 100; engine 50; scorer 10), volume (sheet 500; engine 5; scorer 1),
  per-leg bid/ask (sheet 0.15; verticals none; scorer 0.50). Seed the sheet floors; rationale notes the
  relaxed code values + "relaxed for low-volume symbols — revisit in tuning."
- **Min DTE boundary:** sheet `>= 7`; code `<= 7 -> PASS` (off-by-one at 7). Seed `>= 7`; note the boundary.
- **Max DTE:** sheet 60; chain-fetch 70; NakedOptionEngine 90. Seed 60; note the wider code values.
- **Cushion % of price:** sheet hard gate (SP `>= 1%`, WG `>= 1.5%`); code = graduated penalty (1%/2%, no
  WG-specific). Seed the sheet gate values; note the code implements it as a penalty (the graduated penalty
  itself is captured separately in OTA-688 as a layered post-scoring adjustment — "intentional layering"
  per the sheet).
- **Data-completeness gates** (IV-rank / delta / ATR_14 IS NOT NULL): sheet gates on null; code substitutes
  defaults / B-S fallback. Seed the gates; rationale flags that ATR_14 and true IV-rank have **no producer
  yet** (options-chain adapter, later feature) and delta has a B-S fallback.
- **Underlying price floor:** sheet `>= 20` (MISSING in code). Seed it.
- **Spread-width tier compliance:** sheet 5-tier table; code single `max_spread_width = 10`. Seed references
  the OTA-690 `width_configuration` lookup; note the code's single value.
- **Trade Type structure enablement (WG, LT):** sheet vs `business-rules.md`/code disagree — WG (sheet adds
  LONG_CALL + LONG_PUT; code credit-only); LT (sheet LONG_PUT + IRON_CONDOR; code LONG_CALL + LONG_PUT, no
  IC). OTA-687 already seeded the **sheet** values. This story RECORDS the divergence in `rationale`
  ("differs from business-rules.md — reconcile during tuning"); it does NOT change OTA-687's seeded values.

Source: `insight_engine-schema-ddl.md` § 2
Divergence notes live in the junction `rationale` (or `engine_rules.intent` where the divergence is about
the rule definition rather than a strategy's parameter). Keep each note to one line.

## Scope
For every PARTIAL / DIVERGENT / MISSING row in audit § 2, ensure the **sheet** value is the captured value
in `engine_rules` / junction, and record a one-line divergence note (sheet vs code) in the appropriate
`rationale` / `intent` field for later tuning.

## Acceptance criteria
- Every § 2 PARTIAL/DIVERGENT/MISSING row has its sheet value captured and a one-line divergence note in
  `rationale`/`intent`.
- No value-correctness decision is embedded — notes describe the divergence and defer to tuning.
- The WG/LT structure divergence is recorded as a note; OTA-687's seeded structures are unchanged.
- The ATR_14 / true-IV-rank "no producer yet" dependency is flagged on the relevant rules.
- No § 2 divergence is left without either a captured value or a recorded note.

## Out of scope
- Choosing which value is "correct" (tuning, later, via the rule-management screen).
- Creating the rules themselves (OTA-683 decompositions, OTA-688 backfills) — this story records
  divergences against the captured rules; coordinate so notes attach to existing rows.
- Building input producers (ATR_14, true IV rank) — adapter feature, later.

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
```
- For each § 2 divergence, confirm the captured value matches the sheet and a `rationale`/`intent` note
  records the code divergence.
- Spot-check the liquidity floors (OI 100, vol 500, bid/ask 0.15) are the seeded values with relaxed-code
  notes; confirm the WG/LT structure note exists without altering OTA-687's rows.

Post-Build QA Gate: Level 1 (config-data + rationale notes). Note in commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
STOP until OTA-683, OTA-686, OTA-687, and OTA-688 complete  *(divergence notes attach to rules/junction rows those stories create; run last among the content stories).*

## Commit message template (if committing)
```
OTA-685 feat: capture sheet values + record sheet-vs-code divergences

Seeds sheet-authoritative values; one-line rationale notes for each PARTIAL/
DIVERGENT/MISSING row (liquidity floors, DTE boundaries, cushion gate-vs-penalty,
data-completeness, WG/LT structures). Value-correctness deferred to tuning. QA L1.
```
