# OTA-690 — ETF detection input (is_etf catalog requirement + Width Config lookup)

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none assumed — confirm with Don.
- Cross-terminal dependencies: requires OTA-681 (tables) + OTA-682 (importer). Largely independent of the
  other content stories; touches `engine_lookups` and the ETF adjustment rule's `referenced_named_values`.

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md                 # § 6.1 engine_lookups; input-catalog (referenced_named_values)
cat claude_context/insight_engine-schema-ddl.md       # § 2 engine_lookups, engine_rules.referenced_named_values
# Canonical content: requirements/Scoring Parameters.xlsx — "Width Configuration" sheet + Sheet1 "ETF underlying" rule
```

## Relevant Context — Do Not Deviate Without Escalation

Source: `Scoring Parameters.xlsx` — "Width Configuration" sheet → seed an `engine_lookups` set
(`lookup_set = 'width_configuration'`, `owner_app_id = 'OTA'`). Default Widths by Price Tier:

| Price Min | Price Max | Width Min | Width Max | Width Increment |
|---|---|---|---|---|
| 20 | 50 | 1 | 5 | 1 |
| 50 | 150 | 2.5 | 10 | 2.5 |
| 150 | 300 | 5 | 20 | 5 |
| 300 | 500 | 10 | 25 | 5 |
| 500 | 99999 | 25 | 50 | 5 |

Plus a per-security override structure (Ticker, Width Min, Width Max, Width Increment, Note) — currently
empty; seed the set/shape so overrides can be added later. Engine logic (consumed at runtime, not built
here): look up the ticker in the override set first; if absent, fall back to the price-tier default.

Source: `Scoring Parameters.xlsx` Sheet1 — "ETF underlying" (Post-Scoring, INCREASE SCORE BY 5 IF underlying
IS ETF; applies to all four strategies). Audit gap: `is_etf` is not currently produced as an input.

Source: OTA-690 description + `insight_engine.md` § 5/§ 6.1
1. Register `is_etf` in the input-catalog requirements: it must appear in the ETF adjustment rule's
   `engine_rules.referenced_named_values`, with the producer noted as the options-chain adapter (a later
   feature, not yet ticketed — this story records the REQUIREMENT, it does not build the producer).
2. The ETF adjustment rule references both `is_etf` and the `width_configuration` lookup as needed.

Source: OTA-682 model
Produce these as `engine_lookups` rows + the rule's `referenced_named_values` via the seed path. Build
mechanism is your choice within the importer framework; do not fork a second seed path.

## Scope
Seed the `width_configuration` `engine_lookups` set (five default tiers + the override-set shape), and add
`is_etf` to the ETF adjustment rule's `referenced_named_values` with the options-chain-adapter producer note.

## Acceptance criteria
- `engine_lookups` carries the `width_configuration` set with all five price tiers (exact min/max/width/
  increment from the table) under `owner_app_id = 'OTA'`; every `payload` is `ISJSON = 1`.
- The per-security override structure is seeded (shape present, rows empty) so overrides can be added later.
- `is_etf` appears in the ETF adjustment rule's `referenced_named_values`, with the producer noted as the
  options-chain adapter.
- The ETF adjustment rule references both `is_etf` and the `width_configuration` lookup entry it needs.

## Out of scope
- Building the `is_etf` producer (options-chain adapter — later feature).
- The runtime tier-lookup/override-resolution logic (engine core / adapter — later feature). This story
  seeds the data + declares the input requirement only.

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
```
- `SELECT lookup_key, payload FROM dbo.engine_lookups WHERE lookup_set = 'width_configuration'` → five tier
  rows matching the table; override-set shape present.
- The ETF rule's `referenced_named_values` includes `is_etf`; all JSON validates.

Post-Build QA Gate: Level 1 (lookup-data seed). Note in commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency.  *(Other content stories don't depend on this one; run any time after OTA-681/682.)*

## Commit message template (if committing)
```
OTA-690 feat: seed width_configuration lookups + is_etf input requirement

Five price-tier width rows + empty per-security override shape in engine_lookups;
is_etf registered in the ETF adjustment rule's referenced_named_values (producer =
options-chain adapter, later). QA Level 1.
```
