---
ticket: OTA-724
feature: OTA-712 — Options-chain (screening) input adapter
epic: OTA-679 — Insight Engine migration
terminal: T1 (Primary)
commit: yes
allowedTools:
  - Read
  - Glob
  - Grep
  - Edit
  - Write
  - Bash
---

# OTA-724 — Options-chain adapter integration tests + /analyze parity check

## Terminal context
- **This terminal:** Terminal 1 (Primary) — OTA-712 options-chain screening adapter. **This is the final story of the feature.**
- **Concurrent terminals:** Terminal 2 (Secondary) — OTA-725 screening rule library. Disjoint files.
- **Cross-terminal dependencies:** Requires OTA-713–723 committed (full adapter + published catalog). Parity is end-to-end through the engine; the engine core (OTA-695 stories) is Code & Test Complete.

---

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
```
Plus (resolve in Phase 0): `insight_engine.md` §5; migration plan (S3.12); the Epic OTA-679 parity acceptance criterion ("same Schwab inputs produce the same verdicts as pre-extraction for SP/WG/TR/LT").

Domain: **Scoring / gates / health / P&L** (parity is verdict-level) + **Architecture / patterns / providers**.

---

## Relevant Context — Do Not Deviate Without Escalation

**Source: Epic OTA-679 acceptance criteria (end-to-end parity)**
> Same Schwab inputs produce the same verdicts as pre-extraction for SP/WG/TR/LT.
The parity bar is **verdict-level** (and score-level where the audit did not deliberately change a value). Where the migration deliberately fixed an audit divergence (e.g. the IV-rank proxy replaced in OTA-720, divergences reconciled during the OTA-680 seed), parity is against the **corrected** expectation, not the legacy buggy output — document each such intentional difference rather than forcing it to match the old number.

**Source: migration plan (S3.12)**
Test the adapter against the engine with the canonical Scoring Parameters configuration. End-to-end parity: `adapter.produce_candidates` + `engine.evaluate(...)` reproduces pre-extraction `/analyze/*` results for SP/WG/TR/LT.

**Source: CLAUDE.md — validation baseline (OTA-149)**
Validation tickers: AAPL, XOM, IWM, LLY, AVGO, META. Use these as the parity fixture set where practical; baseline agreement context is documented under OTA-149.

**Source: insight_engine.md §5.2 / tier contract**
Parity must hold through the COMPUTED callback: confirm `populate_computed` fires once on survivors only and the COMPUTED values feed the verdict identically.

---

## Scope
Build the adapter↔engine integration test suite and the `/analyze/*` parity check. Tests run the adapter against the engine using the canonical seeded configuration and assert verdict (and score, modulo documented intentional fixes) parity with pre-extraction `/analyze/*` output for SP/WG/TR/LT across the validation tickers.

## Acceptance criteria
- An integration suite exercises `produce_candidates` → `engine.evaluate(...)` end-to-end against the canonical config for SP/WG/TR/LT.
- For the OTA-149 validation tickers, verdicts match pre-extraction `/analyze/*` results; score-level differences are either zero or attributed to a documented intentional audit fix (with the fix named).
- The COMPUTED callback is exercised: a test confirms `populate_computed` fires once, on survivors only, with exactly the needed COMPUTED names, and that COMPUTED values reach the verdict.
- A test confirms the adapter contributes no rule/scoring/verdict logic — the engine produces the verdict; the adapter only supplies named values.
- The suite is runnable in CI and green.

## Out of scope
Changing producers or the catalog (OTA-714–723), screening rule library (Terminal 2 / OTA-725), wiring `/analyze/*` routes to the engine in production (that is the Wave-4 app-side wiring feature — this story tests parity, it does not flip the production route).

---

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
& ".\venv\Scripts\Activate.ps1"

python -m pytest tests/ -k "options_chain and (integration or parity)" -q

# Parity report across validation tickers
python -m pytest tests/ -k "parity" -q -ra
```
Apply **Level 3** (scoring-engine-adjacent + cross-cutting): full adapter integration suite + verdict parity across the OTA-149 tickers + a scoring-narrative consistency spot check. Record the level and attach the parity report summary to the commit body, naming any intentional divergences.

## Commit instruction
**I have been instructed to commit. Do you approve? (yes / no)** — this commit closes the OTA-712 feature.

## Coordination footer
**Independent — no downstream dependency.** OTA-712 (options-chain adapter) is complete after this commit. Terminal 1 may close.

## Commit message template
```
OTA-724 feat: options-chain adapter integration tests + /analyze verdict parity (SP/WG/TR/LT) closing OTA-712
```

---

## Phase 0 — read-only discovery (no edits)
Report, then wait: (1) how pre-extraction `/analyze/*` results are captured today (fixtures, recorded baselines, or live) so parity has a reference; (2) the list of intentional audit fixes that will legitimately diverge from legacy output (IV-rank proxy, seed reconciliations) so they're asserted-as-corrected, not forced-to-match; (3) the canonical config load path for SP/WG/TR/LT; (4) existing test scaffolding for the engine (from OTA-707) that this suite can extend. If no pre-extraction baseline exists to compare against, STOP and report — parity needs a reference.
