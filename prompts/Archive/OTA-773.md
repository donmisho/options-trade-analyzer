---
description: OTA-773 — Per-strategy verdict bands (config side; pairs with OTA-761)
allowed-tools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash
---

# OTA-773 — Per-strategy verdict bands (config side)

## Terminal context
- This terminal: **Terminal A**.
- Concurrent terminals: **none**.
- Cross-terminal dependencies: none. **Prompt 6 of 6 — end of the OTA-766 chain.** Config-side pair to **OTA-761** (app-side, already authored in the OTA-757 set — removes the hardcoded 70/50 verdict literals from `_assign_verdict`). The two must stay consistent: the bands this story seeds must equal the literals OTA-761 removes, so app and engine config cannot drift.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
cat claude_context/insight_engine-schema-ddl.md
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: insight_engine.md §3.8 (Verdict bands) + §4 Phase 7 (Verdict band lookup)**
Verdict bands are **per-strategy**. Phase 7 maps the final score to a verdict via the strategy's own band set. There is no universal verdict band.

**Source: insight_engine-schema-ddl.md §"engine_strategies"**
The band set lives in `engine_strategies.verdict_band_set` — `nvarchar(MAX)` JSON, `ISJSON = 1` checked, one row per strategy. The engine validates the set is **monotonic** at load (§6.6).

**Source: OTA-695 confirmed assumptions**
Three score bands stand: EXECUTE / WAIT / PASS at **70 / 50** for the screening strategies. `WAIT_FOR_EARNINGS` is an earnings-gate halt verdict (terminal_verdict on the junction row), **not** a fourth band.

**Source: OTA-761 (Relates) — "Remove hardcoded verdict bands from app layer"**
OTA-761 strips the hardcoded 70/50 literals from `_assign_verdict`. This story seeds those same 70/50 thresholds into each screening strategy's `verdict_band_set` so the engine band lookup is the single source. **Embed the exact values OTA-761 removes** as the seeded band edges — they must match.

**Source: OTA-766 scope anchor**
Independence only — this story owns the **config side** (define + seed per-strategy bands). The code-removal side is OTA-761 / S5.3.

## Phase 0 — Read-only discovery (HARD STOP before any edits)

Do not edit in Phase 0.

1. Find the verdict-band literals and the engine band lookup:
   ```powershell
   rg -n "_assign_verdict|verdict_band|EXECUTE|WAIT|PASS|70|50" app/analysis app/insight_engine
   ```
2. `cat` `_assign_verdict` (the literals OTA-761 targets) and the engine's Phase-7 band-lookup code (OTA-695). Confirm the engine reads `engine_strategies.verdict_band_set`.
3. For each screening strategy (SP/WG/TR/LT), inspect its current `engine_strategies.verdict_band_set` value — is it already seeded with 70/50, empty, or placeholder?
4. Confirm the `verdict_band_set` JSON shape the engine expects (band edges, verdict labels, monotonic order).
5. **Seed mechanism:** how `verdict_band_set` reaches the live `engine_strategies` rows — code/JSON seed, SQL migration, admin UI, or `Scoring Parameters.xlsx`. Claude-editable vs Don-applied.
6. Verify OTA-761's status — if it has NOT yet shipped, flag the ordering: the engine must read seeded bands before the app literals are removed, or verdicts break.

**STOP report (paste back to Claude.ai):**
- `_assign_verdict` literals + file:line; exact band edges (expected 70/50) and labels.
- Engine Phase-7 band-lookup location; confirms it reads `verdict_band_set`.
- Current `verdict_band_set` per screening strategy.
- Expected JSON shape.
- Seed mechanism + Claude-editable vs Don-applied.
- OTA-761 status and any ordering risk.
- Go / no-go.

Wait for Claude.ai's go decision.

## Scope (after go)
- Define each screening strategy's `verdict_band_set` (SP/WG/TR/LT) with its own EXECUTE/WAIT/PASS bands at the values OTA-761 removes (70/50), in the engine-expected JSON shape.
- Ensure the engine's Phase-7 band lookup reads verdicts from per-strategy `verdict_band_set` (confirm; no code change if already wired by OTA-695).
- Seed the values. If config is Don-applied (xlsx/UI/SQL), STOP and hand Don the exact `verdict_band_set` JSON per strategy.

## Acceptance criteria
- Per-strategy verdict bands are defined in `engine_strategies.verdict_band_set` (Strategies-tab config).
- The engine's band lookup reads verdicts from per-strategy config.
- No per-strategy verdict-band values remain hardcoded (coordinated with OTA-761's removal).
- Seeded bands equal the values OTA-761 removes — app and engine config agree.
- Each band set passes the engine's monotonic-load validation.

## Out of scope
- Do not remove the app-layer `_assign_verdict` literals — that is OTA-761 (must be coordinated, not duplicated here).
- No new verdict logic or extra bands; `WAIT_FOR_EARNINGS` stays a halt verdict, not a band.
- No changes to scoring, delta, cushion, or normalization.

## Verification steps
- Engine startup validation passes: every screening strategy's `verdict_band_set` is present, valid JSON, monotonic.
- For representative scores around the 70 and 50 edges, the engine emits EXECUTE/WAIT/PASS identical to the pre-change `_assign_verdict` output (parity with OTA-761's removed literals).
- `rg` confirms verdict thresholds are not duplicated in code once OTA-761 lands.
- `pytest` green.

## Commit instruction
I have been instructed to commit. **Don runs the commit manually** — do not run `git commit`. Surface any Don-applied config (and the OTA-761 ordering note) in the approval ask. Present the changed-file list + the exact message below, then ask: **Do you approve? (yes / no)**

## Coordination footer
**Independent — no downstream dependency.** End of the OTA-766 chain. Terminal A can close after this commit.

## Commit message template
```
OTA-773 feat: seed per-strategy verdict bands into engine_strategies.verdict_band_set; engine band lookup is the single source
```

## Recommended QA level
**High** — verdict bands decide the final EXECUTE/WAIT/PASS output and are coupled to OTA-761; require edge-case parity (scores at 49/50/51 and 69/70/71) before commit.
