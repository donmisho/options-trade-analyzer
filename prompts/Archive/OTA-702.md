---
allowedTools: [Read, Grep, Glob, Write, Edit, Bash]
---

# OTA-702 — Adapter callback for COMPUTED-value population

## Terminal context
- This terminal: Terminal B (engine-core build)
- Concurrent terminals: Terminal A (MCP Server build, OTA-605 → OTA-609)
- Cross-terminal dependencies: none. Formalizes the COMPUTED-callback seam left in OTA-701; in-package only. No edit to any shared file. If wiring into a shared file appears necessary, **HARD STOP and report**.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/insight_engine.md
```

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine.md` §5.2 / §4.1 (COMPUTED callback contract)
Rule: Between Phase 2 (DERIVED gates) and Phase 3 (COMPUTED gates), the engine collects the set of COMPUTED named-value names referenced by **remaining active rules** (Phase-3 gates, scoring criteria, adjustments), passes the **surviving** candidate set plus that name set to the adapter, and ingests the populated candidates. Signature: `adapter.populate_computed(candidates: list[Candidate], needed: set[str]) -> list[Candidate]`.

Source: `insight_engine.md` §3.5 (tier system as performance contract)
Rule: COMPUTED values are **never** computed for candidates already halted at RAW/DERIVED gates. The engine knows the survivor set; the adapter knows *how* to compute. The engine defines the callback signature; it implements **no** COMPUTED math (that is the adapter, Wave 3).

## Scope
Formalize the COMPUTED-population callback: the adapter Protocol method, the needed-names collection, the survivor filtering, and once-only invocation at the Phase 2 → Phase 3 boundary.

### Phase 0 — Discovery (READ ONLY, no edits)
1. Read the OTA-701 orchestrator and locate the Phase 2 → Phase 3 seam.
2. Confirm how the orchestrator tracks survivors vs. halted candidates and how rules expose their `referenced_named_values` + tier (OTA-697 / OTA-698).
3. Confirm whether an input-adapter Protocol already exists; if not, define a minimal one carrying `populate_computed` (the concrete OTA adapter is Wave 3).
4. Report findings. If a COMPUTED callback already exists from a precursor, **HARD STOP and report**.

### Phase 1 — Implementation
1. Define the input-adapter Protocol method `populate_computed(candidates, needed) -> candidates` (engine-side contract only).
2. At the seam, collect `needed` = exactly the COMPUTED named-value names referenced by **remaining active rules** — nothing more.
3. Pass only **surviving** candidates (exclude any halted at RAW/DERIVED) to the adapter; ingest the returned, populated candidates.
4. Ensure the callback fires **exactly once** per run.

## Acceptance criteria
- The callback fires exactly once, between Phase 2 and Phase 3, with only surviving candidates.
- `needed` contains exactly the COMPUTED names referenced by remaining active rules — nothing more.
- Halted candidates are excluded from the survivor set passed to the adapter.
- The engine defines the callback signature and implements no COMPUTED math.
- OTA-696 guard/CI scan still passes.

## Out of scope
- Any COMPUTED math or the concrete OTA options adapter (Wave 3).
- Result builder (OTA-703), bronze (OTA-704), sink (OTA-705).
- Any options-domain term or rule value; any edit to a shared file.

## Verification steps
1. Fixture: a strategy with COMPUTED-tier rules in Phase 3 + scoring. Run with a synthetic adapter whose `populate_computed` records its inputs.
2. Assert the callback fired once, received only survivors, and `needed` matched exactly the COMPUTED names referenced by remaining active rules.
3. Halt a candidate at a DERIVED gate; assert it is absent from the survivor set passed to the adapter.
4. `grep` confirms no COMPUTED computation logic lives in the engine package.
5. Run the suite; OTA-696 scan passes.

## Commit instruction
"I have been instructed to commit. Do you approve? (yes / no)"

## Coordination footer
OK to continue to OTA-703.md

## Commit message template (if committing)
```
OTA-702 feat: COMPUTED-value adapter callback between DERIVED and COMPUTED gates
```
