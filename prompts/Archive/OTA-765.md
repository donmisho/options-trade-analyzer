---
ticket: OTA-765
title: Wire directional comparison through the engine
feature: OTA-757 — App-side wiring
epic: OTA-679 — Insight Engine
allowedTools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash(cat:*)
  - Bash(grep:*)
  - Bash(rg:*)
  - Bash(find:*)
  - Bash(git ls-files:*)
  - Bash(python:*)
  - Bash(pytest:*)
  - Bash(git status:*)
  - Bash(git diff:*)
  - Bash(git add:*)
---

# OTA-765 — Wire directional comparison through the engine

## Terminal context
- This terminal: **Terminal A — Insight Engine app-wiring (Phase 2 end-state)**
- Concurrent terminals: **none** — single-terminal sequential batch: 759 → 763 → 764 → **765** (last in batch).
- Cross-terminal dependencies:
  - **Upstream (must be DEPLOYED — Phase 0 hard-gate):** F12 = the **OTA-752 family** — directional input adapter (`produce_candidates`) + directional rule library + directional strategies seeded in `engine_*`. If F12 is not live, this story **cannot run** (see Phase 0).
  - **Downstream:** landing this story unblocks **OTA-756** (retire `directional_engine.py`) — authored as a separate prompt, not part of this batch.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md                   # directional thesis scoring inputs
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md     # S5.8
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: `insight_engine.md` §1 — directional comparison is its own consumer.**
Directional thesis comparison is one of OTA's evaluation surfaces — its own adapter, strategies, and verdict bands, running the **same** engine on the single path. No separate evaluation path may remain in the route.

**Source: ticket OTA-765 — route flow.**
The directional-comparison route calls `engine.evaluate(..., source_app_id="OTA")` against the `directional` adapter. The `fitness_score` field that `directional_engine.py` produced is replaced by the engine's verdict + weighted score on the response.

**Source: `insight_engine.md` §8 — boundaries.**
The route shapes the response from the engine's result record; it does not compute a fitness score or any directional value itself.

**Retirement linkage.** `directional_engine.py` retirement (OTA-756 / plan S12.4) is unblocked once **no caller remains**. This story removes the route's caller; Phase 0 reports any other callers so OTA-756 can be scoped accurately.

## Phase 0 — Read-only discovery (HARD STOP / possible NO-GO)
Read-only. Make **no edits**. Report:
1. **F12 deployment check (gating):** does the `directional` input adapter (`produce_candidates`) exist and load, and are the directional rules + strategies seeded and validating in `engine_*`? **If F12 is not deployed, this is a NO-GO — report and STOP. Do not edit.**
2. The current directional-comparison route and its `directional_engine.py` call site.
3. All consumers of `fitness_score` (route response, frontend, anything downstream) so the field swap is complete.
4. Every remaining caller of `directional_engine.py` across the codebase (to scope OTA-756).
5. The `engine.evaluate(...)` signature and the verdict + weighted-score fields on the result record.

**Then STOP and present a GO / NO-GO.** Do not edit until Don issues GO.

## Scope
Rewire the directional-comparison route to call `engine.evaluate(..., source_app_id="OTA")` against the `directional` adapter. Replace `fitness_score` on the response with the engine verdict + weighted score. Remove the `directional_engine.py` evaluation path from the route.

## Acceptance criteria
- The directional-comparison route calls `engine.evaluate(..., source_app_id="OTA")` against the `directional` adapter — **no `directional_engine.py` evaluation path remains in the route.**
- `fitness_score` is superseded by the engine verdict + weighted score on the response.
- `directional_engine.py` retirement (OTA-756) is unblocked — Phase 0's caller report confirms whether the route was the last caller.

## Out of scope
- Deleting `directional_engine.py` (OTA-756, gated on this story).
- Screening routes (OTA-759); position health (OTA-764).
- Engine internals, the directional adapter/rules (OTA-752 family), sink mechanics.

## Verification steps
- `grep -rn` the directional route: no `directional_engine.py` evaluation call; no inline fitness/score math.
- Run the route against a directional fixture through the engine; confirm verdict + weighted score replace `fitness_score` on the response and any `fitness_score` consumer is updated.
- Report residual `directional_engine.py` callers (for OTA-756).
- **QA Level 2 (Regression)** per CLAUDE.md — eval-path change. Document the level in the commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency in this batch. *(Landing this unblocks OTA-756 — `directional_engine.py` retirement — which is authored as a separate prompt.)*

## Commit message template (if committing)
```
OTA-765 feat: wire directional comparison through the engine (directional adapter; verdict + weighted score replaces fitness_score)
```
