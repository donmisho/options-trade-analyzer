---
allowed-tools: Read, Grep, Glob, Edit, Bash(git status:*), Bash(git diff:*), Bash(python:*), Bash(pytest:*), Bash(Get-ChildItem:*), Bash(Select-String:*)
---

# OTA-780 — Move inline evaluation_routes.py gates to engine

Part of Feature OTA-774 — Domain coupling in engine machinery · Epic OTA-679. Third file in the batch. Canonical ticket for the inline-gate migration; **supersedes OTA-732** (now Cancelled).

## Terminal context
- This terminal: Terminal A (engine-decoupling — Feature OTA-774)
- Concurrent terminals: none
- Cross-terminal dependencies: none. Runs after OTA-778 has committed. Next in this terminal: OTA-779 (which also touches `evaluation_routes.py`, so these must stay sequential). The new gates registered here must use the **action codes** introduced in OTA-776 — do not author them against the old `PASS`/`WAIT_FOR_EARNINGS` strings.

## Phase 0 — READ-ONLY, hard STOP
Make no edits. Report, then STOP for go/no-go.

1. **Inline gates.** Report the current inline gate logic at `evaluation_routes.py:626-663` (line numbers may have drifted — locate by content): the **DTE ≤ 7 hard filter**, the **DTE 8–13 penalty**, and the **credit/debit quality gate**.
2. **Registration mechanism.** Confirm the F4 screening rule library (OTA-725) registration mechanism exists (`app/options_rules/screening/`, the `formula:<name>` registry hook) and report how a gate/formula is registered.
3. **Registry contract.** Confirm the SHARED `formula_registry` lookup set exists (`('SHARED','formula_registry')` per OTA-689) and that OTA-699 load-time validation runs against both the lookup set and the live registered library. Report the names already present.
4. **BRONZE FLAG (required).** Determine whether relocating these gates touches the persistence / bronze write path (a registered gate emits an `EvaluationDecision` record through the sink). If it does, hold to the **as-built reality** in the Relevant Context below — a **single** `dbo.bronze_evaluations` table discriminated by `record_type`. If anything you read in `insight_engine.md` §4.3 (its "two-table Phase-1 schema" language) appears to require two physical tables, **flag the conflict explicitly in this report and STOP** — do not code to the two-table description.

**STOP.** Wait for go/no-go.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
cat claude_context/insight_engine-schema-ddl.md
```

## Relevant Context — Do Not Deviate Without Escalation

- **Source: OTA-780; migration plan S7.6 (audit §5b #5, §5c #8).** Move the three inline gates at `evaluation_routes.py:626-663` into registered engine gates per F4. The route is left invoking the engine; **no gate logic remains inline.**
- **Source: OTA-725 (Screening rule library); OTA-689; OTA-699.** Registered gate/formula names must appear in the SHARED `formula_registry` contract (OTA-689). OTA-699 validates membership at load against both the lookup set and the live registry, and treats drift between them as its own failure mode. Each formula is a pure `(named_values, params) -> float in [0, 100]`; thresholds (the DTE cutoffs, the penalty sizes, the credit/debit quality bar) come from **junction rows**, never literals in formula code.
- **Source: OTA-776 (this batch).** The migrated gates return the generic action codes `BLOCK` / `DEFER` / `MODIFY` / `OK`; the DTE ≤ 7 hard filter is a `BLOCK` (stopping) gate. Do not emit domain verdict strings from the gate.
- **BRONZE — as-built schema reality. Source: `insight_engine-schema-ddl.md` §3.** The OTA sink lands **both** engine record streams into a **single** physical table, `dbo.bronze_evaluations`, discriminated by `record_type` (`'SNAPSHOT' | 'DECISION'`); plus the `bronze_payload_contract` registry. There are **not** two physical bronze tables.
- **BRONZE — reconciliation. Source: `insight_engine.md` §4.3 + OTA-695 scope anchor.** The engine still emits **two logical streams** (`CandidateSnapshot`, `EvaluationDecision`) through a two-method sink (`write_snapshots`, `write_decisions`); collapsing them into one physical table is the **sink's** job (app-side), not the gate's. `insight_engine.md` §4.3 / its v3 change-log still describe a "two-table Phase-1 schema" — that wording is the known doc-vs-as-built discrepancy. If the gate migration forces you to write a bronze record directly (it generally should **not** — the engine drives persistence, gates just return results), escalate per the Phase-0 BRONZE FLAG rather than reconciling it yourself.
- **Source: CLAUDE.md (Governance + House Style).** No SoT-doc edits. Commit only on instruction. `OTA-NNN feat: …`. PowerShell. Provider routing never hardcoded.

## Scope
- Remove the DTE ≤ 7 hard filter, the DTE 8–13 penalty, and the credit/debit quality gate from `evaluation_routes.py`.
- Re-create each as a registered engine gate/formula in the screening rule library (OTA-725 mechanism), thresholds parameterised from junction rows.
- Register the names in the `formula_registry` contract (OTA-689) so OTA-699 passes at load.
- Leave the route invoking the engine; no inline gate logic remains.

## Acceptance criteria
- The three gates are removed from `evaluation_routes.py` and exist as registered engine gates.
- Registered names are present in the `formula_registry` contract (OTA-689) and pass OTA-699 load-time validation.
- `evaluation_routes.py:626-663` (by content) contains no inline gate logic.
- Grep confirms no inline gate evaluation remains in the route.

## Out of scope
No new gates, no threshold-value changes (carry the existing cutoffs into junction rows as-is), no verdict-band changes, no bronze schema changes. The bronze write path is the sink's; do not modify it here.

## Verification steps
1. `Select-String -Path app/api/.../evaluation_routes.py -Pattern 'dte|credit_pct|debit_pct|quality'` around the former 626-663 region → no inline gate logic; route only invokes the engine.
2. Confirm the three registered names resolve via `formula:<name>` and appear in the `('SHARED','formula_registry')` set.
3. Start the engine; confirm OTA-699 load validation passes (registry membership + lookup-set parity).
4. **QA Level 2 (regression), escalate to Level 3 if verdicts shift.** Run the backend suite, then the AMZN regression suite and the MSFT anchor regression endpoint (OTA-284). The migrated gates must produce the **same** screening outcomes (same candidates filtered by DTE ≤ 7, same DTE 8–13 penalty, same credit/debit gate decisions) as the inline versions. If any verdict band shifts, escalate to Level 3 and add a scoring-narrative consistency check on three symbols. Note the Level in the commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-779.md

## Commit message template
OTA-780 feat: move inline evaluation_routes.py DTE and credit/debit gates into registered engine gates
