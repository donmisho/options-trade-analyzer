# OTA-790 — Apply / Reset and reload semantics

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none
- Cross-terminal dependencies: draft substrate (OTA-791), CRUD (OTA-782), validation (OTA-783), and the four editors (OTA-785–788) committed

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/UI-GUIDANCE.md
cat claude_context/insight_engine.md
cat claude_context/strategy-rules-prototype-v2.html
```

## Phase 0 — Read-only investigation (hard go/no-go STOP)

Do NOT edit in Phase 0. Investigate and report:

1. The draft substrate from OTA-791 (the `<key>__draft` row and how it's created/updated).
2. Where `config_version` lives and how it is bumped (schema + any existing bump logic).
3. The prototype's "Apply" and "Reset to defaults" controls.
4. The seeded-defaults source for Reset (the OTA-680 seed importer / `Scoring Parameters.xlsx` seed) — confirm how a strategy's seeded defaults are recovered without re-importing the whole workbook.
5. Confirm no existing code performs an in-place hot-reload of the running engine config — Apply must change what the engine loads **next start**, not the running config (§6.5).

Report findings + go/no-go.

## Relevant Context — Do Not Deviate Without Escalation

Source: OTA-781 design decision + `insight_engine.md` §6.5 (restart-only)
- **Apply** promotes the draft strategy row onto the live strategy key, bumps `config_version`, and surfaces a pending-restart banner — table changes take effect on the **next engine start**. There is no general hot-reload; Apply changes what the engine *will* load, not what the running engine is using.

Source: ticket AC
- Apply promotes draft → live on the strategy key, bumps `config_version`, and persists through CRUD (OTA-782) **only after** validation (OTA-783) passes.
- A pending-restart banner appears after Apply, stating the change is effective on the next engine start (§6.5).
- **Reset** reverts the strategy to its seeded defaults.
- No code path performs an in-place hot-reload of the running engine config.

Source: `insight_engine.md` §6.2 (tables as runtime source)
- Promotion is a table write (draft → live key); the engine reads the tables at next start. Config never comes from anywhere but the tables.

Source: `UI-GUIDANCE.md` Part 3a
- The pending-restart banner is a callout — `var(--bg2)` is permitted on inset/callout boxes (not on full-width bands/section backgrounds). Tokens only.

## Scope
Implement Apply (promote draft→live on the strategy key + `config_version` bump + pending-restart banner, gated on validation) and Reset (revert strategy to seeded defaults). Enforce restart-only: no hot-reload path.

## Acceptance criteria
- Apply promotes draft → live, bumps `config_version`, and persists via CRUD (OTA-782) only after validation (OTA-783) passes.
- A pending-restart banner appears after Apply, stating the change is effective on next engine start.
- Reset reverts the strategy to its seeded defaults.
- No code path hot-reloads the running engine config (verified).

## Out of scope
- The draft/preview substrate (OTA-791), the editors (OTA-785–788), the audit trail (OTA-792 — next).
- Adding hot-reload.

## Verification steps
- Manual: edit → Apply → confirm live row updated, `config_version` bumped, banner shown; running engine config unchanged until restart.
- Manual: Reset → strategy returns to seeded defaults.
- Negative: Apply with an invalid draft is blocked by validation; live row untouched.
- Grep/confirm no hot-reload path was introduced.
- **QA level: high.** Promotion + `config_version` + the restart-only invariant are the operationally risky bits.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-792.md

## Commit message template (if committing)
OTA-790 feat: Apply (draft→live + config_version + restart banner) and Reset to defaults
