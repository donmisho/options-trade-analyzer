---
allowedTools: [Read, Grep, Glob, Write, Edit, Bash]
---

# OTA-705 — Persistence sink interface + in-memory sink

## Terminal context
- This terminal: Terminal B (engine-core build)
- Concurrent terminals: Terminal A (MCP Server build, OTA-605 → OTA-609)
- Cross-terminal dependencies: none. In-package sink interface + in-memory implementation; the orchestrator drives the sink after each run. No edit to any shared file. If wiring into a shared file appears necessary, **HARD STOP and report**.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/insight_engine.md
```

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine.md` §4.3 (pluggable sink owns write mechanics)
Rule: The engine depends on a sink **interface**, never on a database. Interface:
```
sink.write_snapshots(records: list[CandidateSnapshot]) -> None
sink.write_decisions(records: list[EvaluationDecision]) -> None
```
The consuming application injects a concrete sink at startup; the engine drives it at the end of each run.

Source: `insight_engine.md` §8 (boundaries)
Rule: The engine is **not** responsible for the sink's mechanics — connection, credentials, transactions, retry, the physical store. **No DB import anywhere in the engine package.** The concrete Azure SQL sink that lands records into `bronze_evaluations` is **app-side wiring (Wave 4)** and out of scope here.

## Scope
Define the `PersistenceSink` interface and an in-memory implementation; have the orchestrator drive the injected sink after each run.

### Phase 0 — Discovery (READ ONLY, no edits)
1. Read OTA-704 (the two record streams) and OTA-701 (where a run ends, to drive the sink) and the `ConfigSource` injection pattern from OTA-698 (mirror it).
2. Confirm no DB import exists in-package.
3. Report findings. If a sink already exists from a precursor, **HARD STOP and report**.

### Phase 1 — Implementation
1. Define `PersistenceSink` (protocol/ABC) with `write_snapshots` and `write_decisions`.
2. Implement an in-memory sink that captures both streams for assertion in tests.
3. Have the engine accept an injected sink and drive it after each run; no connection/transaction/retry logic anywhere in engine code.

## Acceptance criteria
- `PersistenceSink` interface defined with `write_snapshots` and `write_decisions`.
- The engine accepts an injected sink and drives it after each run; no DB import anywhere in the engine package.
- An in-memory sink captures both streams for assertion in tests.
- No connection/transaction/retry logic in engine code (those are sink concerns per §8).
- OTA-696 guard/CI scan still passes.

## Out of scope
- The concrete Azure SQL sink (app-side, Wave 4) and the bronze DDL (OTA-708).
- `source_app_id` enforcement (OTA-706) and integration tests (OTA-707).
- Any options-domain term or rule value; any edit to a shared file.

## Verification steps
1. Inject the in-memory sink; run a fixture evaluation; assert both `write_snapshots` and `write_decisions` were called with the expected records.
2. `grep -rniE "pyodbc|sqlalchemy|import azure|connect\(" app/insight_engine/` returns nothing.
3. Confirm the engine raises/behaves correctly if no sink is injected (per the chosen contract — required injection).
4. Run the suite; OTA-696 scan passes.

## Commit instruction
"I have been instructed to commit. Do you approve? (yes / no)"

## Coordination footer
OK to continue to OTA-706.md

## Commit message template (if committing)
```
OTA-705 feat: PersistenceSink interface + in-memory sink (engine drives injected sink, no DB import)
```
