# OTA-686 — Fill in the TBD scoring formulas (capture definitions + proxy flags)

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none assumed — confirm with Don.
- Cross-terminal dependencies: requires OTA-681 (tables) + OTA-682 (importer). Produces `engine_rules`
  `formula_ref` / `condition_expression` + `parameter_schema`; feeds the OTA-689 formula registry.

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md                 # § 6.3 expression library, § 6.6 formula-registry validation
cat claude_context/insight_engine-schema-ddl.md
# Capture source (temporary): requirements/Scoring Parameters.xlsx (Sheet1 — Scoring Criteria marked TBD)
# Current code formulas / proxy status: requirements/insights-engine-audit-2026-05-20.md  § 4
```

## Relevant Context — Do Not Deviate Without Escalation

Source: Don, project direction (operating principle)
We are **not defining optimal formulas now** — we are capturing each formula so the engine can read and
process it. Where the code already has a formula (even a proxy), capture that as the definition. Where it's
a proxy, capture it AND flag the planned replacement in `intent`/`rationale`. Optimal-formula tuning happens
later via the rule-management screen. The win condition is: every scoring criterion has a captured,
processable definition and a registered `formula:<name>`.

Source: audit `insights-engine-audit-2026-05-20.md` § 4 — capture these (code formula = the captured
definition unless noted):
- **Credit Width %** (WG): `abs(net_debit) / spread_width * 100` — capture as canonical.
- **Liquidity** (WG): sum of both legs' volume + OI — capture as canonical.
- **Delta Quality** (TR): gaussian-like peak around target delta range — capture as canonical.
- **Payout Ratio** (LT): `(delta * price * 0.10 * 100) / premium_dollars` — capture as canonical.
- **Delta OTM Score** (LT): `1.0 - delta / 0.25` — capture as canonical.
- **Bid Ask Tightness** (LT): `1.0 - ba_pct / 100.0` — capture as canonical.
- **Theta Gamma Ratio** (WG): proxy = `abs(net_theta) / max_loss` — **NO gamma involved.** Capture the proxy
  and FLAG: true version needs per-leg gamma propagated to the candidate (options-chain adapter, later).
- **SMA Alignment Score** (TR): currently a `0.5` passthrough. Capture as a proxy and FLAG: needs a
  classification(BULLISH/BEARISH/MIXED/NEUTRAL)→[0,1] formula (define during tuning).
- **IV Percentile Cost** (TR): proxy = `1.0 - iv_decimal`. Capture and FLAG: true IV percentile unavailable
  (needs historical-IV producer, later).
- **Runway Score** (TR): raw `theta_runway_days`. Capture and FLAG: needs [0,100] normalization (tuning).
- **Open Interest** (LT): raw `open_interest`. Capture and FLAG: needs [0,100] normalization (tuning).

Source: `insight_engine.md` § 6.3 / scoring convention
Scoring formulas resolve to a value in `[0, 100]`. Several captured proxies currently emit other scales
(raw days, raw OI, 0–1 ratios). Capture the current formula AND note in `intent` where normalization to
[0,100] is still owed — do not silently "fix" the scale here (that's tuning).

Source: OTA-689 boundary
Every `formula:<name>` you introduce must be registerable — OTA-689 scans `engine_rules.formula_ref` to
build the registry contract. Use stable, descriptive `formula:` names.

## Scope
For each TBD scoring criterion, record a captured definition in `engine_rules` (a `formula_ref` and/or
`condition_expression`, with `parameter_schema` where parameterised), marking proxies and their planned
replacement in `intent`/`rationale`.

## Acceptance criteria
- All TBD scoring criteria from sheet/§ 4 have a captured definition (formula ref or expression).
- The six listed as canonical are captured from the code formula; the five proxies are captured AND flagged
  with their planned replacement.
- Where a formula's current output is not yet on the [0,100] scale, the normalization owed is noted in
  `intent` (not silently changed).
- Every introduced `formula:<name>` is stable/descriptive and discoverable by OTA-689's scan.

## Out of scope
- Defining optimal/final formulas (tuning, later, via the rule-management screen).
- Building producers for missing inputs — gamma propagation, true IV percentile (adapter, later).
- Implementing the formulas in code (later screening rule library feature) — this story records the
  definitions/contract only.

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
```
- `SELECT rule_key, formula_ref, condition_expression, intent FROM dbo.engine_rules` for the scoring
  criteria — every TBD criterion has a captured definition; proxies carry a replacement flag.
- Confirm each `formula:<name>` is present and stable; confirm normalization-owed notes exist where relevant.

Post-Build QA Gate: Level 1 (rule definitions / contract data). Note in commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-689.md  *(OTA-689 scans the formula_ref values this story seeds to build the registry contract).*

## Commit message template (if committing)
```
OTA-686 feat: capture TBD scoring formula definitions + proxy flags

Captures code formulas as definitions (Credit Width %, Liquidity, Delta Quality,
Payout Ratio, Delta OTM, Bid Ask Tightness); flags proxies + planned replacements
(Theta Gamma Ratio/no gamma, SMA Alignment passthrough, IV Percentile, Runway/OI
normalization). Optimal-formula tuning deferred. QA Level 1.
```
