# OTA-810 — Named formula registry as SKILL.md

## Terminal context
- This terminal: Terminal A (single stream)
- Concurrent terminals: OTA-807 / OTA-808 / OTA-809 may run in parallel — disjoint files.
- Cross-terminal dependencies: none. (Reads the live rule library + the SHARED `formula_registry` lookup set; does not modify them.)

## Required reading
Before any edits:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
```
Also:
- Locate and read `insight_engine.md` — §6.3 (`formula:<name>`), §6.6 (startup validation).
- Read `skill_loader.py` to confirm the canonical `app/skills/{skill_name}/SKILL.md` path and SKILL.md format the loader expects.
- Identify the live registered rule library and the SHARED `formula_registry` lookup set (`('SHARED','formula_registry')`) that OTA-699 validates the runtime registry against.

## Relevant Context — Do Not Deviate Without Escalation

**Source: OTA-810 scope.** Produce a companion **SKILL.md** documenting every `formula:<name>` the options consumer registers. For each formula: its name, signature `(named_values, params) -> float in [0, 100]`, and required inputs.

**Source: `insight_engine.md` §6.3 / §6.6 + OTA-699.** This SKILL.md is the **human-readable companion** to the runtime `('SHARED','formula_registry')` lookup set. The documented set MUST match the registered rule library — the same set OTA-699 validates at load. The SKILL.md is documentation; the lookup set is the runtime contract. Keep them identical.

**Source: `skill_loader.py` convention.** The file lives at the canonical `app/skills/{skill_name}/SKILL.md` path and follows the loader's expected structure. Do not hardcode it elsewhere.

**Source: do-not-invent rule.** Document only the formulas that are actually registered. If the registered set and the `formula_registry` lookup set disagree, that is a finding — STOP and report it; do not paper over it by documenting an idealized set.

## Scope

### Phase 0 — Discovery (READ-ONLY, then STOP)
No edits. Enumerate and report:
1. Every `formula:<name>` actually registered by the options rule library, with its signature and required inputs.
2. The contents of the `('SHARED','formula_registry')` lookup set.
3. Any mismatch between (1) and (2) — these must agree (OTA-699 enforces it at load).
4. The exact SKILL.md path/name per `skill_loader.py`.

**STOP and report the formula list + the path. Proceed on approval.**

### Phase 1 — Author (after approval)
Write the SKILL.md at the canonical path listing each registered formula with signature and inputs, matching the validated set exactly. Include a dated header.

## Acceptance criteria
- A SKILL.md exists listing every `formula:<name>` the options consumer registers, each with signature `(named_values, params) -> float in [0, 100]` and required inputs.
- The documented set matches the registered rule library — the same set OTA-699 validates at load.
- Lives at the canonical `app/skills/{skill_name}/SKILL.md` path per `skill_loader.py`.

## Out of scope
- No changes to the rule library, the registry, or the lookup set.
- Do not add, remove, or rename any formula — documentation only.

## Verification steps
- The SKILL.md formula set equals the Phase 0 registered set (1:1, no extras, no omissions).
- The set equals the `('SHARED','formula_registry')` lookup contents (the OTA-699 source).
- Path matches `skill_loader.py`'s convention; loader can resolve it.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency.

## Commit message template (if committing)
```
OTA-810 docs: add named formula registry SKILL.md companion to the formula_registry contract
```
