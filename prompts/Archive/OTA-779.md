---
allowed-tools: Read, Grep, Glob, Edit, Bash(git status:*), Bash(git diff:*), Bash(python:*), Bash(pytest:*), Bash(Get-ChildItem:*), Bash(Select-String:*)
---

# OTA-779 — Decompose strategy_scorer.py (file disappears)

Part of Feature OTA-774 — Domain coupling in engine machinery · Epic OTA-679. **Capstone — last file in the batch.** `app/analysis/strategy_scorer.py` is deleted; its contents split three ways.

## Terminal context
- This terminal: Terminal A (engine-decoupling — Feature OTA-774)
- Concurrent terminals: none
- Cross-terminal dependencies: none. Runs **last**, after OTA-775-776-777, OTA-778, and OTA-780 have all committed. This is the only file in the batch that deletes a module and consolidates engine-core normalization — it is gated on the dual-scorer / strategy-identity branching already being gone.

## Phase 0 — READ-ONLY, hard STOP (capstone gate)
Make no edits. Report, then STOP for go/no-go. This gate is load-bearing — deleting the file prematurely breaks scoring.

1. **OTA-767 / OTA-768 landed.** Confirm the dual-scorer / strategy-identity branching has been removed (OTA-767 / OTA-768, under OTA-766). Verify the **code condition**, not just ticket status: there is no remaining strategy-identity branching or dual-scorer path that `strategy_scorer.py` deletion depends on being gone. Report what you find; if branching remains, STOP.
2. **OTA-778 landed.** Confirm `StrategyDefinition` has been split (StrategyConfig / OptionsStrategyParams) so the scorer's strategy-config reads have a clean target.
3. **Three destinations ready.** For each chunk of `strategy_scorer.py`, confirm the destination exists and report the target:
   - **Orchestration** → app-side route changes (OTA-757 — App-side wiring / F5).
   - **Per-rule scoring math** → registered formulas in the screening rule library (OTA-725 — Screening rule library / F4).
   - **Normalization** → the single canonical engine-core normalization function (F1 / OTA-695).
4. **Callers.** Grep repo-wide for every importer/caller of `strategy_scorer` and report them — all must be repointed before deletion.

**STOP.** Wait for go/no-go.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
```

## Relevant Context — Do Not Deviate Without Escalation

- **Source: OTA-779; migration plan S7.5 (audit §5c #7, §7.5 #3).** `strategy_scorer.py` disappears. Its contents split: orchestration → app-side routes; per-rule scoring math → registered formulas; normalization → engine core, the single canonical implementation.
- **Source: linked tickets (Relates).** Split destinations are real, existing features: **OTA-725 — Screening rule library** (per-rule scoring math) and **OTA-757 — App-side wiring** (orchestration). OTA-779 is linked `Relates` to both. Land each chunk in the corresponding feature's surface; do not invent a new home.
- **Source: F1 / OTA-695.** Normalization uses the **single** engine-core canonical implementation. Do not leave a second `normalize()` behind. This is also the target of the not-yet-ticketed three-way `normalize()` consolidation (audit §7.5 #3) — by routing through the engine-core function you satisfy that consolidation for the scorer's copy.
- **Source: CLAUDE.md (Governance + House Style + shared files).** No SoT-doc edits. `app/main.py`, `app/database.py`, `web/src/api/client.js` are project-critical shared files — this work shouldn't touch them, but if it must, flag it. Commit only on instruction. `OTA-NNN feat: …`. PowerShell.

## Scope
- Move orchestration logic into the app-side route(s) (OTA-757 surface).
- Move per-rule scoring math into registered formulas (OTA-725 surface).
- Repoint normalization to the engine-core canonical implementation.
- Delete `app/analysis/strategy_scorer.py`. No module imports it afterward.

## Acceptance criteria
- `strategy_scorer.py` is deleted; no module imports it.
- Orchestration lives in app-side routes (OTA-757); per-rule math is registered as engine formulas (OTA-725); normalization uses the single engine-core canonical implementation.
- Grep confirms no remaining references to `strategy_scorer`.

## Soft overlap (no link — target not ticketed)
The normalization consolidation overlaps the **not-yet-ticketed** three-way `normalize()` cleanup (audit §7.5 #3; formerly plan "S8.3" — `_normalize()` in `strategy_scorer.py`, `normalize()` in `vertical_engine.py`, `normalize()` in `long_call_engine.py`). No OTA ticket exists for the full consolidation. **Do not block on it.** This story removes the `strategy_scorer.py` copy by routing through engine-core; note in the commit body that the `vertical_engine.py` / `long_call_engine.py` copies remain for the future cleanup.

## Out of scope
No new scoring math, no threshold/weight changes, no behavior change to verdicts. The COMPONENT_DISPLAY_NAMES / ATR(14)-TODO items in this file (audit §7.5 #4, #6) are separate stories — leave them unless their removal is required to delete the file cleanly, in which case flag it.

## Verification steps
1. `Get-ChildItem app/analysis/strategy_scorer.py` → not found (file deleted).
2. Repo-wide Grep for `strategy_scorer` → zero hits.
3. Confirm only one engine-core `normalize()` is on the scoring path for the migrated math.
4. **QA Level 3 (full regression).** Scoring-engine surface changed: run the backend suite + the AMZN regression + the MSFT anchor regression (OTA-284) + a manual scoring-narrative consistency check on three symbols. Scores and verdicts must match pre-deletion within tolerance. Note the Level in the commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency (last file in the OTA-774 batch).

## Commit message template
OTA-779 feat: decompose and delete strategy_scorer.py — orchestration to routes, math to formulas, normalization to engine core
