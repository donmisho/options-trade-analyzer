# OTA-827 — Parameters tab (core scalar params editor)

## Terminal context
- This terminal: **Terminal A** (frontend editor chain).
- Concurrent terminals: Terminal B may be running OTA-761 (backend) and/or OTA-829 (test harness) — both disjoint.
- Cross-terminal dependencies: none with B. **Within Terminal A this is the first story of the chain (`827 → 790 → 828`)** — all three touch `web/src/pages/StrategyAdminPage.jsx` and reuse `web/src/api/client.js` methods (project-critical shared files; never edited by two terminals at once). Run on a clean tree.
- **Hard prerequisite:** OTA-785 (Hard Gates) must be landed — this story reuses the shared param-editor subcomponent and the `client.js` junction/catalog methods OTA-785 created. Do not re-implement them.

## Required reading
Before any code changes:

    cat claude_context/CLAUDE.md
    cat claude_context/UI-GUIDANCE.md
    cat claude_context/insight_engine.md          # §3.6 (gate phase / junction params), §6.1 (junction + strategy columns)

## Relevant Context — Do Not Deviate Without Escalation

Source: UI-GUIDANCE.md (prototype is the layout contract)
Rule: `strategy-rules-prototype-v2.html` is the authoritative layout for the Parameters section (`.params-grid` / `.param-card`). Match the mockup's stacked structure; do not invent a different layout.

Source: insight_engine.md § 2.1 (tables as source of truth) + § 3.4 / § 6.1 (strategy + junction columns)
Rule: There is **one** source of truth per datum. A strategy's header-level scalars (e.g. DTE range) live on the strategy row; per-rule thresholds (e.g. short-delta max, IV-rank minimum) live in the `parameters` JSON of specific gate **junction** rows. Do not create a third "parameters" concept that duplicates either. Editing the same junction param from two surfaces must write the same row — no divergent second write path.

Source: OTA-784 (editor shell) + OTA-827 ticket (data-model resolution)
Fact: **DTE range is already a header field** rendered/edited on the header card (OTA-784). The Parameters section must NOT add a second DTE control — either omit DTE here or bind to the exact same header field (single source of truth).

Source: OTA-785 (shared subcomponent) + OTA-791 (draft substrate)
Fact: OTA-785 built the shared `JunctionRowEditor` / `ParameterEditor` / `defaultsFromSchema` (opt-in props: `showStopIfFail` / `showWeight` / `weightAsPercent` / `showScorePenalty` / `showEvaluationOrder`) and the `client.js` junction/catalog methods (`getStrategyJunctions`, `getRulesAdmin`, `createJunction`, `updateJunction`, `deleteJunction`). Reuse them. All writes route against the draft key via the established save-to-draft path (`createOrResumeDraft` clones live header+junctions, then writes target `<key>__draft`) — live is untouched until Apply (OTA-790).

Source: OTA-790 draft-validation model
Invariant: per-strategy invariants are NOT enforced at save against the draft; any client indicator here is **advisory** (flag, don't block). The authoritative catch is the preview path / Apply. Do not word any behavior as "rejected at save."

Source: CLAUDE.md § House Style
Rule: CSS `var(--*)` tokens only (no inline hex); no `$` prefix; scores `##.00`; buttons content-sized; `var(--bg2)` only on filter bars / QuoteBar / pill badges.

## Phase 0 — Read-only investigation (HARD STOP — no edits)

Do not modify any file. Re-verify every claim against the live tree.

Investigate and report:
1. **Enumerate the cards.** In `strategy-rules-prototype-v2.html`, list every card the Parameters section (`.params-grid` / `.param-card`) shows. Report the full set.
2. **THE hard data-model map.** For EACH card, determine its backing store and report an explicit table — `card → {header field on strategy row | gate-junction `parameters` key on rule X} → write path (strategy CRUD OTA-782 | junction CRUD)`. Resolve the two known cases: **DTE = header field (do not duplicate)**; **short-delta / IV-rank = gate-junction `parameters`**. For any card you cannot confidently map, STOP and surface it — do not guess a store.
3. **No-divergence check.** For any junction param that is ALSO editable in the OTA-785 Hard Gates tab, confirm the Parameters card writes the *same* junction row (a curated shortcut view), not a parallel write path that could diverge from Hard Gates. Report how you'll guarantee single-write.
4. **Reuse surface.** Locate the OTA-785 shared subcomponent and `client.js` methods; report their exact prop/method signatures so this story reuses them with no second implementation.
5. **Slot + draft wiring.** Find the Parameters `SectionScaffold` placeholder slot in `StrategyAdminPage.jsx`; report the slot and the story-tag to set. Confirm the draft-write path (`createOrResumeDraft` → draft key) is the same one the other sections use.
6. **DTE binding.** Confirm exactly how the header DTE field is stored/edited (OTA-784) so this section either omits it or binds to the identical field.

**End Phase 0 with an explicit GO / NO-GO recommendation and STOP.** Lead with the full card→backing-store map (the gate for this story). Wait for Don's approval before any edit.

## Scope
After GO: build the Parameters section per the prototype — the curated core scalar params as `.param-card`s — each card wired to its correct backing store per the Phase 0 map, writing against the draft key. Reuse OTA-785's shared param-editor and `client.js` methods. No duplicated DTE control; no second write path for any junction param shared with Hard Gates.

## Acceptance criteria
- The Parameters section renders the strategy's core scalar params as cards matching the mockup's `.params-grid` / `.param-card` layout.
- Each card writes to its correct backing store per the Phase 0 map — header field via the strategy CRUD (OTA-782) or gate-junction `parameters` via the junction CRUD — against the **draft key**. No duplicated DTE control; no third "parameters" store.
- Any junction param shared with the Hard Gates tab writes the same junction row (single source of truth); no divergent second write path.
- Reuses OTA-785's shared param-editor subcomponent and `client.js` junction methods — no second implementation.
- Saves route through the CRUD API (OTA-782) and validation (OTA-783); any client indicator is advisory (flag, not block).
- The shell `SECTIONS` slot story-tag for this section is set to OTA-827.
- House style holds (CSS `var(--*)`, no inline hex, no `$`, scores `##.00`, buttons content-sized).

## Out of scope
- Apply / Reset / pending-restart banner — OTA-790.
- Other sections (Hard Gates / Scoring / Adjustments / Verdict Bands) — OTA-785/786/787/788.
- Catalog browser UI — OTA-789.
- Any backend change (the CRUD + junction-read endpoints already exist via OTA-782 / OTA-826).
- Selector draft filtering — OTA-828.

## Verification steps (PowerShell) — QA Level 1 (single-section UI change)
    cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
    .\venv\Scripts\Activate.ps1

    # 1) Build the frontend
    cd web; npm run build; cd ..

    # 2) Confirm no second param-editor implementation and no duplicated client method
    Select-String -Path web\src\pages\StrategyAdminPage.jsx,web\src\components\*.jsx -Pattern "ParameterEditor|JunctionRowEditor"
    #   -> Parameters section imports/reuses the OTA-785 component; no new copy

    # 3) Confirm DTE single source (no second DTE control in the Parameters section)
    Select-String -Path web\src\pages\StrategyAdminPage.jsx -Pattern "DTE|dte"
    #   -> review: DTE edits in one place (header), not duplicated in Parameters

    # 4) Manual: run dev, open /strategy-admin, select a strategy, edit a Parameters
    #    card, confirm it writes to the draft (live unchanged), and that a param also
    #    shown in Hard Gates reflects the same value (single source of truth).

## Commit instruction
"I have been instructed to commit. Do you approve? (yes / no)"

## Coordination footer
OK to continue to OTA-790.md (next in Terminal A chain).

## Commit message template (if committing)
OTA-827 feat: strategy-editor Parameters section (core scalar params, draft-write, reuses OTA-785 editor) (QA L1)
