# OTA-787 — Adjustments tab

## Terminal context
- This terminal: Terminal A (or a parallel terminal — see OTA-791 parallelization note)
- Concurrent terminals: possibly OTA-785 / 786 / 788
- Cross-terminal dependencies: draft substrate (OTA-791), CRUD (OTA-782), validation (OTA-783), catalog (OTA-789) committed

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/UI-GUIDANCE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/strategy-rules-prototype-v2.html
```

## Phase 0 — Read-only investigation (hard go/no-go STOP)

Do NOT edit in Phase 0. Investigate and report:

1. The shell's adjustments-tab mount point and the draft write target.
2. **Parallelization gate** (same three conditions as OTA-785). Report and sequence if any fail.
3. Reuse the shared junction-row / param-editor subcomponent if an earlier tab created one.
4. The catalog picker contract from OTA-789 (phase-filtered to `adjustment`).

Report findings + go/no-go.

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine.md` §3.6 / §4.1 (Phase 6: Adjustments)
- Adjustments evaluate **after** the raw score exists; each evaluates its condition in `evaluation_order` and, if triggered, adds, subtracts, floors, or caps. The score is clamped to `[0, 100]` after each adjustment.
- Floor/cap adjustments (e.g. "if stop level breached, force score below the F band") are how categorical hard-floor behaviour is expressed within a scoring model — they are ordinary adjustment rows. **No special-casing in the UI** for floor/cap; they are edited like any adjustment.

Source: `insight_engine-schema-ddl.md` §1/§2 (junction storage)
- Editable: `evaluation_order` (typed column) + threshold parameters (`parameters` JSON). `phase` lives on the rule.

Source: ticket AC
- "Add adjustment from catalog" / "Remove" insert/delete junction rows. Saves route through CRUD (OTA-782) + validation (OTA-783) against the draft.

Source: `UI-GUIDANCE.md` Part 3a / Part 4
- Tokens only; `var(--bg2)` never on rows/headers/panels. Scores `##.00`. No `$`.

## Scope
Build the Adjustments tab: render `adjustment`-phase junction rows with editable threshold parameters and `evaluation_order`; add an adjustment from the catalog (OTA-789, phase=adjustment); remove one. Floor/cap-style adjustments are edited as ordinary adjustment rows. Edits write to the draft via CRUD + validation.

## Acceptance criteria
- Adjustment junction rows render with editable threshold parameters and `evaluation_order`.
- "Add adjustment from catalog" / "Remove" insert/delete junction rows.
- Parameter edits write to the junction `parameters` JSON; saves route through CRUD (OTA-782) + validation (OTA-783).
- Floor/cap-style adjustments are editable as ordinary adjustment rows — no special-casing.

## Out of scope
- Other tabs / bands / catalog internals / Apply.

## Verification steps
- Manual: edit an adjustment's params + order; add a floor/cap adjustment from catalog and confirm it edits like any other; remove one.
- Component test.
- UI-GUIDANCE spot check.
- **QA level: medium.** Component + manual click-through.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-788.md  (or run in parallel per the OTA-791 note)

## Commit message template (if committing)
OTA-787 feat: Adjustments tab — adjustment junction editor (params, order)
