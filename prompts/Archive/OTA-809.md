# OTA-809 — Update CLAUDE.md with engine rules

## Terminal context
- This terminal: Terminal A (single stream)
- Concurrent terminals: OTA-807 / OTA-808 / OTA-810 may run in parallel — disjoint files.
- Cross-terminal dependencies: none.

## Required reading
Before any edits:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
```
Also locate and read `insight_engine.md` — §2 (principles), §2.6 (LLM precedence), §6 (config loading). Confirm path with `Get-ChildItem -Recurse -Filter insight_engine.md` if not at `claude_context/`.

## Relevant Context — Do Not Deviate Without Escalation

**Source: OTA-809 scope.** Add these six engine-rule entries to `CLAUDE.md`, phrased as prescriptive rules (no deliberation/alternatives framing):
1. Engine code (under `app/insight_engine/`) imports nothing from domain modules.
2. No `if strategy_id == ...` or `if strategy_key == ...` branches anywhere.
3. The junction is the only home of strategy×rule parameters, `evaluation_order`, `stop_if_fail`, `score_penalty`, and weights.
4. All rule content comes from the runtime tables; no magic numbers in code. The spreadsheet is a build-time seed only.
5. Every `engine.evaluate(...)` passes `source_app_id`.
6. The engine never calls an LLM; all disqualifying rules run before any LLM call (principle §2.6).

**Source: as-built reconciliation.** Persistence is a **single `bronze_evaluations` table discriminated by `record_type`** — junction-only parameters; no two-table bronze model. Any example or phrasing must be consistent with the as-built engine.

**Source: scope boundary.** These are *engine* rules. Do not restate business rules (those live in `business-rules.md`) or duplicate architecture-plan.md content — reference, don't copy.

**Source: `CLAUDE.md` House Style (SoT doc edits).** Update the `Last Updated` header (`yyyy-mm-dd hh:mm UTC`) and add a Change Log entry citing **OTA-809**. (You are editing CLAUDE.md itself — the Change Log is at the bottom of this same file.)

## Scope

### Phase 0 — Discovery (READ-ONLY, then STOP)
No edits. Report:
1. The right home for an "Insight Engine rules" subsection in `CLAUDE.md` (proposed heading + placement).
2. Whether any existing CLAUDE.md content already states (or contradicts) any of the six rules — to avoid duplicates/conflicts.

**STOP and report placement + any conflicts. Proceed on approval.** (Quick check — this is a small, well-specified addition.)

### Phase 1 — Edit (after approval)
Add the six prescriptive entries in the agreed location. Update header + Change Log (OTA-809).

## Acceptance criteria
- `CLAUDE.md` contains each of the six engine-rule entries above, phrased prescriptively.
- Entries are consistent with the as-built engine (single `bronze_evaluations` table; junction-only parameters).
- No duplication of business-rules.md or architecture-plan.md content (cross-reference instead).
- `Last Updated` header + Change Log entry (OTA-809) present.

## Out of scope
- No code changes.
- No business-rule content (belongs in business-rules.md).
- No re-statement of architecture-plan.md sections.

## Verification steps
- All six entries present and prescriptive (no "we considered…" framing).
- `Select-String -Path claude_context\CLAUDE.md -Pattern "source_app_id|stop_if_fail|evaluation_order"` → confirms the junction/engine entries landed.
- No conflicting older rule statements remain.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency.

## Commit message template (if committing)
```
OTA-809 docs: add Insight Engine prescriptive rules to CLAUDE.md
```
