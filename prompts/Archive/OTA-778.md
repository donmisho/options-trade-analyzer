---
allowed-tools: Read, Grep, Glob, Edit, Bash(git status:*), Bash(git diff:*), Bash(python:*), Bash(pytest:*), Bash(Get-ChildItem:*), Bash(Select-String:*)
---

# OTA-778 — Split StrategyDefinition (StrategyConfig vs OptionsStrategyParams)

Part of Feature OTA-774 — Domain coupling in engine machinery · Epic OTA-679. Second file in the batch.

## Terminal context
- This terminal: Terminal A (engine-decoupling — Feature OTA-774)
- Concurrent terminals: none
- Cross-terminal dependencies: none. Runs after OTA-775-776-777 has committed. Next in this terminal: OTA-780. Note OTA-779 (later in this batch) deletes `strategy_scorer.py`; if `StrategyDefinition` is defined in or tightly bound to that file, this story must leave it in a state OTA-779 can cleanly finish — see Phase 0.

## Phase 0 — READ-ONLY, hard STOP
Make no edits. Report, then STOP for go/no-go.

1. **Locate the type.** Report where `StrategyDefinition` is defined and every module that imports/reads it. State explicitly whether it lives in (or is imported by) `app/analysis/strategy_scorer.py` — this affects sequencing with OTA-779.
2. **Engine config target.** Report where the generic `StrategyConfig` should live (the engine-config object from F1 / OTA-695) and how strategy config is currently loaded.
3. **Live vs dead fields.** Classify each `StrategyDefinition` field as live (read somewhere) or dead (declared, never read). The audit §7.5 #1 candidates for dead are: `name`, `delta_min`, `delta_max`, `iv_rank_min`, `credit_pct_min`, `credit_pct_max`, `exit_profit_pct`, `exit_loss_multiplier`. Confirm against actual readers — do not trust the list blindly.
4. **Source of live options params.** For each live options-specific param, report whether its value should come from a junction row (`engine_strategy_rule_junction`) or the adapter, per F4/F5.

**STOP.** Wait for go/no-go.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
```

## Relevant Context — Do Not Deviate Without Escalation

- **Source: OTA-778; migration plan S7.4 (audit §5c #6, §7.5 #1).** Split `StrategyDefinition` into a generic `StrategyConfig` (key, label, weights, structures — lives in engine config) and `OptionsStrategyParams` (delta, IV, exit levels). Per the F4 audit most options params are dead code; the **live** ones move to junction rows or the adapter, not the engine.
- **Source: OTA-774 (Feature goal).** The engine references only `StrategyConfig` — no options vocabulary remains on the generic config type.
- **Source: OTA-680 / OTA-695 (config model).** Per-strategy junction weights are the sole weighting mechanism (validated to sum to 1.0000 per scoring set). `StrategyConfig.weights` is the generic weight carrier; do not reintroduce a parallel weight set on the options-params type.
- **Source: CLAUDE.md (Governance + House Style).** No SoT-doc edits. Commit only on instruction. `OTA-NNN feat: …`. PowerShell.

## Scope
Split `StrategyDefinition`:
- Generic `StrategyConfig` (key / label / weights / structures) → engine config; the engine references only this.
- `OptionsStrategyParams` (live delta / IV / exit-level params only) → sourced from junction rows / adapter. Dead fields are not carried forward into either type.

## Acceptance criteria
- `StrategyDefinition` is split: generic `StrategyConfig` (key / label / weights / structures) lives in engine config; options-specific params live in `OptionsStrategyParams`.
- Live options params are sourced from junction rows / adapter; dead ones are not carried forward.
- The engine references only `StrategyConfig` — no options vocabulary remains on the generic config type.

## Soft overlap (no link — target not ticketed)
Dead-field removal overlaps the **not-yet-ticketed** cleanup for unread `StrategyDefinition` fields (audit §7.5 #1; formerly plan "S8.1"). There is no OTA ticket for it. **Do not block on it.** If you drop the dead fields here as a by-product, note exactly which fields were dropped in the commit body so the future cleanup can be closed as already-done.

## Out of scope
No new strategy content, no threshold/weight value changes, no new strategies. If a "live" param has no junction/adapter source yet, STOP and report rather than hardcoding it.

## Verification steps
1. `Select-String -Path <engine config path> -Pattern 'delta|iv_rank|credit_pct|exit_profit|exit_loss'` → no options vocabulary on `StrategyConfig`.
2. Repo-wide Grep: every reader of the old `StrategyDefinition` now reads `StrategyConfig` (engine side) or `OptionsStrategyParams` (domain side); no dangling references.
3. Confirm dead fields appear in neither type.
4. **QA Level 2 (regression).** Backend test suite + a screening run confirming strategy config still resolves weights/structures and produces the same candidate ordering as before. Note the Level in the commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-780.md

## Commit message template
OTA-778 feat: split StrategyDefinition into generic StrategyConfig and OptionsStrategyParams
