# OTA-826 — Engine config junction read API + evaluation_order uniqueness validation

## Terminal context
- This terminal: Terminal A (solo)
- Concurrent terminals: none
- Cross-terminal dependencies: none. **Backend-only (Python).** Does NOT touch `web/src/api/client.js` or `StrategyAdminPage.jsx`, so it is safe to run before the OTA-785+ frontend chain. Run on a clean tree.

## Required reading
Before any code changes:

    cat claude_context/CLAUDE.md
    cat claude_context/insight_engine.md            # §6.1 junction model, §6.6 startup validation
    cat claude_context/insight_engine-schema-ddl.md  # §2 engine_strategy_rule_junction columns

Then read the as-built sibling read endpoints + the validation/loader path to mirror their shape:

    cat app/api/engine_config_store.py     # list_strategies_admin(), list_rules_admin() — COPY this pattern
    cat app/api/engine_config_routes.py    # GET /config/strategies/admin, /config/rules/admin — require_read
    cat app/insight_engine/validation.py   # the §6.6 checks AS BUILT — does order-uniqueness already exist?
    cat app/insight_engine/loader.py       # load_config(..., include_draft_key=...) — the draft-validation path

(Confirm the store/routes file locations in Phase 0 — OTA-825 placed `list_rules_admin()` in `engine_config_store.py` and the route in `engine_config_routes.py`; verify against the tree.)

**This prompt explicitly authorizes editing `claude_context/insight_engine.md` (§6.6 list + Change Log)** — one of the rare cases where Claude Code updates a SoT doc, per the OTA-709 precedent (code + doc land in the same commit). No other SoT doc may be modified.

## Relevant Context — Do Not Deviate Without Escalation

Source: insight_engine-schema-ddl.md §2 (`engine_strategy_rule_junction`)
Columns: `junction_id`, `strategy_id`, `rule_id`, `evaluation_order`, `stop_if_fail`, `score_penalty`, `weight`, `parameters` (JSON), `rationale`, `enabled`, `created_at`, `updated_at`, plus `terminal_verdict varchar(32) NULL` (added by OTA-709). `UNIQUE (strategy_id, rule_id)`. **`phase` is NOT on the junction — it lives on the rule.** The DDL comment (≈ line 129) states: *evaluation_order uniqueness within (strategy, rule.phase) is enforced at engine load (§6.6), not by a DB constraint.*

Source: insight_engine.md §6.6 (Startup validation) — CURRENT prose list
The loader rejects the configuration if any of: a rule references a named value not in the input catalog; a junction references a rule_id/strategy_id that does not exist; a junction omits a parameter in the rule's schema; a gate junction omits `evaluation_order` or `stop_if_fail`; active scoring weights do not sum to 1.0 within tolerance; a verdict band set is not monotonic; a condition references an unregistered formula; a parameter value violates type/bound; a named value's null semantics are incompatible.
**This list does NOT currently enumerate an `evaluation_order` uniqueness check — despite the DDL doc claiming it is enforced at load. Resolving this contradiction is Phase 0's job.**

Source: OTA-825 as-built pattern (the sibling to copy)
`list_rules_admin()` in `engine_config_store.py`: owner-spanning (`OTA` + `SHARED`), **no** `enabled=1` filter, live DB read via `async_session`, NOT the restart-gated runtime projection, NOT `AzureSqlConfigSource`. Route `GET /api/v1/config/rules/admin` in `engine_config_routes.py`, `Depends(require_read)`. Mirror this exactly for junctions.

Source: OTA-791 as-built — draft keys
Draft key = `<live_key>__draft` (`status='draft'`, `enabled=0`, excluded from runtime load). Junction CRUD resolves by `strategy_key`; a draft key is OTA-owned. `load_config(source, *, include_draft_key=None)` admits exactly one disabled key into a LOCAL config and runs full §6.6 validation — this is the path that can surface a draft's collisions.

Source: OTA-791 as-built — `validation.py` FK check (DO NOT REGRESS)
`_check_junction_fks` checks the strategy FK by EXISTENCE, not enablement. Do not regress it.

## Phase 0 — Read-only discovery (HARD STOP, go/no-go)
Make NO edits. Read and report:

1. **Sibling pattern:** paste the signatures of `list_strategies_admin` / `list_rules_admin` and their route definitions; confirm `async_session` + `require_read` + no `AzureSqlConfigSource`. Confirm exact file locations.
2. **Junction model:** confirm the ORM model / row shape and how to join rule metadata (`phase`, `intent`, `parameter_schema`). Confirm `terminal_verdict` is present on the model (OTA-709).
3. **Draft resolution:** confirm that passing `<key>__draft` through the read path returns the draft's junction rows (report the key-resolution path).
4. **§6.6 order-uniqueness — the doc conflict (decisive):** report whether `validation.py` ALREADY enforces `evaluation_order` uniqueness within (strategy, rule.phase). Quote the code if present, or confirm its absence. State which branch applies:
   - EXISTS → expose it to the draft (item 5) + correct the §6.6 prose list in `insight_engine.md`.
   - ABSENT → add the check to `validation.py` + add it to the §6.6 list.
5. **Draft reachability:** confirm whether the check (existing or to-be-added) runs over a draft via `include_draft_key`, or only over the running config.
6. **Startup-safety scan (HARD GATE):** query the current live + seeded junction config for any (strategy, phase) where two enabled rows share an `evaluation_order`. Report every collision. **If any exist, STOP** — enforcing/exposing the check would refuse engine load until they're resolved. Do not proceed.
7. **No-regress:** confirm `_check_junction_fks` remains existence-based; report file:line.

Paste the STOP report back to Claude Web. Wait for explicit go before Phase 1.

## Scope (Phase 1, after go)
- `list_strategy_junctions(strategy_key)` in `engine_config_store.py` (draft-aware key resolution; join rule metadata) + `GET /api/v1/config/strategies/{key}/junctions` in `engine_config_routes.py`, `Depends(require_read)`.
- §6.6 `evaluation_order` uniqueness within (strategy, rule.phase): confirm-existing-and-expose, or add — per the Phase 0 branch — with the check reachable for a draft via `include_draft_key`.
- Update `insight_engine.md` §6.6 prose list to enumerate the check + dated Change Log entry.

## Acceptance criteria
- `GET /api/v1/config/strategies/{key}/junctions` returns all junction rows for the resolved strategy (live or `__draft` key): full junction shape (`junction_id`, `strategy_id`, `rule_id`/`rule_key`, `evaluation_order`, `stop_if_fail`, `score_penalty`, `weight`, `parameters`, `terminal_verdict`, `rationale`, `enabled`) + joined rule display metadata (`phase`, `intent`, `parameter_schema`). `require_read`; read-only.
- Live DB read via `async_session`; grep confirms `AzureSqlConfigSource` untouched and no new runtime read path.
- §6.6 enforces `evaluation_order` uniqueness within (strategy, rule.phase) as a hard load failure with a structured error (strategy, phase, colliding order).
- The check is reachable for a draft (`include_draft_key`) so the editor can surface collisions before Apply — NOT a client-only reimplementation.
- `insight_engine.md` §6.6 list enumerates the check; Change Log has a dated entry.
- `_check_junction_fks` remains existence-based.

## Out of scope
- The Hard Gates / Scoring / Adjustments editors (OTA-785/786/787 — consume this endpoint).
- Client-side collision UX feedback (OTA-785+).
- Junction-binding writes (OTA-782 owns CRUD).
- Apply / Reset / preview (OTA-790 / OTA-791).

## Verification steps (PowerShell)
    cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
    .\venv\Scripts\Activate.ps1

    # Endpoint, live key then draft key:
    #   GET /api/v1/config/strategies/<key>/junctions
    #   GET /api/v1/config/strategies/<key>__draft/junctions
    #   -> confirm shape + draft rows

    # Negative (collision): stage a draft with two gate rows sharing evaluation_order;
    #   run the draft-validation path (include_draft_key) -> structured collision error.

    # Startup still clean (no pre-existing collisions):
    #   start the backend; confirm the engine config loads without error.

    # No-regress greps:
    Select-String -Path app\insight_engine\*.py,app\api\*.py -Pattern "AzureSqlConfigSource"
    #   -> unchanged; no new runtime read path
    Select-String -Path app\insight_engine\validation.py -Pattern "exists|enabled"
    #   -> _check_junction_fks still existence-based

## Commit instruction
"I have been instructed to commit. Do you approve? (yes / no)"

## Coordination footer
OK to continue to OTA-785.md — once OTA-826 commits, the Hard Gates tab renders gate rows from this endpoint.

## Commit message template (if committing)
OTA-826 feat: junction read endpoint and evaluation_order uniqueness validation
