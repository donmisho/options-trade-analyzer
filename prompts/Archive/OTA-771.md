---
description: OTA-771 — Eliminate shared normalization pool (per-strategy min-max)
allowed-tools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash
---

# OTA-771 — Eliminate shared normalization pool

## Terminal context
- This terminal: **Terminal A**.
- Concurrent terminals: **none**.
- Cross-terminal dependencies: none. **Prompt 3 of 6.** Depends on OTA-768 + OTA-767 (prompt 2) having collapsed scoring to a single per-strategy engine call — normalization scope only makes sense once scoring runs per-strategy.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: insight_engine.md §2, Principle 2 (Strategy independence)**
"No strategy's logic may reference, depend on, or be defined in terms of any other strategy." A strategy's normalized scores must not be influenced by candidates eligible for a *different* strategy.

**Source: insight_engine-migration-plan.md §6.5 / Feature 6 S6.5**
Min-max normalization happens per-strategy, not across the engine's full candidate pool. SP's normalization range stops being influenced by WG-eligible candidates.

**Source: insight_engine.md §4 (Pipeline) + OTA-695 scope anchor**
Normalization is engine-core (single canonical implementation). The pipeline evaluates one strategy at a time over that strategy's candidate set. The normalization min/max window is therefore the strategy's own candidate set, not the union pool.

## Phase 0 — Read-only discovery (HARD STOP before any edits)

Do not edit in Phase 0. Authored without live-repo access — reconcile carefully.

1. Locate every normalization site:
   ```powershell
   rg -n -i "normaliz|min_max|min-max|\.min\(\)|\.max\(\)|MinMax" app/insight_engine app/analysis
   ```
2. `cat` the engine-core normalization implementation (OTA-695) and any legacy normalization still in `strategy_scorer.py`.
3. Determine the **current normalization window**: is min/max computed over the full cross-strategy candidate pool, or already per-strategy? Identify exactly where the candidate set is bounded.
4. Confirm where, post-OTA-768, candidates are grouped by strategy before scoring — that grouping boundary is where the normalization window must be scoped.

**STOP report (paste back to Claude.ai):**
- Normalization implementation location + file:line (engine-core vs legacy).
- Current min/max window: cross-pool or per-strategy? Evidence.
- The exact point where candidates are partitioned by strategy.
- Proposed change: scope min/max to each strategy's candidate set; whether this is a one-line window change or a structural reorder.
- Go / no-go.

Wait for Claude.ai's go decision.

## Scope (after go)
Compute min-max normalization within each strategy's own candidate set. Remove any dependence of a strategy's normalization range on the full cross-strategy candidate pool.

## Acceptance criteria
- Normalization min/max is computed within each strategy's own candidate set.
- The full cross-strategy candidate pool no longer affects any individual strategy's normalization range.
- SP's normalized scores are independent of which WG-eligible candidates are present in the same evaluation run (demonstrated by a test: run SP alone vs SP+WG-eligible candidates → SP's normalized scores identical).

## Out of scope
- Do not change any scoring formula or weight.
- Do not touch verdict bands, delta, or cushion thresholds.
- Do not change the engine's fixed phase order.

## Verification steps
- Add/extend a test: evaluate a fixed SP candidate set (a) alone and (b) alongside WG-eligible candidates; assert SP normalized scores are byte-identical across the two runs.
- `rg` confirms no normalization path consumes a cross-strategy/global candidate collection.
- `pytest` engine + scoring modules green.

## Commit instruction
I have been instructed to commit. **Don runs the commit manually** — do not run `git commit`. Present the changed-file list and the exact message below, then ask: **Do you approve? (yes / no)**

## Coordination footer
**OK to continue to OTA-769.md**

## Commit message template
```
OTA-771 feat: scope min-max normalization to each strategy's own candidate set; remove shared normalization pool
```

## Recommended QA level
**High** — silent scoring drift risk; the alone-vs-combined parity test is the acceptance gate, not optional.
