---
description: OTA-770 — Per-strategy cushion penalty thresholds (junction-sourced)
allowed-tools:
  - Read
  - Grep
  - Glob
  - Edit
  - Write
  - Bash
---

# OTA-770 — Per-strategy cushion penalty thresholds

## Terminal context
- This terminal: **Terminal A**.
- Concurrent terminals: **none**.
- Cross-terminal dependencies: none. **Prompt 5 of 6.** Depends on prompts 1–4. Pairs with **OTA-728** (F4 — cushion penalty as a registered post-scoring adjustment, shipped under OTA-725): OTA-728 owns the adjustment-rule mechanics; **this story owns the per-strategy thresholding side** (move the literals to the junction so SP and WG can differ).

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
cat claude_context/insight_engine-schema-ddl.md
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: insight_engine.md §2, Principle 2 (Strategy independence)**
SP and WG must be able to hold different cushion-penalty thresholds. Shared literals in a per-family scorer violate this.

**Source: insight_engine.md §3.4 + §6.3; insight_engine-schema-ddl.md §"Junction parameter storage"**
Per-rule thresholds live in `engine_strategy_rule_junction.parameters` (JSON, validated vs `parameter_schema` at load). The cushion-penalty adjustment formula reads its thresholds from `params`, never literals.

**Source: insight_engine-migration-plan.md §6.4 / S6.4 + S4.6 (OTA-725)**
The cushion penalty is already a registered post-scoring adjustment (two atomic adjustment rules per S2.2). Thresholds come from the junction, not literals. The legacy literals were at `strategy_scorer.py:138-142` (`_cushion_penalty`, the 1.0% / 2.0% values) — verify current location in Phase 0.

## Phase 0 — Read-only discovery (HARD STOP before any edits)

Do not edit in Phase 0.

1. Find the cushion penalty literals and the registered adjustment:
   ```powershell
   rg -n -i "cushion|_cushion_penalty|cushion_pct|1\.0|2\.0" app/analysis app/options_rules
   ```
2. `cat` the legacy `_cushion_penalty` (expected `strategy_scorer.py:138-142`) and the registered cushion adjustment formula (OTA-728/OTA-725). Confirm where the 1.0% / 2.0% thresholds currently live — legacy scorer, formula literal, or already junction-sourced.
3. Confirm whether OTA-767 (prompt 2) already deleted the shared credit-spread scorer that held these literals. If so, the literals may now live in the registered adjustment formula or nowhere — adjust scope accordingly.
4. Confirm the threshold params are (or need to be added) in the cushion adjustment rule's `parameter_schema`.
5. **Seed mechanism:** same determination as OTA-769 — how junction `parameters` values reach the live rows, and whether Claude-editable or Don-applied.

**STOP report (paste back to Claude.ai):**
- Current home of the 1.0% / 2.0% thresholds + file:line.
- Whether the cushion adjustment formula already reads thresholds from `params`.
- `parameter_schema` status for the threshold params.
- Seed-application mechanism + Claude-editable vs Don-applied.
- Proposed per-strategy values: SP and WG each get the current thresholds carried forward (independence only — no recalibration; they *can* differ later, they need not differ now).
- Go / no-go.

Wait for Claude.ai's go decision.

## Scope (after go)
- Remove the 1.0% / 2.0% cushion-penalty literals from wherever they remain (shared scorer or formula body).
- The registered cushion adjustment reads its thresholds per-strategy from junction `params`.
- Seed SP and WG junction rows with their thresholds (carried forward). If config is Don-applied, STOP and hand Don the exact rows.

## Acceptance criteria
- The 1.0% / 2.0% cushion-penalty literals are removed from the shared/credit-spread scorer (and from any formula body).
- Thresholds are sourced per-strategy from junction rows.
- SP and WG can hold independent cushion-penalty thresholds (schema + junction support it, even if seeded equal today).
- Cushion-penalty behavior preserved at current values.

## Out of scope
- Do not change the adjustment-rule *mechanics* (atomic decomposition) — that is OTA-728, already shipped.
- No recalibration of threshold values.
- No verdict-band, delta, or normalization changes.

## Verification steps
- `rg -n "1\.0|2\.0" <cushion formula/scorer paths>` → no cushion-threshold literals remain.
- Engine startup validation passes (threshold params validate vs `parameter_schema`).
- Cushion adjustment output for representative SP and WG candidates unchanged vs pre-change.
- `pytest` green.

## Commit instruction
I have been instructed to commit. **Don runs the commit manually** — do not run `git commit`. Surface any Don-applied config in the approval ask. Present the changed-file list + the exact message below, then ask: **Do you approve? (yes / no)**

## Coordination footer
**OK to continue to OTA-773.md**

## Commit message template
```
OTA-770 feat: move cushion-penalty thresholds to per-strategy junction params; SP and WG can hold independent thresholds
```

## Recommended QA level
**Standard** — threshold relocation; gate on startup validation and cushion-adjustment parity for SP and WG.
