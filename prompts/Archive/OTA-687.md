# OTA-687 + OTA-691 + OTA-692 + OTA-693 + OTA-694 — Configure strategies (SP / WG / TR / LT)

> Multi-ticket prompt. The four per-strategy subtasks are one cohesive seed session (shared domain, small,
> atomic rollback) per `build-execution.md`. One commit covers all five keys.

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none assumed — confirm with Don.
- Cross-terminal dependencies: requires OTA-681 (tables), OTA-682 (importer), and the rule rows from
  OTA-683 (+ ideally OTA-686/OTA-688) so junction rows can reference real `rule_key`s. Gate *mechanics*
  (`stop_if_fail`/`evaluation_order`/`score_penalty`) are set afterward in OTA-684.

## Required reading
```
cat claude_context/CLAUDE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md                 # § 3.4 strategy×rule junction, § 3.7 scan params, § 3.8 verdict bands
cat claude_context/insight_engine-schema-ddl.md       # § 2 engine_strategies, engine_strategy_rule_junction
# Canonical content: requirements/Scoring Parameters.xlsx (Sheet1 — per-strategy columns + Scan Params + Verdict Bands)
```

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine-schema-ddl.md` § 2
Each strategy = one `engine_strategies` row under `owner_app_id = 'OTA'` (`strategy_key`, `display_name`,
`consumer_surface = SCREENING`, `compatible_structures` JSON, `verdict_band_set` JSON NOT NULL, `dte_min`,
`dte_max`) plus `engine_strategy_rule_junction` rows for every rule it participates in (`weight`,
`parameters` JSON blob, `rationale`).

Source: `Scoring Parameters.xlsx` Sheet1 — Verdict Bands (universal; store identically on each strategy):
`EXECUTE: score >= 70` · `WAIT: 50 <= score < 70` · `PASS: score < 50`. Applied to the final adjusted score.
Per the schema, each strategy carries its OWN `verdict_band_set` (currently identical values across all four).

Source: `Scoring Parameters.xlsx` Sheet1 — scoring weights MUST sum to 1.0000 per strategy. Exact weights:

| Strategy | strategy_key | Structures (compatible_structures) | DTE window | Scoring weights (sum = 1.0) |
|---|---|---|---|---|
| **SP** Steady Paycheck | `steady_paycheck` | BULL_PUT_CREDIT, BEAR_CALL_CREDIT | 21–45 | Theta Margin Ratio 0.30 · Probability of Profit 0.25 · Expected Value 0.20 · Reward Risk 0.15 · IV Rank 0.10 |
| **WG** Weekly Grind | `weekly_grind` | BULL_PUT_CREDIT, BEAR_CALL_CREDIT, LONG_CALL, LONG_PUT | 14–20 | Probability of Profit 0.25 · Expected Value 0.15 · Theta Gamma Ratio 0.35 · Credit Width % 0.20 · Liquidity 0.05 |
| **TR** Trend Rider | `trend_rider` | BULL_CALL_DEBIT, BEAR_PUT_DEBIT | 30–45 | Expected Value 0.20 · SMA Alignment Score 0.30 · Delta Quality 0.25 · IV Percentile Cost 0.15 · Runway Score 0.10 |
| **LT** Lottery Ticket | `lottery_ticket` | LONG_PUT, IRON_CONDOR | 30–60 | Payout Ratio 0.45 · Delta OTM Score 0.25 · Bid Ask Tightness 0.20 · Open Interest 0.10 |

Source: `Scoring Parameters.xlsx` Sheet1 — Strategy Scan Parameters:

| Param | SP | WG | TR | LT |
|---|---|---|---|---|
| Evaluation cap (max trades scored) | 2500 | 1000 | 1000 | 500 |
| DTE priority start (days) | 30 | 14 | 35 | 45 |
| DTE expansion direction | bidirectional | shortest-first | bidirectional | bidirectional |
| Output rank cap (max surfaced) | 20 | 20 | 20 | 20 |

Source: `Scoring Parameters.xlsx` Sheet1 — per-strategy gate `parameters` (the values; mechanics in OTA-684):
- **SP:** Credit % of width `>= 0.3` · Cushion % of price `>= 0.01` · Cushion vs ATR `>= 1.0` · Earnings
  buffer past expiry `>= 0` · spread-width tier = Width Config lookup (OTA-690).
- **WG:** Credit % of width `>= 0.3` · Cushion % of price `>= 0.015` · Cushion vs ATR `>= 1.5` ·
  Earnings buffer `>= 0`.
- **TR:** Debit % of width `<= 0.4` · Chart state confirms direction = Y · Earnings buffer `>= 0`.
- **LT:** **Earnings buffer past expiry `>= 7`** (the 7-day buffer — audit § 5a #8) · Theta load fraction
  `<= 0.5` · Chart state confirms direction = Y.

Source: OTA-687 description
- Strategy abbreviations/colors (SP/WG/TR/LT) stay as-is pending the future taxonomy redesign — do NOT
  rename. **Note:** the workbook names the fourth strategy "Lottery Ticket"; subtask OTA-694's title says
  "Long-dated." Use the workbook value (`lottery_ticket`, "Lottery Ticket") and flag the title mismatch to
  Don rather than guessing.
- LT's earnings-buffer difference (7 vs 0) is expressed PURELY as junction `parameters` data — no
  per-strategy code branch.
- No business rule for any strategy may live outside the engine tables.

## OTA-687 — Scope
Create the four `engine_strategies` rows and their `engine_strategy_rule_junction` rows from the values
above, via the OTA-682 seed path.

## OTA-691 — SP scope · OTA-692 — WG scope · OTA-693 — TR scope · OTA-694 — LT scope
One subtask per strategy: that strategy's `engine_strategies` row (structures, verdict bands, DTE bounds)
plus its junction rows (scoring weights summing to 1.0, gate `parameters`, `rationale`). LT (OTA-694)
carries the 7-day earnings buffer as one parameter among its set.

## Acceptance criteria (all five tickets)
- Four `engine_strategies` rows exist under `owner_app_id = 'OTA'` with the structures, `verdict_band_set`
  (70/50), and DTE bounds above; `consumer_surface = SCREENING`.
- Each strategy has junction rows for every rule it participates in, with `weight`, valid-JSON
  `parameters`, and `rationale`; **scoring weights sum to exactly 1.0000 per strategy.**
- Scan parameters (eval cap / DTE start / expansion / output cap) are seeded per the table.
- LT's earnings buffer is `>= 7`; SP/WG/TR retain `>= 0`. Difference is junction `parameters` only — no
  per-strategy code branch anywhere.
- No hardcoded per-strategy threshold remains in code.

## Out of scope
- Gate mechanics `stop_if_fail`/`evaluation_order`/`score_penalty` (OTA-684).
- Defining rule logic / formulas (OTA-683, OTA-686) — this story binds existing rules to strategies.
- Renaming strategies (future taxonomy redesign).

## Verification steps
```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
```
- `SELECT strategy_key, compatible_structures, verdict_band_set, dte_min, dte_max FROM dbo.engine_strategies`
  → four rows matching the table.
- For each strategy: `SUM(weight)` over scoring junction rows = 1.0000.
- LT junction shows earnings buffer `>= 7`; SP/WG/TR show `>= 0`.
- All `parameters`/`verdict_band_set`/`compatible_structures` are `ISJSON = 1`.

Post-Build QA Gate: Level 1 (config-data seed). Note in commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-684.md  *(OTA-684 sets gate mechanics on the junction rows created here).*

## Commit message template (if committing)
```
OTA-687 OTA-691 OTA-692 OTA-693 OTA-694 feat: configure SP/WG/TR/LT strategies

engine_strategies rows + junction rows from Scoring Parameters.xlsx: structures,
verdict bands (70/50), DTE windows, scan params, scoring weights (sum 1.0), gate
parameters incl. LT 7-day earnings buffer (data-only, no code branch). QA Level 1.
```
