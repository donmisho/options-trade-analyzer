---
allowedTools: Read, Edit, Write, Bash, Grep, Glob
---

# OTA-541 — Architecture #6: Route File Consolidation

## Terminal context
- This terminal: **Terminal B**
- Concurrent terminals: Terminal A (pipeline), Terminal C (AI stack), Terminal D (provider/strategy/docs)
- Cross-terminal dependencies: STOP — do not start until **B2 (OTA-543) commits**. Both touch `app/main.py`.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
```

Pay specific attention to `architecture-plan.md` § 3 Key API Endpoints (current state with status flags) and the Cleanup Roadmap "Should Fix" section.

## Relevant Context — Do Not Deviate Without Escalation

**Source: ticket OTA-541 → Six work items**
1. Rename `agent_routes.py` → either `trade_evaluation_routes.py` or `trade_agent_routes.py`. Pick one and document.
2. Rename `agents_routes.py` → `position_monitor_routes.py`.
3. Resolve `watchlist_routes.py` vs `named_watchlist_routes.py` overlap. Named-watchlist is the forward-looking shape — that one survives. Migrate any unique functionality from the loser, then delete the loser.
4. Wire `cors_origins` setting from `core/config.py` into `main.py` CORS middleware. Delete the hardcoded list.
5. Remove `test_routes.py` from `main.py` includes; delete the file or move to `tests/` if utility value remains.
6. Audit `validation_routes.py` — document purpose if active, delete if vestigial.

**Source: CLAUDE.md → Project-critical shared file rules**
`app/main.py` is on the project-critical shared list. Two parallel terminals must never edit it simultaneously. Terminal B's chain serializes on this file; this prompt is the third in the series.

**Source: CLAUDE.md → House Style + Provider routing**
Provider routing always via `_get_provider()` — a route rename should not introduce hardcoded provider references.

**Source: CLAUDE.md → Frontend client.js coupling**
`web/src/api/client.js` is also on the project-critical shared list. Route renames cascade into client.js. Coordinate edits there carefully — no other terminal should touch client.js while this prompt runs.

## Phase 1 — Read-only diagnosis (STOP gate before Phase 2)

1. `cat app/main.py` — list every `app.include_router(...)` call. Note the prefix and tag for each.
2. `cat app/api/agent_routes.py` and `cat app/api/agents_routes.py` — read both end to end. Confirm scope: `agent_routes.py` is trade-evaluation agent (triage / deep-dive / followup); `agents_routes.py` is position monitor scheduler status. Pick a final name from the two options in the ticket and report it to Don for sign-off.
3. `cat app/api/watchlist_routes.py` and `cat app/api/named_watchlist_routes.py`. Read both. List all endpoints in each. Identify any endpoint in the legacy file that does NOT have an equivalent in the named-watchlist file — those need migration before deletion.
4. `cat app/core/config.py` — find `cors_origins` setting. Note current value/default.
5. Find the hardcoded CORS list in `main.py`. Diff against `cors_origins`. Note any drift.
6. `cat app/api/test_routes.py` and `cat app/api/validation_routes.py`. Note whether they have any active callers (`grep -rn "from app.api.test_routes\|from app.api.validation_routes" app/ web/`).
7. `cat web/src/api/client.js` — list every URL fragment that touches the soon-to-rename routes (`/agents`, `/agent`, `/watchlist`, `/named_watchlist`).

**STOP. Report back to Don with:**
- Final names chosen for the two renames (Don approves both).
- Surviving watchlist file (named-watchlist per ticket) + list of endpoints to migrate before deleting the loser.
- Test_routes and validation_routes status (active/vestigial) and proposed action for each.
- Number of client.js call sites that will need URL updates.
- Proposed `architecture-plan.md` § 3 update.

Wait for Don's go-ahead before Phase 2.

## Phase 2 — Apply changes

### Renames

1. `git mv app/api/agent_routes.py app/api/<chosen-name>.py` (actually use `git mv` — preserves history).
2. `git mv app/api/agents_routes.py app/api/position_monitor_routes.py`.
3. Update every `from app.api.agent_routes` → `from app.api.<chosen-name>` and likewise for `agents_routes`. `grep -rn "from app.api.agent_routes\|from app.api.agents_routes\|import agent_routes\|import agents_routes" app/`.
4. Update `app.include_router(...)` calls in `main.py`.
5. Update any test files that import the renamed modules.

### Watchlist resolution

1. Migrate any unique endpoints from `watchlist_routes.py` into `named_watchlist_routes.py`.
2. Delete `app/api/watchlist_routes.py`.
3. Update `client.js` URLs that referenced the old paths.
4. `grep -rn "watchlist_routes\|/watchlist[^_]" app/ web/` — confirm no orphans.

### CORS wiring

1. Replace the hardcoded CORS list in `main.py` with the value from `settings.cors_origins`.
2. If `cors_origins` is empty/default in dev, ensure the dev frontend origin is captured by either expanding the default or using an env-overridable value.
3. Test in local dev: open the frontend, confirm requests succeed (CORS not blocking).

### test_routes purge

1. Remove the `app.include_router(test_routes_router, ...)` from `main.py`.
2. If the file has no production value, delete it.
3. If utility value remains (e.g., dev-only ping endpoints), move to `tests/` directory and document.

### validation_routes audit

1. Per Phase 1 finding: if active, leave it and document its scope in `architecture-plan.md` § 3.
2. If vestigial, delete it and remove its include.

### architecture-plan.md update

Update § 3 Key API Endpoints to reflect the renamed files and the watchlist consolidation. Add Change Log entry referencing OTA-541. Update `Last Updated` header.

## Acceptance criteria

- No file named `agent_routes.py` or `agents_routes.py` exists in `app/api/`.
- The two new file names match Don's Phase 1 decision.
- Either `watchlist_routes.py` or `named_watchlist_routes.py` exists, not both. Named-watchlist survives.
- CORS configuration is sourced from `settings.cors_origins`, not hardcoded.
- `test_routes.py` is not registered in the production app.
- `validation_routes.py` status decided and applied.
- `architecture-plan.md` § 3 reflects the cleanup with Change Log entry.
- All frontend callers (in `client.js`) updated and working.
- Backend imports clean: `python -c "import app.main"` succeeds.

## Out of scope

- Adding new routes.
- Auth route consolidation (Story OTA-538 / B1 handles).
- Splitting `client.js` 867-line monolith (Story #12 backlog item).
- Reshaping route bodies — pure rename + consolidation.

## Verification steps

1. `python -c "import app.main"` succeeds.
2. Local backend starts cleanly; `/health` returns 200.
3. `/docs` (FastAPI Swagger) loads and shows the renamed routes under sensible groupings.
4. Local frontend loads and exercises:
   - Trade evaluation triage / deep-dive / followup (covers renamed `agent_routes`)
   - Position Monitor status panel (covers renamed `agents_routes`)
   - Watchlist add/remove (covers consolidated watchlist)
5. CORS: open browser DevTools, confirm no CORS errors on cross-origin requests.
6. `grep -rn "agent_routes\|agents_routes\|watchlist_routes" app/ web/` — only legitimate hits remain.
7. `git diff` review with Don. Confirm `architecture-plan.md` Change Log entry.

QA Level: **Level 2** — cross-cutting refactor with frontend coupling. Run the three workflows above + backend test suite.

## Commit instruction

I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer

OK to continue to **B4-OTA-544.md**

## Commit message template

```
OTA-541 refactor: rename agent_routes/agents_routes for clarity; consolidate to named_watchlist; wire cors_origins; drop test_routes
```
