# OTA-683 — Decompose compound rules into atomic rules (during seed)

## Terminal context
- This terminal: Terminal A
- Concurrent terminals: none assumed — confirm with Don.
- Cross-terminal dependencies: requires OTA-681 (tables) applied and OTA-682 (importer) present. Produces `engine_rules` rows. No project-critical shared file is touched.

## Required reading
Before any changes, from the repo root:

```
cat claude_context/CLAUDE.md
cat claude_context/business-rules.md
cat claude_context/insight_engine.md                # § 3.6 rule phases + gate mechanics
cat claude_context/insight_engine-schema-ddl.md      # § 2 engine_rules shape
```

Then read the canonical content + the code-side compounds you must decompose:

```
# Seed source of truth (the canonical rule catalog):
#   requirements/Scoring Parameters.xlsx  (Sheet1)
# Code-derived compounds — read these to get exact route/band semantics:
cat app/analysis/hard_gates/earnings_gate.py            # the 4-route earnings tree
cat app/analysis/strategy_scorer.py                     # _cushion_penalty graduated bands (~lines 138-142)
# credit/debit quality gate (Pipeline Gate 2):
#   app/api/evaluation_routes.py  (~lines 626-663)
```

If a path is absent, STOP and ask Don. The workbook is the canonical content source; the audit report
(`requirements/insights-engine-audit-2026-05-20.md`) is supplementary if present.

## Relevant Context — Do Not Deviate Without Escalation

Source: `insight_engine.md` § 3.6 (atomic rules); audit § 5b
Each `engine_rules` row evaluates exactly ONE condition. Compound conditions are split into multiple atomic
rules, each with its own `rule_key`, and ordered within their phase via the junction `evaluation_order`
(set later in OTA-684). BETWEEN is two atomic comparison rules (`>= low` AND `<= high`).

Source: `Scoring Parameters.xlsx` Sheet1 + audit § 5b pre-loaded suspects — decompose EXACTLY these:
1. **Chart state confirms direction** (Hard Gate, Per-Strategy; TR=Y, LT=Y) → 2 atomic rules:
   (a) `chart_state IN {Bullish Alignment, Bearish Alignment}`; (b) `chart_state matches trade direction`.
2. **Stock extended in trade direction** (Post-Scoring penalty, REDUCE SCORE BY 10) → 2 atomic rules:
   (a) `(abs(spot - SMA_50) / SMA_50) > 0.05`; (b) `extension matches trade direction`.
3. **Cushion barely above ATR floor** (Post-Scoring penalty, REDUCE SCORE BY 5) → 2 atomic rules
   (BETWEEN split): (a) `cushion_vs_ATR >= 1.0`; (b) `cushion_vs_ATR <= 1.5`.
4. **Earnings gate 4-route tree** → 4 atomic rules, ordered, with per-route `stop_if_fail` set
   appropriately (stopping vs non-stopping routes distinguished). The exact 4 routes come from
   `earnings_gate.py` — read it and mirror its route semantics; do not invent routes.
5. **Pipeline Gate 2 — credit/debit quality** → 2 atomic rules. Sheet thresholds: Credit % of width
   `>= 0.3`; Debit % of width `<= 0.4`. Confirm exact behavior against `evaluation_routes.py:626-663`.
6. **Cushion penalty graduated bands** → 2 atomic adjustment-phase rules, ordered. Read
   `strategy_scorer.py` `_cushion_penalty` for the exact band breakpoints; mirror them.

Source: OTA-683 description
The runtime `BETWEEN` operator itself belongs to the later engine-core expression library (not yet
ticketed) — do NOT implement an operator here. Here you only split the compound into two comparison rows.

Source: OTA-682 model
These rows are produced via the build-time seed path (workbook restructuring + importer transformation,
per OTA-682). Specify the target `engine_rules` rows; the exact authoring mechanism is your choice within
the importer framework. If achieving atomic rows requires a design change to OTA-682's importer, STOP and
flag it rather than forking a second seed path.

## Scope
Seed `engine_rules` so every compound rule above exists as the stated number of atomic rows, each with a
distinct `rule_key`, correct `phase`/`tier`, and a `condition_expression` (or `formula_ref`) capturing its
single condition.

## Acceptance criteria
- Each of the six compounds is present in `engine_rules` decomposed into exactly the stated count of atomic
  rules (2/2/2/4/2/2), each with its own `rule_key`.
- BETWEEN-style logic is two comparison rows; no runtime BETWEEN operator is implemented here.
- The earnings 4-route tree is four rows whose route semantics mirror `earnings_gate.py`, with per-route
  `stop_if_fail` correct (stopping vs non-stopping).
- The graduated cushion penalty is two ordered adjustment-phase rows; no single graduated-band rule remains.
- No atomic rule contains `AND`/`OR`/nested conditions.

## Out of scope
- Setting `evaluation_order`/`stop_if_fail`/`score_penalty` on junction rows (OTA-684) — except where a
  route's `stop_if_fail` is intrinsic to defining the rule; coordinate the boundary with OTA-684.
- Binding rules to strategies via junction rows (OTA-687).
- The runtime BETWEEN operator and any engine-core expression library work.

## Verification steps
PowerShell, project root, dev DB (OTA-681 + OTA-682 in place):

```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1
# run the seed path, then verify the atomic rows:
```
- `SELECT rule_key, phase, tier, condition_expression FROM dbo.engine_rules` shows the expected atomic rows
  for all six compounds; counts match (2/2/2/4/2/2).
- No `condition_expression` contains `AND`/`OR`.
- Earnings rows match `earnings_gate.py` routes; cushion-penalty rows match `strategy_scorer.py` bands.

Post-Build QA Gate: data-seed change, no existing reader of `engine_*` yet → Level 1. Note in commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-687.md  *(strategy config + junction rows reference these rule_keys; OTA-684 then sets gate mechanics on those junction rows).*

## Commit message template (if committing)
```
OTA-683 feat: decompose compound rules into atomic engine_rules during seed

Splits chart-state, stock-extended, cushion-vs-ATR, earnings 4-route,
credit/debit quality, and graduated cushion penalty into atomic rule rows.
BETWEEN -> two comparisons; runtime operator deferred to engine core. QA Level 1.
```
