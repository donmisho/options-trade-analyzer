---
allowedTools: Read, Edit, Write, Bash, Grep, Glob
---

# OTA-537 — Architecture #2: Trade Evaluation Prompts to SKILL.md

## ⚠️ GATE — DO NOT START THIS PROMPT UNTIL TERMINAL A's A2 (OTA-549) COMMITS

Both prompts touch `app/api/evaluation_routes.py`. A2 wires in the narrative grounding validator; this prompt migrates the prompt source. They will conflict if run simultaneously.

Confirm with Don that A2 is committed (and ideally that the changes are pulled into your working branch) before proceeding.

## Terminal context
- This terminal: **Terminal C**
- Concurrent terminals: Terminal A (pipeline), Terminal B (architecture main.py chain), Terminal D (provider/strategy/docs)
- Cross-terminal dependencies: gated on Terminal A's A2 (OTA-549)

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
```

Pay specific attention to `architecture-plan.md` § Skill-Driven Prompt Architecture (Deep Dive) and § Pattern 2 (Skill-Driven Prompt Architecture).

## Relevant Context — Do Not Deviate Without Escalation

**Source: architecture-plan.md § Pattern 2 (canonical)**
Pattern 2 is honored consistently for the agent pipeline (`agent_routes.py` loads prompts from `app/skills/claude-trade-agent/SKILL.md`) but violated by `app/ai/prompts.py` which contains a 110-line hardcoded `TRADE_EVALUATION_SYSTEM_PROMPT` and a 14-line `FOLLOW_UP_SYSTEM_PROMPT`. This is the most-trafficked AI path in the system. Closing this Pattern 2 violation is the highest-leverage consistency win.

**Source: CLAUDE.md → House Style**
"Prompts in SKILL.md: Never hardcode prompts in Python or React. Always load via `skill_loader.py`."

**Source: architecture-plan.md § Skill-Driven Prompt Architecture (Deep Dive)**
SKILL.md sections are loaded via `get_skill("<skill-name>").get("<SECTION>", **context)`. The `**context` provides values for `{{variable}}` interpolation in the markdown.

## Phase 1 — Read-only diagnosis (STOP gate before Phase 2)

1. `cat app/ai/prompts.py` — read both prompt constants verbatim. Note every dynamic insertion (Python f-string formatting, `.format(...)` calls, etc.) — these become `{{variable}}` interpolations.
2. `cat app/skills/claude-trade-agent/SKILL.md` — confirm the existing pattern. Note the section header style.
3. `cat app/skills/skill_loader.py` — confirm the `get_skill().get("SECTION", **context)` API and any path conventions for new skills.
4. `cat app/api/evaluation_routes.py` — find every import from `app.ai.prompts`. Note the call sites where the prompts are passed to `adapter.chat()`. Confirm A2 (OTA-549) has landed and the validator is already in this file.
5. Confirm prompt-version recording. `grep -rn "prompt_version\|prompt_section" app/` — does `agent_run_log` already capture these? If yes, confirm the new SKILL.md path will record correctly. If no, add the recording.

**STOP. Report back to Don with:**
- The two prompt constants verbatim plus a side-by-side proposed SKILL.md form (with `{{variable}}` interpolation marked).
- Section names: recommend `## TRADE_EVALUATION_SYSTEM` and `## FOLLOW_UP_SYSTEM` per the ticket.
- Path: `app/skills/trade-evaluation/SKILL.md`.
- Prompt-version recording status (already captured, or needs addition).
- Confirmation A2 has landed (re-confirm before proceeding).

Wait for Don's go-ahead before Phase 2.

## Phase 2 — Apply migration

### Create the SKILL.md

`app/skills/trade-evaluation/SKILL.md`:

```markdown
# trade-evaluation

Source-of-truth prompts for the structured trade evaluation path. Loaded by `evaluation_routes.py` via `get_skill("trade-evaluation").get(SECTION, **context)`.

## TRADE_EVALUATION_SYSTEM

[The current `TRADE_EVALUATION_SYSTEM_PROMPT` content, with any Python-side dynamic insertions converted to `{{variable}}` interpolation. Preserve every word otherwise.]

## FOLLOW_UP_SYSTEM

[The current `FOLLOW_UP_SYSTEM_PROMPT` content, same treatment.]
```

### Update evaluation_routes.py

1. Replace `from app.ai.prompts import TRADE_EVALUATION_SYSTEM_PROMPT, FOLLOW_UP_SYSTEM_PROMPT` with `from app.skills.skill_loader import get_skill`.
2. Replace the call site:
   ```python
   system_prompt = TRADE_EVALUATION_SYSTEM_PROMPT  # old
   ```
   with:
   ```python
   system_prompt = get_skill("trade-evaluation").get("TRADE_EVALUATION_SYSTEM", **context)  # new
   ```
   where `context` carries any variables the prompt needs (from Phase 1 inventory).
3. Same for `FOLLOW_UP_SYSTEM`.

### Delete app/ai/prompts.py

```bash
git rm app/ai/prompts.py
```

`grep -rn "from app.ai.prompts\|import app.ai.prompts" app/` — zero hits.

### Confirm prompt-version recording

In the call site, after loading the prompt, log:
- `prompt_section` = `"TRADE_EVALUATION_SYSTEM"` (or `FOLLOW_UP_SYSTEM`)
- `prompt_version` = file hash of `app/skills/trade-evaluation/SKILL.md` (use the existing helper if Pattern 2 already records this for the agent path; otherwise add `hashlib.sha256(skill_path.read_bytes()).hexdigest()[:12]`)

If the `agent_run_log` schema doesn't have these columns yet, add via additive migration (file Subtask under OTA-523 — do NOT alter the schema in this prompt without the migration).

### Smoke-test output equivalence

Pick a known input (an AMZN vertical, for instance). Run it through the OLD code path BEFORE the migration commit AND through the NEW code path AFTER. Confirm:
- The verdict is the same (EXECUTE/WAIT/PASS/WAIT_FOR_EARNINGS).
- The narrative is qualitatively similar (the model's stochasticity will produce textual variation; verdict-and-reason content should be equivalent).
- `TradeVerdict` validation passes identically.

If the verdicts differ, the prompt content didn't migrate cleanly — re-check the SKILL.md transcription for any whitespace or semantic drift.

## Acceptance criteria

- `app/ai/prompts.py` does not exist.
- `app/skills/trade-evaluation/SKILL.md` exists with both sections.
- `evaluation_routes.py` imports `get_skill` from `app.skills.skill_loader`, not constants from `app.ai.prompts`.
- All structured trade evaluations work end-to-end: input → SKILL.md prompt → Claude → `TradeVerdict` validates → response.
- `agent_run_log` rows for these calls record `prompt_section` and `prompt_version`.
- No regression: same input → same verdict in dev smoke test.
- Narrative grounding validator from A2 still fires correctly on the new prompt path (confirm one validation-failing case still triggers regen).

## Out of scope

- Reshaping prompt content (this is a migration, not a re-tuning exercise).
- AI stack merge (Story OTA-536 / C1 handles, already shipped this terminal).
- Migrating other prompts (any prompt that doesn't live in `app/ai/prompts.py` is out of scope for this Story).

## Verification steps

1. `python -c "import app.main"` succeeds.
2. `python -c "from app.skills.skill_loader import get_skill; print(get_skill('trade-evaluation').get('TRADE_EVALUATION_SYSTEM'))"` returns the prompt text without errors.
3. Local backend + frontend running. Evaluate AMZN vertical via the Evaluate button. Returns valid verdict.
4. Inspect a fresh `agent_run_log` row from that call. Confirm `prompt_section` and `prompt_version` populated.
5. Force a validation failure (deliberately craft a payload that will produce a positive-EV claim against negative computed EV) — confirm A2's validator still fires and regen happens.
6. `grep -rn "TRADE_EVALUATION_SYSTEM_PROMPT\|FOLLOW_UP_SYSTEM_PROMPT\|from app.ai.prompts" app/` — zero hits.
7. `git diff` review with Don.

QA Level: **Level 3** — primary AI path migration. Run AMZN regression + manual narrative consistency check on three symbols.

## Commit instruction

I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer

Independent — no downstream dependency

## Commit message template

```
OTA-537 refactor: migrate TRADE_EVALUATION_SYSTEM_PROMPT and FOLLOW_UP_SYSTEM_PROMPT to app/skills/trade-evaluation/SKILL.md; close Pattern 2 violation
```
