# OTA-644 — Export MD v2: Invalidation conditions section ("This Trade Is Wrong If")

## Terminal context
- This terminal: Terminal B
- Concurrent terminals: Terminal A (running OTA-643)
- Cross-terminal dependencies: OTA-638 must be committed. No shared file edits with Terminal A (Score breakdown and Invalidation are separate section builders).

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/business-rules.md
```

Also read the v2 reference sample:

```
qa_handoff_schema_v2_QQQ_example.md
```

Inspect the evaluation pipeline and where the verdict is produced:

```
# Find the agent that produces the verdict + narrative + invalidation list
grep -rn "TradeVerdict\|invalidation\|wrong if" app/agents/ app/ai/
# Find the evaluation SKILL.md
find app/skills/ -name "SKILL.md" | xargs grep -l "evaluation\|verdict" 2>/dev/null
# Inspect the persisted JSON shape
grep -rn "agent_run_log\|class AgentRunLog" app/models/
# Find the Pydantic schema for the verdict
grep -rn "class TradeVerdict" app/ai/
```

## Relevant Context — Do Not Deviate Without Escalation

**Section heading — exact text.** The QA skill greps for this exact string. Do not paraphrase.

```
### Invalidation conditions ("This Trade Is Wrong If")
```

Source: QQQ v2 sample.

**Section placement.** Sits **under** the `### App narrative ("Claude's Read")` subsection inside the App verdict block, immediately after the narrative paragraph(s). Source: QQQ v2 sample structure (Score breakdown → App narrative → Invalidation conditions).

**Format — bullets, one condition per line.** Source: QQQ v2 sample:

```
- QQQ closes above $717.32 (8-day SMA) for two consecutive sessions — reclaim of short-term trend invalidates bearish setup.
- QQQ trades above $720.71 (50-day SMA) intraday — full bullish reclaim.
- VIX drops below 16 with QQQ flat or rising — IV crush on long put leg, theta decay accelerates against position.
- 7 DTE reached without QQQ touching $711 — time stop, gamma risk too high.
```

Each bullet is a single line. No nesting. No headers within the section. The QA skill expects to parse the bullet list as a flat array.

**No re-generation at export time.** Conditions come from the persisted evaluation result, not from a fresh Claude call. Cost guardrail (`business-rules.md` Cost Guardrails — no page-load Claude calls; no export-time Claude calls).

**Same content for trades and positions.** Both export types render the section identically.

**No empty-section render.** If the evaluation produced zero invalidation conditions (legacy data, or pre-v2 evaluation), render:

```
### Invalidation conditions ("This Trade Is Wrong If")

_No invalidation conditions recorded for this evaluation._
```

(Italicized via underscores.) Do not omit the section heading — the QA skill needs the grep target to be present even when the content is empty.

## Scope — Phase 1 (audit, mandatory STOP-and-report gate)

**Read the evaluation pipeline and `agent_run_log` shape.** Answer these questions in a Jira comment on OTA-644 before any code change:

1. Where in `app/agents/` (or `app/skills/`) is the trade verdict produced? Name the agent and the SKILL.md path.
2. What is the current `TradeVerdict` Pydantic schema (`app/ai/schemas.py`)? Does it contain a discrete `invalidation_conditions` field today? If yes, what type (`list[str]`? `list[Condition]`?)? If no, where does the list live?
3. Is the list persisted on the `TradeRecommendation` row as a discrete column, in `agent_run_log.response` JSON only, or not at all?
4. If in `agent_run_log.response`: what JSON path resolves to the list? (e.g., `response.verdict.invalidation_conditions`).
5. Does the evaluation SKILL.md currently instruct Claude to produce the list? Re-read the SKILL and confirm.

**Post the findings here in the prompt session and STOP.** Three outcomes:

- **Outcome 1 — already a discrete field on `TradeRecommendation`.** Phase 2 proceeds normally; just read and render.

- **Outcome 2 — embedded in `agent_run_log.response` JSON.** Phase 2 proceeds with a parse step: extract the list from the JSON path identified in Phase 1. Document the JSON path in a code comment so future schema changes surface clearly.

- **Outcome 3 — not persisted at all.** Phase 2 cannot proceed directly. A precursor change is needed: (a) add the field to the `TradeVerdict` Pydantic schema, (b) update the evaluation SKILL.md to instruct Claude to produce the list as a structured field, (c) Alembic migration (additive) on `TradeRecommendation`. Report this finding and STOP. Do not commit. Do not write the precursor here — that's a separate Subtask.

## Scope — Phase 2 (only after Phase 1 clears or Outcomes 1/2 confirmed)

1. **Add the `### Invalidation conditions ("This Trade Is Wrong If")` subsection** under `### App narrative` in both trade and position exports.

2. **Implement `invalidation_block(rec_or_position)` builder.** Reads the list from the source identified in Phase 1:
   - Outcome 1: read directly from the column.
   - Outcome 2: parse from `agent_run_log.response` JSON at the documented path. Defensive parse — if the JSON is malformed or the path is missing, fall through to the empty-section render.

3. **Empty-section handling.** When the list is empty or missing, render the italicized fallback line. Always emit the section heading.

4. **No Claude call.** Verify by inspection that the builder imports nothing from any Claude adapter and makes no network call. Add a unit test that asserts no adapter is instantiated when the builder runs.

5. **Snapshot tests.**
   - Fixture A: evaluation with three or more invalidation conditions. Assert exact-match on the section.
   - Fixture B: evaluation with empty/missing invalidation list. Assert the fallback line renders with the section heading.

6. **QA skill smoke test.** After implementation, export a fresh evaluation and upload the resulting MD to claude.ai. Run the `options-analyzer-qa` skill and confirm Step 0 picks up the invalidation list as a structured array (the skill greps for the exact heading string).

## Acceptance criteria

- [ ] Phase 1 audit findings posted as a comment on OTA-644 in Jira before any code change
- [ ] If Phase 1 indicated Outcome 3 → STOP confirmed; prompt did not proceed to Phase 2; no commit
- [ ] If Phase 1 cleared (Outcomes 1 or 2) → Invalidation conditions subsection present on every v2 export (both trade and position)
- [ ] Section heading matches the exact string `### Invalidation conditions ("This Trade Is Wrong If")` — including the quote marks around "This Trade Is Wrong If"
- [ ] Bullets are one-line each, no nesting, no headers within the section
- [ ] Empty / missing list renders the italicized fallback `_No invalidation conditions recorded for this evaluation._` under the same heading
- [ ] No Claude API call at export time — verified by inspection and by the no-adapter unit test
- [ ] If Outcome 2 path: code comment documents the JSON path resolved from `agent_run_log.response`
- [ ] Both snapshot tests pass
- [ ] QA skill smoke test passes — `options-analyzer-qa` Step 0 picks up the invalidation list as structured data

## Out of scope

- Changing the content or quality of invalidation conditions Claude produces
- Adding rules-based fallback conditions (no synthetic invalidations)
- Modifying the evaluation prompt or SKILL.md to ask Claude for more or fewer conditions
- Frontend rendering of the invalidation list

## Verification steps

1. Run backend test suite + the two new snapshot tests + the no-adapter unit test. All pass.
2. Manual export against a position with a known evaluation that included invalidation conditions. Confirm the section renders with each condition on its own bullet.
3. Manual export against a position with an evaluation that lacks the list (if any exist). Confirm the fallback renders with the heading still present.
4. Upload the manual-export MD to claude.ai, run `options-analyzer-qa` skill. Confirm Step 0 reports the invalidation list as a structured array, not as free text.
5. QA Level: 2 (new section + reads from evaluation pipeline + smoke test against the QA skill). Record in commit body.

## Commit instruction

I have been instructed to commit. Do you approve? (yes / no)

If Phase 1 reported Outcome 3 and the prompt stopped, do NOT commit. Report the finding and wait for Don to file the precursor Subtask covering the schema add + SKILL.md update + Alembic migration.

## Coordination footer

Independent — no downstream dependency.

If Phase 1 stopped: STOP until the precursor `TradeVerdict` schema-add Subtask completes.

## Commit message template

```
OTA-644 feat: Export MD v2 — Invalidation conditions section

QA Level: 2 (new section + reads from evaluation pipeline + QA-skill smoke test).
```
