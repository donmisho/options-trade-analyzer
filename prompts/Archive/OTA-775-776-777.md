---
allowed-tools: Read, Grep, Glob, Edit, Bash(git status:*), Bash(git diff:*), Bash(python:*), Bash(pytest:*), Bash(Get-ChildItem:*), Bash(Select-String:*)
---

# OTA-775 + OTA-776 + OTA-777 — Gate-framework decoupling

Combined, atomic. GateTradeContext → generic candidate reference (OTA-775); GateResult generic action codes (OTA-776); gate-specific metadata off the framework GateResult (OTA-777). All three modify the same file (`app/analysis/hard_gates/__init__.py`) and ship as a single commit. Part of Feature OTA-774 — Domain coupling in engine machinery · Epic OTA-679.

## Terminal context
- This terminal: Terminal A (engine-decoupling — Feature OTA-774)
- Concurrent terminals: none
- Cross-terminal dependencies: none. This is the **first** file in the OTA-774 batch. The batch runs sequentially in one terminal: **this file → OTA-778 → OTA-780 → OTA-779.** Do not start a second terminal against any of these — they share `hard_gates/__init__.py`, `evaluation_routes.py`, and engine-core normalization.

## Phase 0 — BATCH ENTRY GATE (READ-ONLY, hard STOP)
Make no edits in Phase 0. Produce the report, then STOP and wait for go/no-go. Paste the report back to Claude Web.

1. **Doc locations.** Confirm these exist: `claude_context/CLAUDE.md`, `claude_context/architecture-plan.md`, `claude_context/insight_engine.md`, `claude_context/insight_engine-migration-plan.md`. If any is at a different path, locate it (`Get-ChildItem -Recurse -Filter <name>`) and report the actual path.
2. **F1 engine core (OTA-695) present.** Confirm `app/insight_engine/` exists and report the paths for: the generic `Candidate` type, the engine config object, and the single engine-core normalization function. This batch assumes F1 has landed.
3. **OTA-766 landed.** Confirm the strategy-independence cleanups are in on this branch — no remaining `if strategy == ...` branches in engine / adapter / rule code that this decoupling assumes are already gone. Report any you still find.
4. **Gate framework.** Confirm `app/analysis/hard_gates/__init__.py` defines `GateTradeContext` and `GateResult`. Report the current attribute list of each, and list every reader of `GateTradeContext.expected_value` / `.expiry_date` / `.dte` and every reader of `GateResult` verdict strings (`PASS`, `WAIT_FOR_EARNINGS`) and of `_dte_after_earnings` / `_reevaluate_on`, repo-wide (use the Grep tool).

**STOP.** Wait for go/no-go before any edit.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/architecture-plan.md
cat claude_context/insight_engine.md
cat claude_context/insight_engine-migration-plan.md
```

## Relevant Context — Do Not Deviate Without Escalation

- **Source: OTA-774 (Feature goal); migration plan, Feature 7.** After this feature, engine-machinery code references **zero** domain terms. `GateTradeContext`, `GateResult` action codes, and `GateResult` metadata carry no options-specific vocabulary. Domain meaning is supplied by the OTA adapter / domain layer, never baked into the generic engine.
- **Source: OTA-775; migration plan S7.1 (audit §5c #1, #2).** Replace `expected_value`, `expiry_date`, `dte` on `GateTradeContext` with a single generic `candidate: Candidate` reference. Gate rules access values they need by **named key off the candidate**, not hardcoded domain attributes.
- **Source: OTA-776; migration plan S7.2 (audit §5c #4).** Replace domain verdict strings (`PASS`, `WAIT_FOR_EARNINGS`) on `GateResult` with generic action codes `BLOCK` / `DEFER` / `MODIFY` / `OK`. The **OTA domain layer** owns the action-code → verdict-string mapping (e.g. `DEFER` → `WAIT_FOR_EARNINGS`). The engine's `GateResult` never names a domain verdict.
- **Source: OTA-695 "Confirmed architectural assumptions"; insight_engine.md §4 / OD-2.** `WAIT_FOR_EARNINGS` is an earnings-gate **terminal halt verdict**, not a fourth score band. A stopping gate may emit a terminal verdict that bypasses the Phase-7 band lookup (driven by the junction `terminal_verdict` column). The action-code refactor must **preserve** this: the earnings gate returning `DEFER` must still resolve — via the domain mapping — to the `WAIT_FOR_EARNINGS` terminal verdict downstream. Do not collapse `DEFER` into a non-terminal outcome.
- **Source: OTA-777; migration plan S7.3 (audit §5c #5).** `_dte_after_earnings` and `_reevaluate_on` come **off** the framework `GateResult` and land on either a gate-specific result extension (subclass) or a generic `metadata: dict`. The framework `GateResult` declares no gate- or domain-specific named fields.
- **Source: CLAUDE.md (Document Governance + House Style).** Do not modify any SoT doc. Commit only when this prompt instructs and Don approves. Commit-message format `OTA-NNN feat: …`. PowerShell for all shell commands.

## OTA-775 — Scope
Refactor `GateTradeContext` so it carries a single `candidate: Candidate` reference instead of `expected_value` / `expiry_date` / `dte`. Update every gate that read those attributes to obtain the values through the candidate's named-key accessor.

### OTA-775 — Acceptance criteria
- `GateTradeContext` no longer declares `expected_value` / `expiry_date` / `dte` (or any other domain-specific field).
- Gates obtain those values through the generic `candidate` reference, addressed by named key.
- Grep confirms no domain-named fields remain on `GateTradeContext`.

### OTA-775 — Out of scope
No new gate logic, no threshold changes, no new named values invented. If a needed named value isn't published by the adapter/catalog, STOP and report — do not synthesize it here.

## OTA-776 — Scope
Replace the verdict strings on `GateResult` with the generic action codes `BLOCK` / `DEFER` / `MODIFY` / `OK`. Add (or extend) the OTA domain-layer mapping that turns an action code back into the domain verdict string, preserving the `WAIT_FOR_EARNINGS` terminal-halt behavior described above.

### OTA-776 — Acceptance criteria
- `GateResult` carries only the generic action codes — no domain verdict strings.
- The domain layer owns the action-code → verdict-string mapping.
- Grep confirms no `PASS` / `WAIT_FOR_EARNINGS` (or other domain verdict) literals on the framework `GateResult`.
- The earnings gate still produces a `WAIT_FOR_EARNINGS` terminal verdict downstream via the mapping (manual check).

### OTA-776 — Out of scope
Do not change verdict-band thresholds (70/50) or the junction `terminal_verdict` mechanism. This is a code-rename + mapping relocation, not a verdict-semantics change.

## OTA-777 — Scope
Move `_dte_after_earnings` and `_reevaluate_on` off the framework `GateResult` onto a gate-specific result extension or a generic `metadata: dict`. Update the producers (earnings gate) and consumers accordingly.

### OTA-777 — Acceptance criteria
- `_dte_after_earnings` / `_reevaluate_on` are removed from the framework `GateResult`.
- The values are carried on a gate-specific result extension or a generic `metadata: dict`.
- The framework `GateResult` declares no gate- or domain-specific named fields.

### OTA-777 — Out of scope
No change to what the earnings gate computes — only where the values are carried.

## Verification steps
Run after all three stories' edits. This prompt fails if any criterion fails.

1. PowerShell field checks (scope reading to the class bodies in the report, not just the file):
   - `Select-String -Path app/analysis/hard_gates/__init__.py -Pattern 'expected_value|expiry_date|dte'` → no hits on `GateTradeContext`.
   - `Select-String -Path app/analysis/hard_gates/__init__.py -Pattern 'PASS|WAIT_FOR_EARNINGS'` → no hits on `GateResult`.
   - `Select-String -Path app/analysis/hard_gates/__init__.py -Pattern '_dte_after_earnings|_reevaluate_on'` → no hits on the framework `GateResult`.
2. Repo-wide Grep (Grep tool) for callers of the removed attributes/strings — all updated, none dangling.
3. **QA Level 2 (regression).** Run the backend test suite. Manually exercise gate evaluation on a screening run and confirm: a normal candidate yields `OK`/`BLOCK` mapped to the same verdict as before; an earnings-window candidate still yields the `WAIT_FOR_EARNINGS` terminal verdict via the new `DEFER` → verdict mapping. Note the Level applied in the commit body.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
OK to continue to OTA-778.md

## Commit message template
OTA-775 OTA-776 OTA-777 feat: decouple gate framework — generic candidate ref, action codes, metadata off GateResult
