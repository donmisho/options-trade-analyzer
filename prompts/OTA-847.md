---
allowedTools:
  - Read
  - Grep
  - Glob
  - Edit
  - Bash(cat*)
  - Bash(python*)
  - Bash(pytest*)
  - Bash(grep*)
  - Bash(ls*)
---

# OTA-847 — reconcile screening seed named-values/units against the options-chain adapter catalog

## Terminal context
- This terminal: Terminal A (single)
- Concurrent terminals: none
- Branch: `OTA-836-build-to-testable` only. Do NOT create a branch. Do NOT commit — Don holds the commit gate.
- **Sequencing:** runs AFTER OTA-843 is committed in this same terminal. Both touch the screening seed/config — never run concurrently with OTA-843. If OTA-843 edited the seed, re-seed before measuring here.

## Required reading
Before any changes:

```
cat claude_context/CLAUDE.md
cat claude_context/insight_engine.md     # §4 pipeline / data-completeness; §6.6 named-value catalog + startup validation
cat claude_context/business-rules.md      # screening named values: delta, cushion, ATR — definitions and units
```

Then inspect (do not assume — read before editing):

```
# the options-chain (screening) adapter where emitted named values + units are declared
ls app/ota_adapters
# the screening seed/config + SP/TR rule configs where named values are referenced
grep -rn "delta\|cushion\|atr" app/ota_adapters app/options_rules scripts/seed_engine_config.py
cat tests/insight_engine/test_cross_consumer_system.py
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: OTA-847.** Live credit-spread screening halts at data-completeness because seeded rule named-values do not match the options-chain adapter's emitted names/units → candidates are dropped before scoring/verdict → the **"Trades returns no recommendations"** regression. **The adapter's emitted catalog is the source of truth.**

**Suspected mismatches — CONFIRM in Phase 0, do not presume the resolution:**
- `delta` (seed) vs `long_delta` / `short_delta` (adapter emits leg-specific deltas)
- `cushion_vs_atr_ratio` — naming inconsistency between seed and adapter
- `cushion_pct` — units mismatch (fraction vs percent)

**Source: OTA-841.** The engine consumes canonical hyphen+lowercase keys; unknown keys fail closed (422). Confirm whether any apparent mismatch is actually a key-format/canonicalization issue rather than a genuine name or unit difference.

**Fix:** align the screening seed/config named-values and units to the adapter's actual emitted catalog so a representative candidate passes data-completeness and reaches a verdict. Then add/confirm a §6.6 named-value-catalog check so this class of error fails **loud at load** rather than silently dropping candidates at runtime.

**Invariants:** named values come only from the adapter catalog; fail-closed; single engine path; no per-strategy branching.

**Hard dependency:** OTA-843 must be committed first — the screening config must load/validate and the EV-gate formula must resolve before any candidate can reach the data-completeness stage this story fixes.

## Phase 0 — Read-only discovery (HARD GO/NO-GO STOP)

Make NO edits. Answer A–F, then STOP and report.

A. **Adapter catalog (authoritative).** From the options-chain/screening adapter source, list the exact named values it emits — names AND units. This is the target every seed reference must match.

B. **Seed references.** List the named values the SP/TR screening seed/configs actually reference (names + assumed units).

C. **Precise diff.** Every seed-referenced name/unit that does NOT match the adapter catalog. Confirm or refute the three suspected (`delta`, `cushion_vs_atr_ratio`, `cushion_pct`); surface any others.

D. **Classify each mismatch + target.** For each diff item, classify as: pure rename / unit mismatch (fraction vs percent) / key-format canonicalization (OTA-841) / leg-specificity (`delta` → `long_delta`+`short_delta`). State the correct adapter-aligned target for each.

E. **Write path + 843 overlap.** Is the fix in `scripts/seed_engine_config.py`, a rule-library config module, or both? Confirm whether OTA-843 already edited those files on this branch; if it touched the seed, plan a re-seed. Report exact write paths.

F. **Validation hook.** Where is the data-completeness check, and does a §6.6 named-value-catalog validation already exist? If absent, report where it should hook — do not build it yet, just report.

**GO only if:** adapter catalog enumerated · diff precise · each mismatch's correct target + write path known. **NO-GO and STOP** otherwise.

## Phase 1 — Reconcile named values (after GO)
- Align the seed/config named-values + units to the adapter catalog per the Phase-0 diff. Adapter is the source of truth — only change an adapter name if the adapter itself is demonstrably wrong, and flag that to Don rather than fixing silently.
- Re-seed; confirm the screening config loads and validates.

## Phase 2 — Fail-loud validation
- Add/confirm the §6.6 named-value-catalog check so a future name/unit mismatch is rejected at load with a structured error, not silently dropped at runtime. (This converts OTA-847's whole failure class from a silent runtime drop into a loud load-time rejection.)

## Phase 3 — Verify

```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1

python scripts/seed_engine_config.py
pytest tests/insight_engine/test_cross_consumer_system.py -q
pytest tests/insight_engine/test_config_validation_matrix.py tests/insight_engine/test_trace_and_bronze.py -q
pytest tests/insight_engine/test_full_seed_boot.py -q
```

- A representative credit-spread fixture candidate passes data-completeness and reaches a verdict through the real engine path (offline fixture — not a live Schwab call).
- A regression fixture covers the previously-mismatched named values.
- The new §6.6 check rejects a deliberately-mismatched fixture with a structured error.
- OTA-799 trio + boot test stay green.
- **Note:** this greens the offline path; the live in-app "Trades returns recommendations" smoke test is Don's manual step after deploy.

## Acceptance criteria
- Seeded screening named-values/units match the adapter catalog (`long_delta`/`short_delta`, `cushion_vs_atr_ratio`, `cushion_pct` reconciled per the Phase-0 diff).
- A representative live credit-spread fixture passes data-completeness and reaches a verdict through the real path.
- A regression fixture covers the previously-mismatched named values; a §6.6 named-value-catalog check rejects mismatches at load.
- OTA-799 trio and the seed-boot test remain green.

## Out of scope
- The EV-gate formula (OTA-843); position-health; grade parity.
- The live Schwab / in-app smoke test (Don's manual gate).
- Broad screening business-logic changes beyond name/unit reconciliation.

## Verification steps
See Phase 3.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency. Completes the screening-repair pair; the in-app live "Trades returns recommendations" smoke test is Don's manual step.

## Commit message template (if committing)
OTA-847 fix: reconcile screening seed named-values/units to options-chain adapter catalog; add §6.6 named-value-catalog check
