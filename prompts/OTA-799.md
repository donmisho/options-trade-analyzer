---
allowedTools:
  - Read
  - Grep
  - Glob
  - Edit
  - Bash(cat*)
  - Bash(python*)
  - Bash(pytest*)
  - Bash(grep*)
  - Bash(ls*)
---

# OTA-799 — CI wiring and coverage gate for the engine-validation suites

## Terminal context
- This terminal: Terminal A (single)
- Concurrent terminals: none
- Cross-terminal dependencies: none. This touches only `.github/workflows/` + test/coverage config. Do not run it in parallel with any prompt that edits CI YAML.

## Required reading
Before any changes:

```
cat claude_context/CLAUDE.md
cat claude_context/insight_engine.md           # §4 pipeline/result record, §4.2 trace, §6.6 startup validation — the contracts these suites guard
cat claude_context/deployment-workflow.md       # CI topology: GitHub Actions, build-on-push.yml, branch/slot model
cat claude_context/build-execution.md           # commit gate + coordination footer rules
```

Then read what you are wiring (do not modify these test files in this story):

```
ls .github/workflows/
cat .github/workflows/build-on-push.yml
ls tests/insight_engine tests/ota_adapters tests/integration tests/options_rules
```

## Relevant Context — Do Not Deviate Without Escalation

**Source: OTA-799 (this ticket).** Wire the OTA-794 validation suites into CI so they run on every change across the engine core, the adapters, and the rule libraries: the cross-consumer suite (OTA-795), golden-path parity (OTA-796), the config matrix (OTA-797), and the trace/bronze assertions (OTA-798). Add a coverage gate spanning those packages. A failing validation suite blocks merge. CI must be reasonably fast / parallelized.

**Source: live Jira state (2026-06-19).** Three of the four suites are committed and at Code & Test Complete — OTA-795, OTA-797, OTA-798. **OTA-796 (golden-path parity) is NOT built yet** (status: Write Prompt; its test file does not exist on the branch). Therefore: wire 795/797/798 to run unconditionally, and include the 796 parity job **guarded so it is skipped (not failed) when its test path is absent**, so this workflow ships now and the parity job self-activates the moment OTA-796's test lands — no re-issue of this prompt.

**Source: insight_engine.md §4 / §4.2 / §6.6.** These suites exercise the *real* engine + adapters + rule libraries + seeded config (no mocks of the evaluation path). The CI job must run them against the same path, not a mocked substitute. Do not introduce a parallel/mocked evaluation path to make CI green.

**Source: CLAUDE.md (single-path invariant).** No second evaluation path, no domain branching in engine machinery. This story adds CI config only — it must not touch `app/insight_engine/`, any adapter, any rule library, or any seed script.

**Source: deployment-workflow.md.** CI is GitHub Actions; workflows live in `.github/workflows/`. `build-on-push.yml` builds an artifact on push to `main` and does not deploy. This story adds a **separate** validation/coverage workflow; it must not alter the build/deploy/slot-swap workflows.

**Coverage threshold is a Don decision.** The ticket specifies the threshold is "set with Don." Do NOT pick a number unilaterally. Surface a recommended threshold in Phase 0 and STOP for Don's call before wiring the gate.

## Phase 0 — Read-only discovery (HARD GO/NO-GO STOP)

Make NO edits. Answer, then STOP and report A–F.

A. **Branch.** Confirm the branch the three committed suites (795/797/798) live on, and that this is the branch CI should target. Report the branch name.

B. **Existing CI.** Enumerate `.github/workflows/`. Is there already a pytest/test job (in `build-on-push.yml` or elsewhere), or is the engine suite currently un-run in CI? Report what exists so the new workflow extends rather than duplicates.

C. **Suite paths.** Resolve the actual file/dir paths for each suite and confirm collection:
   - OTA-795 cross-consumer system suite
   - OTA-797 config-validation matrix
   - OTA-798 trace/bronze persistence assertions
   - OTA-796 parity — confirm it does **not** yet exist; report the path it is expected to occupy so the guarded job can reference it.
   Run `pytest --collect-only` on the three existing suites and report counts.

D. **Coverage tooling + packages.** Is `pytest-cov` (or equivalent) already a dev dependency? Where is pytest configured (`pyproject.toml` / `pytest.ini` / `setup.cfg`)? Identify the exact package roots the gate should span — the engine core, the adapters, and the rule libraries (e.g. `app/insight_engine`, `app/ota_adapters`, and the rule-library packages). List them precisely.

E. **Recommended threshold.** Run the suites once locally with coverage over those packages and report the current measured coverage number, then recommend a gate threshold at or just below it (so the gate passes today and ratchets up later). **This is the number Don must approve.**

F. **Known reds.** OTA-843 (`negative_ev_gate` unregistered) leaves 11 engine tests red on `main`, and OTA-847 (screening seed↔adapter mismatch) is open. Confirm whether the 795/797/798 suites are independent of those reds (they should be — they are at Code & Test Complete and passing). If the CI scope as written would pull in the OTA-843 reds, report exactly which paths and STOP — we do not want CI permanently red on known-parked bugs. Propose the narrowest selection that runs the three suites green.

**GO only if:** branch confirmed · suite paths resolve and collect green · coverage tooling identified · a threshold recommendation is ready for Don · the CI selection is green today (no OTA-843/847 reds pulled in). **NO-GO and STOP** if any of these fail, especially F.

## Phase 1 — Validation workflow (after GO + Don's threshold)

- Add a new GitHub Actions workflow (e.g. `.github/workflows/ci-engine-validation.yml`) triggered on `push` and `pull_request` against the confirmed branch line.
- It runs the three existing suites (795/797/798) unconditionally. A failure fails the job (blocks merge).
- It includes a parity job/step for OTA-796 that is **guarded**: if the parity test path is absent, the step is skipped with a clear log line ("OTA-796 parity suite not present — skipping; will activate when the test lands"), not failed. When the file exists, it runs and a failure blocks merge like the others.
- Parallelize where it doesn't add flakiness (e.g. matrix or separate jobs per suite) to keep wall-clock down.

## Phase 2 — Coverage gate

- Wire `pytest-cov` (add to dev deps only if missing) over the exact package roots identified in Phase 0-D.
- Enforce the Don-approved threshold; the build fails when coverage is below it.
- The coverage gate measures line coverage of the engine/adapter/rule-library packages — it is independent of position-health *grade* parity (which is intentionally divergent per OTA-844 and is NOT a CI gate here).

## Phase 3 — Verify

```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer"
.\venv\Scripts\Activate.ps1

# the exact selection confirmed green in Phase 0-F, with coverage over the Phase 0-D packages:
pytest <confirmed-795-path> <confirmed-797-path> <confirmed-798-path> --cov=<pkg1> --cov=<pkg2> --cov-fail-under=<DON-THRESHOLD> -q
```

- Confirm the selected suites pass and the coverage gate passes at the approved threshold.
- Validate the workflow YAML parses (lint / `python -c` YAML load, or `act` if available — do not require Docker).
- Confirm the guarded 796 step skips cleanly (parity file still absent).
- Confirm no build/deploy workflow YAML was modified.

## Acceptance criteria

- A CI workflow runs OTA-795/797/798 on push and PR across the engine, adapters, and rule-library packages; a failing suite blocks merge.
- The OTA-796 parity job is present but skips gracefully until its test file exists, then runs and gates.
- A coverage gate spans the Phase 0-D packages at the Don-approved threshold and fails the build when not met.
- CI is parallelized/scoped so it does not stall the workflow and does not turn red on the OTA-843/847 parked reds.
- No engine, adapter, rule-library, seed, or build/deploy file is modified.

## Out of scope

- Building the OTA-796 parity suite (separate Story).
- Fixing OTA-843 / OTA-847 (separate Bugs).
- Any change to `build-on-push.yml`, `deploy-to-dev.yml`, `deploy-to-prod.yml`, `swap-staging-to-prod.yml`, or `rollback-prod.yml`.
- Any change to engine/adapter/rule-library/seed source.

## Verification steps
See Phase 3.

## Commit instruction
I have been instructed to commit. Do you approve? (yes / no)

## Coordination footer
Independent — no downstream dependency. (The OTA-796 parity job is pre-wired and self-activates when that suite lands; no follow-up CI prompt is required for it.)

## Commit message template (if committing)
OTA-799 ci: wire engine-validation suites (OTA-795/797/798) into CI with coverage gate; guarded OTA-796 parity job
