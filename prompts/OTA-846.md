# OTA-846 — Add "Configuration" left-nav section grouping admin screens (Strategy Admin + System Settings)

## Terminal context
- This terminal: single terminal. Frontend-only.
- Concurrent terminals: none required. Touches the Layout / nav-rail component — if any other in-flight story edits the same nav file, sequence behind it; otherwise standalone. Independent of the OTA-794 validation set (different surface).
- Branch: per Don at issue time. Do NOT create a new branch. Do NOT `git commit` — Don holds the commit gate.

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md
cat claude_context/UI-GUIDANCE.md
```

Then locate and read (Phase 0) the nav-rail/Layout component, the router config, and the System Settings cabinet component + its current launcher:

```
grep -rniE "Dashboard|STRATEGIES|Steady Paycheck|Sign out" web/src
grep -rniE "strategy-admin|System Settings|Settings" web/src
```

## Relevant Context — Do Not Deviate Without Escalation

Source: `UI-GUIDANCE.md` Part 2 (Navigation) — **HARD RULE**
> "Four primary nav items. … Never add more primary nav items." / "Never add a fifth primary nav item."
The four primary items (Dashboard, Security Strategies, Trades, Positions) are **fixed**. **CONFIGURATION is a SECTION, modeled exactly on the existing "STRATEGIES" section** (a section header + indented sub-items) — it is **NOT** a primary nav item and must not be implemented as one. Do not touch the four primary items.

Source: `UI-GUIDANCE.md` Part 2 — rail structure
Current rail order: logo → 4 primary items → `STRATEGIES` section (4 strategies) → bottom group (`● Schwab Connected`, `⚙ Settings`, `Sign out`). The repo also renders a `Change Log` bottom item (added after the doc snapshot) — **leave Change Log exactly where it is.** Left rail is **200px fixed**.

Source: `UI-GUIDANCE.md` Part 2 — section + item styling
- Section title (mirror "STRATEGIES"): 10px, weight 400, uppercase, letter-spacing 0.6px, muted (`var(--muted)`).
- Nav items: 12px, weight 400.
- Active state: 3px solid `var(--teal)` (#2dd4bf) left border, teal text, background `rgba(45,212,191,0.08)`. Inactive: 3px solid transparent, `var(--muted)` (#8b949e) text, no background. (Applies to the routed item.)

Source: `UI-GUIDANCE.md` Part 3 / Part 3a — color
Use CSS variables, never hardcoded hex where a variable exists (`--teal`, `--muted`, `--text`, `--border`, `--bg3`). `--bg2` is RESTRICTED — **never** on section backgrounds or group headers. The CONFIGURATION header gets no `--bg2` background.

Source: OTA-784 (deployed)
The Strategy Admin editor is a **route** at `/strategy-admin` inside the existing Layout shell. Confirm the route is registered and use the same NavLink/active-state mechanism the other routed items use.

Source: story OTA-846 + screenshot
The System Settings cabinet is a **slide-out drawer** (application-wide defaults), currently opened by the bottom `⚙ Settings` gear. It stays a cabinet — the new `System Settings` entry reuses the **existing** open handler. After wiring it into CONFIGURATION, **remove the now-redundant bottom `⚙ Settings` launcher** (single entry point). No role-gating (single-operator app).

## Scope

Add a new `CONFIGURATION` section to the left nav rail, mirroring the `STRATEGIES` section pattern, containing two entries:
1. **Strategy Admin** — a routed NavLink to `/strategy-admin` with standard active-state.
2. **System Settings** — a launcher that opens the existing System Settings cabinet (reusing its current open handler).

Consolidate the bottom `⚙ Settings` launcher into the new `System Settings` entry (remove the duplicate). Leave the four primary items, the `STRATEGIES` section, `● Schwab Connected`, `Change Log`, and `Sign out` unchanged.

## Acceptance criteria

* A `CONFIGURATION` section header (styled identically to `STRATEGIES`) appears in the rail, placed per UI-GUIDANCE (peer to `STRATEGIES`).
* `Strategy Admin` routes to `/strategy-admin` and shows active-state when on that route, consistent with other routed items.
* `System Settings` opens the existing System Settings cabinet via its current handler (cabinet unchanged).
* The redundant bottom `⚙ Settings` launcher is removed — exactly one way to open System Settings.
* No fifth **primary** nav item is added; the four primary items are untouched.
* `Change Log`, `Sign out`, and `Schwab Connected` are unchanged and in their current positions.
* Styling uses CSS variables (no hardcoded hex where a variable exists); no `--bg2` on the section header.

## Out of scope

* Converting the System Settings cabinet into a routed page.
* Any change to the Strategy Admin screens, the System Settings cabinet contents, or the config/settings APIs.
* Moving `Change Log`.
* Any change to the four primary nav items.

## Phase 0 — Read-only discovery (HARD GO/NO-GO STOP)

Make NO edits. Answer, then STOP and report A–E.

1. Identify the nav-rail/Layout component file and how the `STRATEGIES` section + its items are rendered (the exact pattern CONFIGURATION must mirror — section header element + item component).
2. Confirm `/strategy-admin` is a registered route (OTA-784) and identify the NavLink/active-state mechanism in use.
3. Locate the System Settings cabinet component and the current open handler/state behind the bottom `⚙ Settings` gear, so the new entry reuses it and the old launcher can be cleanly removed.
4. Confirm no role/permission gate must be honored for either entry; confirm the CSS variables named above exist.
5. Identify any existing rail/nav UI test (OTA-793 added admin UI tests) to extend with a CONFIGURATION-section assertion.

GO only if: nav-rail pattern located · `/strategy-admin` route confirmed · System Settings open handler located · no gating surprises. NO-GO and STOP otherwise.

## Verification steps

```powershell
cd "C:\Users\DonMishory\OneDrive - jmholistic.com\VS Code Projects\Options Analyzer\options-analyzer\web"
npm run build
npm test    # or the frontend test script in web/package.json
```

Manual: rail shows `CONFIGURATION` with `Strategy Admin` + `System Settings`; Strategy Admin navigates to `/strategy-admin` and highlights; System Settings opens the cabinet; the bottom `⚙ Settings` is gone; `Change Log` unchanged; the four primary items unchanged.

## Manual commit gate

STOP. Summarize the staged diff (files touched, line counts) and the verification results. Do NOT commit yourself — Don commits with the `OTA-846 feat:` prefix.

## Coordination footer

Independent — no downstream dependency. Single terminal; frontend-only. If another in-flight story edits the same nav/Layout file, sequence behind it.

## Commit message template (Don commits)

```
OTA-846 feat: add Configuration nav section (Strategy Admin route + System Settings cabinet), consolidate Settings launcher
```
