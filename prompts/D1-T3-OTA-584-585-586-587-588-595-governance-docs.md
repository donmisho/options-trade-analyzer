# OTA-584 + OTA-585 + OTA-586 + OTA-587 + OTA-588 + OTA-595 — Governance docs cluster

## Deployment context
- Deployment: **D1**
- This terminal: **T3**
- Concurrent terminals: T1 (agent prompt template; disjoint files), T2 (verdict integrity bug cluster; disjoint files)
- Cross-terminal dependencies: **none** — T3 owns the `claude_context/` directory and the repo-root `CLAUDE.md` pointer; T1 and T2 do not touch these

## Required reading
Before any code changes:

```
cat claude_context/CLAUDE.md          # the file OTA-584 will replace — read current state first
cat claude_context/bugfix.md          # OTA-585 refresh target
cat CLAUDE.md                          # repo-root pointer — OTA-595 refresh target
```

Plus the source drafts from the previous documentation-governance session:

```
ls /mnt/user-data/outputs/             # confirm presence of the source drafts referenced below
cat /mnt/user-data/outputs/CLAUDE.md                # source for OTA-584 (canonical claude_context/CLAUDE.md)
```

If the `/mnt/user-data/outputs/` source drafts are not present on this machine, **stop and report** — Don will re-supply the drafts before this terminal proceeds.

## Relevant Context — Do Not Deviate Without Escalation

**Source: prompt-style.md § Mechanism A + Mechanism B**
Every Claude Code prompt must include both `cat` commands (Mechanism A) and embedded relevant context (Mechanism B). This convention is project-agnostic; the project-specific per-domain required-reading table lives in each project's `CLAUDE.md`. Do not duplicate the convention text into the OTA `CLAUDE.md`; pointer-to-profile-level is correct.

**Source: jira-structure.md § Hierarchy**
Active hierarchy across all projects on this profile is **Epic → Story → Subtask**. Feature is not used. Any "Feature" reference in older content is obsolete. Stories parent directly to Epics.

**Source: build-execution.md § Part 4 Deployment Recording**
Every successful non-local deploy must produce an in-app deploy-log record with: Build ID, Environment, Deployment timestamp (UTC ISO 8601), Commit SHA(s), Ticket keys. Implementation belongs to each project's `deployment-workflow.md`. For OTA, the implementation lives under OTA-511.

**Source: CLAUDE.md § Documentation discipline (recurring theme)**
Documents state what IS, never what was considered or what changed. Historical content belongs in version control / Change Logs. Each document has a single clearly bounded responsibility.

**Source: CLAUDE.md § Change Log convention**
Each Governance Document carries a `Change Log` table at the end with columns: Date, Story, Change. Every substantive edit adds a row referencing the Story that authorized the edit.

---

## Execution order within this terminal

Sequential. One commit at the end covering all six tickets.

1. **OTA-584** first — replace `claude_context/CLAUDE.md` with the slimmed restructured draft (foundational; downstream files reference its new pointer structure)
2. **OTA-586** — create `claude_context/product-roadmap.md` (NEW)
3. **OTA-587** — create `claude_context/development-environment.md` (NEW)
4. **OTA-588** — create `claude_context/deployment-workflow.md` (NEW)
5. **OTA-585** — light refresh of `claude_context/bugfix.md` (cross-references the new files; do last among `claude_context/` edits)
6. **OTA-595** — refresh root `CLAUDE.md` pointer (depends on the 12-doc SoT inventory being final)

Do not commit between files. If any Story fails verification, stop and report.

---

## OTA-584 — Apply CLAUDE.md restructure

### Scope

Replace `claude_context/CLAUDE.md` with the restructured draft at `/mnt/user-data/outputs/CLAUDE.md` (from the documentation governance rebuild session).

### Changes vs current

- Hierarchy fixed: Epic → Story → Subtask, no Feature
- 6 sections extracted to pointers: `prompt-style.md`, `build-execution.md`, `product-roadmap.md`, `development-environment.md`, `deployment-workflow.md`, `jira-structure.md`
- Document Governance Rules section added at top
- Profile-Level Conventions subsection added (lists `jira-structure`, `prompt-style`, `build-execution`, `claude-web`)
- Active Cleanup Items dated with valid-until 2026-06-30
- Post-Build QA Gate gets `Last Reviewed: 2026-05-06` with 60-day cadence
- "Common Epic parents" list removed
- Claude Code "review Jira plan?" prompt removed from Session Start
- Bug type ID confirmed as **10215**
- Drops from ~659 lines to ~290

### Acceptance criteria

- File `claude_context/CLAUDE.md` is replaced wholesale.
- `grep -i "feature" claude_context/CLAUDE.md` returns no matches.
- All 6 extracted sections appear as one-paragraph pointers in their place.
- Change Log table at bottom carries a row dated 2026-05-10 referencing OTA-584.

### Out of scope

- Profile-level docs (those are Don's direct work — OTA-589, 590, 591, 592, 594).
- Any change to the repo-root `CLAUDE.md` (OTA-595 covers that, later in this terminal).

---

## OTA-586 — Create product-roadmap.md (NEW)

### Scope

Create `claude_context/product-roadmap.md`. Content sourced from the current `claude_context/CLAUDE.md`'s Product Roadmap (OTAR) section before it was removed by OTA-584.

### Contents

- The OTA ↔ OTAR relationship (OTAR is a separate JPD project; OTA Epics link to OTAR Categories via Polaris work item links)
- All active OTAR Categories (OTAR-7 through OTAR-24) with one-line scope summaries — copy from current CLAUDE.md or reconstruct from OTAR project if needed
- Note OTAR-27 (TMTC Application Framework) as cross-project Category
- Procedure for linking new Epics to Categories (use `createIssueLink` with type "Polaris work item link", `inwardIssue: OTAR-X`, `outwardIssue: OTA-Y`)
- Procedure for proposing new OTAR Categories ("talk to Don first")
- OTAR URL reference
- OTAR-1 (seed template) and OTAR-13 (duplicate of OTAR-16) noted as scheduled-for-archive
- Standard header (Scope, Last Updated 2026-05-10, Governing Story OTA-581)
- Change Log table at bottom — initial row references OTA-586

### Acceptance criteria

- New file at `claude_context/product-roadmap.md`.
- All current OTAR Categories listed.
- Linking procedure includes the inward/outward direction explicitly.

### Out of scope

- Modifying OTAR tickets themselves.
- Polaris cross-project link automation.

---

## OTA-587 — Create development-environment.md (NEW)

### Scope

Create `claude_context/development-environment.md`. Content sourced from the current `claude_context/CLAUDE.md`'s Development Environment + Zombie Process sections before they were removed by OTA-584.

### Contents

- Backend setup (FastAPI, venv conventions — `venv` not `.venv`, HTTPS startup with self-signed certs, port 8000)
- Full `cd` + `venv\Scripts\activate` (PowerShell) / `source venv/bin/activate` (Unix) commands
- Frontend setup (Vite, npm install, dev server on https://localhost:5173, proxy to backend)
- Self-signed cert generation using the Python `cryptography` library (NOT OpenSSL CLI on Windows)
- Testing setup (pytest commands, current minimal coverage state)
- **Zombie process warning** — Windows port 8000; `Get-Process python,uvicorn | Stop-Process -Force`; `netstat -ano | findstr ":8000"`
- Troubleshooting:
  - 404s after route changes → zombie process
  - HTTPS cert errors
- Standard header (Last Updated 2026-05-10, Governing Story OTA-582)
- Change Log table — initial row references OTA-587

### Acceptance criteria

- New file at `claude_context/development-environment.md`.
- Zombie-process workaround included verbatim with the PowerShell commands.
- `venv` (not `.venv`) convention prominently stated.

### Out of scope

- Production environment setup (that's `deployment-workflow.md`).
- IDE-specific guidance (VS Code, JetBrains, etc.).

---

## OTA-588 — Create deployment-workflow.md (NEW)

### Scope

Create `claude_context/deployment-workflow.md`. Content sourced from current CLAUDE.md's Deployment Workflow section + OTA-specific implementation scaffolding for the cross-project Deployment Recording rule (profile-level `build-execution.md` Part 4).

### Contents

- The 4-step deploy model: build-on-push (no deploy) → deploy to staging slot → smoke test → swap-staging-to-prod
- GitHub Actions confirmation tokens (`DEPLOY`, `DEPLOY-DEV`, `SWAP`, `ROLLBACK`)
- Dev environment topology (single slot, fix-forward not roll back)
- Slot-swap discipline (staging and prod share the same DB; expand/contract migration discipline)
- Rollback workflow (re-swap or redeploy a prior `build_run_id`)
- Workflow file references: `build-on-push.yml`, `deploy-to-prod.yml`, `swap-staging-to-prod.yml`, `rollback-prod.yml`, `deploy-to-dev.yml`
- Database migration discipline (Alembic, additive only, OTA-523 perpetual cleanup story)
- **Deployment Recording implementation (OTA-specific):**
  - Schema reference (`deploy_log` table)
  - Endpoint references (`POST /api/v1/changelog/record`, `GET /api/v1/changelog`)
  - UI route (`/changelog`)
  - Deploy-token (`DEPLOY_RECORDER_TOKEN`) rotation procedure
  - Detailed scope of the initial build lives in the OTA-511 Change Log Story (separate Story, not built here)
- Standard header (Last Updated 2026-05-10, Governing Story OTA-583)
- Change Log table — initial row references OTA-588

### Acceptance criteria

- New file at `claude_context/deployment-workflow.md`.
- All four GitHub Actions workflows referenced by name.
- Deployment Recording section includes schema + endpoint + UI route + token rotation procedure scaffolding.

### Out of scope

- Implementing the Change Log feature itself (that's OTA-511).
- Cloudflare Worker keep-alive details (separate concern; lives elsewhere).

---

## OTA-585 — Refresh bugfix.md

### Scope

Light alignment refresh on `claude_context/bugfix.md`. **No protocol changes.**

### Changes

- Confirm "Source of Truth" usable as synonym for "Governance Document" (both terms acceptable; do not introduce a terminology shift)
- Verify hierarchy references match Epic → Story → Subtask (no Feature)
- Verify Subtask creation language matches the new Documentation Governance Epic structure
- Confirm Epic ID **OTA-555** still correct as the parent for bug-fix session Stories
- Update cross-references to other Governance Documents now that names/locations are settled (point to the new `claude_context/product-roadmap.md`, etc., where relevant)
- Append a Change Log row referencing OTA-585

### Acceptance criteria

- No protocol changes vs current behavior (triage criteria, rollup format, commit-vs-no-commit rule unchanged).
- All cross-references resolve correctly given the new files OTA-586–588 add in this same commit.

### Out of scope

- Any change to triage criteria, rollup format, or commit-vs-no-commit rule.

---

## OTA-595 — Refresh root CLAUDE.md pointer

### Scope

Refresh the OTA repo's root `CLAUDE.md` (the bootstrap pointer Claude Code auto-loads at session start) to reflect the current 12-doc SoT inventory.

### Why

The current root pointer lists 8 SoT docs in its cohesion paragraph. After this commit's restructure, the OTA project has 12 entries:

1. `CLAUDE.md`
2. `architecture-plan.md`
3. `business-rules.md`
4. `UI-GUIDANCE.md`
5. `auth-process.md`
6. `bugfix.md`
7. `SCHWAB-LOGIN-PROCESS.md`
8. `azure-naming-conventions.md`
9. `product-roadmap.md` _(new in this commit)_
10. `development-environment.md` _(new in this commit)_
11. `deployment-workflow.md` _(new in this commit)_
12. `jira-structure.md` _(profile mirror in OTA repo per OTA-569 decision)_

The root pointer must list all 12 in its "Why this is a pointer" cohesion section.

### Source

The root pointer follows the profile-level `CLAUDEMD-root-pointer-template.md` (OTA-593 / OTA-594), tailored with OTA's specific inventory.

### Acceptance criteria

- Root `CLAUDE.md` lists all 12 current SoT docs in its cohesion paragraph.
- Bootstrap `cat claude_context/CLAUDE.md` instruction unchanged.
- Standing reminder cross-references `prompt-style.md` for Mechanism A + B.
- File is committed alongside `claude_context/CLAUDE.md` (they travel together per OTA-595 description).

### Out of scope

- Profile-level CLAUDEMD-root-pointer-template.md itself (OTA-594, Don's profile-level direct work).

---

## Combined verification steps (run before commit)

1. **File presence:**
   - `ls claude_context/CLAUDE.md` — replaced
   - `ls claude_context/bugfix.md` — refreshed
   - `ls claude_context/product-roadmap.md` — NEW, exists
   - `ls claude_context/development-environment.md` — NEW, exists
   - `ls claude_context/deployment-workflow.md` — NEW, exists
   - `ls CLAUDE.md` — root pointer refreshed
2. **No Feature references in any of the six files:**
   - `grep -i "feature" claude_context/CLAUDE.md claude_context/bugfix.md claude_context/product-roadmap.md claude_context/development-environment.md claude_context/deployment-workflow.md CLAUDE.md` — only "Feature" appearances must be in change-log entries explaining its removal, or in proper-noun contexts (e.g., "Feature flag" if any). Inspect each match.
3. **All six files carry a Change Log table** with at least one row dated 2026-05-10 referencing the Story key.
4. **Root `CLAUDE.md` lists all 12 SoT docs** (verify by counting list items in the cohesion section).
5. **Cross-references resolve:** any `claude_context/<name>.md` reference in one file corresponds to an actual file in `claude_context/` after this commit.
6. **No code files changed.** `git diff --name-only` shows only the six files listed above. If anything outside `claude_context/` or root `CLAUDE.md` is in the diff, stop and report.

If any verification fails, stop and report.

---

## Commit instruction

**I have been instructed to commit. Do you approve? (yes / no)**

One commit covers all six Stories.

## Push instruction

**DO NOT push. Single push for Deployment 1 will be coordinated by Don after all D1 terminals (T1, T2, T3) report commit.**

## Coordination footer

**Independent — no downstream dependency.** This terminal can close after committing.

## Commit message template

```
OTA-584 OTA-585 OTA-586 OTA-587 OTA-588 OTA-595 docs: governance restructure — slim CLAUDE.md, extract roadmap/dev-env/deploy docs, refresh bugfix and root pointer
```
