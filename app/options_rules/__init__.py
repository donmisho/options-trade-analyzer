"""
Options rules — domain-specific formula implementations for the Insight Engine.

Each sub-package (screening, health, directional) owns a FormulaRegistry
that the consuming application injects into the engine at startup.
"""
